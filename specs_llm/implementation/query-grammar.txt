Query Language Grammar SQL Translation v1 Implementation Specification specifies full query translation pipeline **SDK → Wire** Prisma SDK syntax pre-compiled into compact positional tuples ADR-019) **Wire → AST** Zig server deserializes wire tuples into `QueryFilter AST **AST → SQL** AST translated into SQLite `SELECT` queries specification definitive source truth for backend systems executing queries storage engine Wire Encoding (SDK → Wire) SDK transforms developer-friendly Prisma query into compact positional arrays before transmission follows same principle as path encoding (ADR-019) SDK-friendly syntax developer compact arrays wire Operator Codes query operator maps to fixed integer code Zig `Operator` enum Operator `startsWith `endsWith `notIn` `isNull` `lte `isNotNull` `contains` Condition Tuples condition encoded as positional array```
[field_index: int, op: int, value: any]
````field_index Integer schema dictionary index SDK `op` operator code `value comparison value Omitted `isNull `isNotNull` Sort Tuples descriptor encoded positional array```
[field_index: int, desc: int]
````field_index schema dictionary index `desc `0 ascending descending Transformation Examples query```typescript
// SDK input
{ where: { age: { gte: 18 }, status: { eq: 'active' } } }

// Wire output (assuming age=3, status=4)
{ "conditions": [[3, 4, 18], [4, 0, "active"]] }
```

**Query with OR:**

```typescript
// SDK input
{
  where: {
    priority: { eq: 'high' },
    or: [
      { status: { eq: 'active' } },
      { status: { eq: 'pending' } }
    ]
  }
}

// Wire output (assuming priority=2, status=4)
{
  "conditions": [[2, 0, "high"]],
  "orConditions": [[4, 0, "active"], [4, 0, "pending"]]
}
```**Null check```typescript
// SDK input
{ where: { deleted_at: { isNull: true } } }

// Wire output (assuming deleted_at=5)
{ "conditions": [[5, 11]] }
```

**Array operator:**

```typescript
// SDK input
{ where: { role: { in: ['admin', 'editor'] } } }

// Wire output (assuming role=6)
{ "conditions": [[6, 9, ["admin", "editor"]]] }
```**Nested field```typescript
// SDK input
{ where: { address: { city: { eq: 'NYC' } } } }

// Wire output (assuming address__city=7)
{ "conditions": [[7, 0, "NYC"]] }
```pagination```typescript
// SDK input
{ orderBy: { created_at: 'desc' }, limit: 50, after: 'eyJpZCI6...' }

// Wire output (assuming created_at=8)
{ "orderBy": [8, 1], "limit": 50, "after": "eyJpZCI6..." }
```Full Wire Message complete `StoreQuery```
{
  "type":  "StoreQuery",
  "id":    6,
  "table_index": 0,
  "conditions": [
    [3, 4, 18],
    [4, 0, "active"]
  ],
  "orConditions": [
    [6, 0, "admin"],
    [6, 0, "editor"]
  ],
  "orderBy": [8, 1],
  "limit":   50,
  "after":   "eyJpZCI6..."
}
```Encoding Rules SDK field Wire key Encoding `where` conditions `conditions`[field_index op value tuples `where.or` `orConditions`_index value tuples `orderBy`[field_index desc` tuple `limit` Integer (unchanged `after` String (unchanged Flattening Rules Clients submit paths nested JSON ZyncBase stores nested JSON objects flat columns query nested structure"address SDK flattens field path joining keys `__` condition tuple field `"address__city"` SQLite JSON Extractors `json_extract()->> operators reading document properties Property queries run against `__` delimited flat columns Abstract Syntax Tree wire tuples map Zig AST parser validates wire integer indices against schema field array bounds```zig
pub const QueryFilter = struct {
    conditions: []const Condition,
    or_conditions: ?[]const Condition = null,
    order_by: SortDescriptor,
};

pub const SortDescriptor = struct {
    field_index: usize,
    desc: bool,
};

pub const Condition = struct {
    field_index: usize,
    op: Operator,
    value: ?TypedValue,
};

pub const Operator = enum {
    eq, ne, gt, lt, gte, lte, contains, startsWith, endsWith, in, notIn, isNull, isNotNull
};
```Wire AST Mapping server deserializes condition tuple into `Condition struct without string lookup Tuple position AST field[0]` `Condition.field_index assignment `[1]` `Condition.op` `Operator `[2]` `Condition.value` Absent `isNull Sort tuples map `SortDescriptor` Tuple position AST field[0]` `SortDescriptor.field_index` Integer index assignment `[1]` `SortDescriptor.desc` `1` `true `0` `false` SQLite Translation Rules Core Selection collection queries operate `namespace boundaries```sql
SELECT value_msgpack 
FROM "collection_name" 
WHERE _namespace = ? 
  [AND ...conditions] 
  [AND (...or_conditions)]
ORDER BY [sorts...], id DESC
LIMIT ?
```Parameter Binding ZyncBase separate query layout data injection parameterized queries user-input values formatted into SQL strings Operator SQLite WHERE fragment `eq` `column_name = ? `ne` `column_name ? `gt`_name > `lt` `column_name <_name > ? `lte` `column_name <= ? `startsWith `column_name `endsWith` `column_name LIKE '%' `column_name IN `notIn` `column_name NOT IN `isNull` `column_name NULL` `isNotNull` `column_name NOT NULL` JSONB Array Fields Sorted Sets Array fields persisted canonical sorted unique form Query operands array equality`eq canonicalized before SQL binding equality semantics deterministic order-insensitive for arrays arrays stored as JSONB values properties queried with scalar operators `>` `LIKEZyncBase restricts array fields subset Operator Action Array Field SQLite Subquery Mapping `contains Array contains element `EXISTS (SELECT 1 json_each(column_name value = ? array equality `column_name = ? canonicalized value `isNull Undefined/Missing `column_name NULL `startsWith `gt array field `SCHEMA_VALIDATION_FAILED error scalar-field `in operand array canonicalized deduped during parsing Result semantics order-insensitive duplicate-insensitive Combining Conditions standard filter contains `conditions `orConditions array Example Wire Payload```json
{
  "conditions": [[2, 0, "high"]],
  "orConditions": [[4, 0, "active"], [4, 0, "pending"]]
}
```SQL Form Generation translation engine appending `AND` root conditions isolating `orConditions parenthetical block```sql
SELECT value_msgpack FROM "tasks"
WHERE _namespace = ?
  AND priority = ?
  AND (status = ? OR status = ?)
```Sorting Rules ZyncBase supports sorting multiple fields `orderBy` field single[field, desc] tuple or array multi-field sorting Tie-breaking pagination ZyncBase appends `id DESC` ASC` final tie-breaker if not primary sort field Mapping to SQL `SortDescriptor translates comma-separated fragment `ORDER BY` clause```sql
ORDER BY priority DESC, created_at ASC, id DESC
```
