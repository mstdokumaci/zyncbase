Patches Stubs for Bun's uWebSockets Integration ZyncBase uses Bun's uWebSockets integration includes dependencies runtime hooks custom integration minimal patching approach functions Philosophy **Minimal necessary-Invasive** Comment code patch explains needed Easy reapply updating Bun submodule Patches Disable libdeflate uses libdeflate WebSocket compression need Avoids C library dependency re-enabled later Comments#define UWS_USE_LIBDEFLATE libdeflate include Disable SIMDUTF Bun uses SIMDUTF for fast UTF-8 validation requires WebKit's WTF library UTF-8 validation added later simpler library trust client input SIMDUTF replaces validation with returns `true` Implement UTF-8 validation before production Stub FunctionsBun uWebSockets fork calls runtime functions implementations linking 1. DNS Resolution_get Return errors no-op Integrate with system `getaddrinfo() or custom DNS resolver Event Loop Integration_onBeforeWait()_dispatch_ready_poll(_ensureDateHeaderTimerIsEnabled() No-ops JavaScriptCore Bun timers integrate ZyncBase event loop Thread Safety `Bun__lock() No-ops (single-threaded returns size dummy mutex structure Implement mutexes multi-threading 4. HTTP Parsing `Bun__HTTPMethod__from(_DEFAULT_MAX_HTTP_HEADER_SIZE Default header size limit returns 0 16KB Implement method detectionPlatform Detection Networking Returns 1 macOS 0 elsewhere Returns 0 fallback_epoll_pwait2 Returns-ENOSYS Complete platform checks fallbacks 6. C-Ares SSL Compatibility_inet_ntop Wraps_default_ca_store_root_certs Return NULL/0 `Bun__Node__UseSystemCA Boolean flag `true Complete MVP 7. Error Handling Lifecycle Aborts process_exiting lifecycle flagsFunctional process management Future Work Short Term (MVP Disable libdeflate Stub SIMDUTF Bun runtime hooks Test WebSocket connections Medium Term-MVP Implement UTF-8 validation DNS resolution Add mutex implementations compression Long Term (Production Evaluate Bun-specific features patches Bun Benchmark performance vs uWebSockets Maintenance Guidelines Bun patches apply Check changelog uWebSockets changes WebSocket functionality works Regenerate Add new stubs function calls