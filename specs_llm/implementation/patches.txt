Patches Stubs for Bun's uWebSockets Integration ZyncBase uses Bun's uWebSockets integration includes dependencies runtime hooks need custom integration minimal patching approach functions Philosophy **Minimal Patches** necessary **Non-Invasive** Comment code **Documented** Each patch explains needed **Maintainable** Easy reapply updating Bun submodule Patches Disable libdeflate/PerMessageDeflate Bun uses libdeflate for WebSocket compression disable don't need compression MVP Avoids C library dependency re-enabled later if needed Comments#define UWS_USE_LIBDEFLATE 1 libdeflate include Disable SIMDUTF-disable-simdutf.patch/WebSocketProtocol Bun uses SIMDUTF for fast UTF-8 validation SIMDUTF requires WebKit's WTF library UTF-8 validation added later simpler library trust client input Comments SIMDUTF replaces validation with returns `true` Implement UTF-8 validation before production use Stub Functions_stubsBun's uWebSockets fork calls-specific runtime functions implementations linking Categories 1. DNS Resolution `Bun__addrinfo_get(_set_freeRequest_getRequestResult( Return errors no-op (DNS not implemented Integrate with system `getaddrinfo() or custom DNS resolver Event Loop Integration `Bun__JSC_onBeforeWait()_dispatch_ready_poll()_ensureDateHeaderTimerIsEnabled() No-ops JavaScriptCore Bun timers integrate with ZyncBase's event loop 3. Thread Safety `Bun__lock() No-ops (single-threaded returns size dummy mutex structure Implement mutexes adding multi-threading 4. HTTP Parsing `Bun__HTTPMethod__from()_DEFAULT_MAX_HTTP_HEADER_SIZE` Default header size limit (16KB). returns 0 set to 16KB Implement method detection5. Platform Detection Networking Returns 1 macOS 0 elsewhere `Bun__isEpollPwait2SupportedOnLinuxKernel Returns 0 fallback_epoll_pwait2 Returns-ENOSYS Complete platform checks fallbacks 6. C-Ares SSL Compatibility_inet_ntop( Wraps `inet_ntop_get_default_ca_store_internal_raw_root_certs Return NULL/0 `Bun__Node__UseSystemCA Boolean flag `true Complete MVP requirements 7. Error Handling Lifecycle `Bun__panic( Aborts process_is_exiting lifecycle flags**Status** Functional process management Future Work Short Term (MVP Disable libdeflate Stub SIMDUTF Bun runtime hooks Test WebSocket connections Medium Term (Post-MVP Implement UTF-8 validation DNS resolution Add mutex implementations compression Long Term (Production) Evaluate Bun-specific features patches Bun Benchmark performance vs uWebSockets Maintenance Guidelines updating Bun submodule conflicts** patches apply **Review Check Bun changelog uWebSockets changes WebSocket functionality works patches** Regenerate patches if files changed stubs** Add new stubs new function calls