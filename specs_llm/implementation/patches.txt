Patches Stubs for Bun's uWebSockets Integration ZyncBase uses Bun's uWebSockets integration includes dependencies runtime hooks custom integration minimal patching approach functions Philosophy **Minimal necessary-Invasive** Comment code patch explains needed Easy reapply updating Bun submodule Patches Disable libdeflate uses libdeflate WebSocket compression need Avoids C library dependency re-enabled later Comments#define UWS_USE_LIBDEFLATE libdeflate include Disable SIMDUTF Bun uses SIMDUTF for fast UTF-8 validation requires WebKit's WTF library UTF-8 validation added later simpler library trust client input SIMDUTF replaces validation with returns `true` Implement UTF-8 validation before production Stub FunctionsBun uWebSockets fork calls runtime functions implementations linking Categories 1. DNS Resolution_get Return errors no-op Integrate system `getaddrinfo() or custom DNS resolver Event Loop Integration_onBeforeWait(_dispatch_ready_poll(_ensureDateHeaderTimerIsEnabled( No-ops JavaScriptCore timers integrate ZyncBase event loop 3. Thread Safety `Bun__lock( No-ops-threaded returns size dummy mutex structure Implement mutexes multi-threading 4. HTTP Parsing( Returns 0 (unknown method Implement HTTP method detection 5. Platform Detection Networking() Returns 1 macOS 0 elsewhere( Returns 0 fallback_epoll_pwait2( Returns -1 Complete platform checks fallbacks 6. C-Ares SSL Compatibility_inet_ntop() Wraps `inet_ntop(_get_default_ca_store_internal_raw_root_certs( Return NULL/0 `Bun__Node__UseSystemCA Boolean flag `true Complete MVP requirements 7. Error Handling Lifecycle `Bun__panic() Aborts process_is_exiting() lifecycle flags Functional process management Future Work Short Term Disable libdeflate Stub SIMDUTF Bun runtime hooks Test WebSocket connections Medium Term Implement UTF-8 validation DNS resolution mutex implementations compression Long Term Evaluate Bun-specific features patches Benchmark performance vs uWebSockets Maintenance Guidelines patches changelog uWebSockets changes WebSocket functionality works Regenerate Add new function calls