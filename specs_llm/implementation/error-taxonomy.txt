ZyncBase Error Taxonomy Handling Strategy Finalized defines formal error taxonomy ZyncBase error categories retry semantics SDK-level handling implementation follows decisions [ADR-019 Formal Error Taxonomy Error Categories grouped into 7 functional categories automatic SDK behavior (retries reverts Typical Codes Retryable SDK Behavior **Connection** `CONNECTION_FAILED` `TIMEOUT` backoff until **Authentication** `AUTH_FAILED `TOKEN_EXPIRED` Fire event wait `authRefresh **Authorization** `NAMESPACE_UNAUTHORIZED `PERMISSION_DENIED` Surface error revert update **Validation** `SCHEMA_VALIDATION_FAILED `INVALID_MESSAGE` Surface error revert update **Rate-Limit** `RATE_LIMITED` `MESSAGE_TOO_LARGE` Auto-retry backoff if `RATE_LIMITED `INTERNAL_ERROR` Retry 3 times with backoff**Hook Server**_UNAVAILABLE_DENIED` Surface error issue developer's hook logic Complete Error Catalog Code  Category Description Trigger HTTP Status Retry Strategy `AUTH_FAILED` Authentication Identity verification failed Invalid ticket expired JWT 401 new token `TOKEN_EXPIRED` Authentication Session expired Connection closed requires re-auth Refresh token `NAMESPACE_UNAUTHORIZED` Authorization No access namespace `setStoreNamespace restricted path 403 Check permissions `PERMISSION_DENIED` Authorization Rule blocked operation `authorization.json Hook Server returned `false` `SCHEMA_VALIDATION_FAILED` Data shape mismatch.set invalid fields/types 400 No - Fix data `COLLECTION_NOT_FOUND` Collection missing schema Path refers table/collection not defined 403 No - Fix schema path `FIELD_NOT_FOUND` Validation Field missing not 400 No - Fix schema data `IMMUTABLE_FIELD` Validation System field protected Attempted modify protected system field400 No - Fix data `INVALID_FIELD_NAME forbidden characters Path/value `__` sequence No - Fix path data `INVALID_ARRAY_ELEMENT Array field non-literal value array nested objects No - Fix data `INVALID_MESSAGE` Malformed frame Failed decode MessagePack missing `type No - Fix message format `RATE_LIMITED Threshold exceeded Too many messages per second IP 429 backoff `MESSAGE_TOO_LARGE-Limit Payload big Exceeding `maxMessageSize 413 No - Reduce message size `CONNECTION_FAILED Transport failure WebSocket closed DNS failure Reconnect `TIMEOUT No server response Request exceededtimeout 10s 408 Retry backoff `INTERNAL_ERROR Zig core failure Crash Disk I/O error unhandled exception 500 Retry 3 times `HOOK_SERVER_UNAVAILABLE Hook Server Logic runtime down Zig reach Hook Server 503 No - Check Hook Server `HOOK_DENIED Logic rejected Developer TS hook returned error 403 No - Check hook logic `MSGPACK_MAX_DEPTH_EXCEEDED MessagePack exceeds depth 32 levels 400 No - Reduce `MSGPACK_MAX_SIZE_EXCEEDED large exceeds 10MB limit 413 No - Reduce size_MAX_STRING_LENGTH_EXCEEDED exceeds 1MB 400 No - Reduce length_MAX_ARRAY_LENGTH_EXCEEDED Array large exceeds 100k elements 400 No - Reduce array size `MSGPACK_MAX_MAP_SIZE_EXCEEDED Map large exceeds 100k entries - Reduce `CHECKPOINT_FAILED Database checkpoint failed SQLite checkpoint failed 500 Automatic retry `DATABASE_BUSY Database lockedSQLite database busy 503  Retry backoff `DATABASE_LOCKED` Server Database locked process 503 Retry backoff `DATABASE_CORRUPT` Server corruption detected SQLite integrity check failed 500  Restore backup `CACHE_REF_COUNT_OVERFLOW concurrent readers exceeded u32 max 500 Indicates bug `CIRCUIT_BREAKER_OPEN` failures 503 Wait timeout `SUBSCRIPTION_LIMIT_EXCEEDED subscriptions Client exceeded max subscriptions 429  No Reduce subscriptions `WAL_SIZE_EXCEEDED Server WAL file large exceeded threshold 500  Automatic checkpoint `MAX_CONNECTIONS_REACHED Server capacity Pool exhausted 503  Try later `INVALID_MESSAGE_FORMAT` Missing fields Message missing `type 400  No - Fix payload `INVALID_ARRAY_ELEMENT` Non-literal array Array contains map nested array 400  No - Fix data SDK Implementation Details `ZyncBaseError` Object errors consistent typed object```typescript
interface ZyncBaseError extends Error {
  code: string;           // Machine-readable code (e.g., 'RATE_LIMITED')
  retryable: boolean;     // Whether the SDK can/will retry this
  retryAfter?: number;    // ms suggested by server to wait
  requestId?: number;     // ID of the failed request
  path?: string[];        // Affected data path (if applicable)
}
```Retry Semantics **Exponential retryable errors SDK uses `initialDelay * base attempt + jitter **Rate-Limit Enabled default server provides `retryAfter SDK respects **Optimistic Reverts** `store.set.remove error response SDK revert local state change maintain synchronization Error Propagation Flow **Wire Server sends code message Internal** Reverts optimistic state if request `id matches write `await calls throw `ZyncBaseError` Continuous streams fire global `client.on('error event **Framework/Vue/Svelte Populate `error` state hook result```typescript
// Example: React handling
const { data, error } = useStore('tasks.123');

```4.1 Asynchronous Error Reporting high-throughput "fire-and-forget operations server returns `ok send unsolicited error message if background persistence fails server sends error message `requestId matches original write request seconds later errors related to `DATABASE_BUSY_LOCKED `DISK_FULL `SCHEMA_VIOLATION background batch processing unsolicited error known `requestId SDK revert optimistic state user moved other operations 5. Detailed Retry Strategies Connection Errors `CONNECTION_FAILED `TIMEOUT` Exponential backoff jitter```typescript
const retryConfig = {
  initialDelay: 1000,      // 1 second
  maxDelay: 30000,         // 30 seconds
  multiplier: 2,           // Double each time
  jitter: 0.1,             // ±10% randomness
  maxAttempts: Infinity    // Keep trying
}

// Retry delays: 1s, 2s, 4s, 8s, 16s, 30s, 30s, ...
```

**Implementation**:
```typescript
async function connectWithRetry() {
  let attempt = 0
  
  while (true) {
    try {
      await client.connect()
      return
    } catch (error) {
      if (error.code === 'CONNECTION_FAILED' || error.code === 'TIMEOUT') {
        attempt++
        const delay = Math.min(
          retryConfig.initialDelay * Math.pow(retryConfig.multiplier, attempt),
          retryConfig.maxDelay
        )
        const jitter = delay * retryConfig.jitter * (Math.random() * 2 - 1)
        await sleep(delay + jitter)
      } else {
        throw error
      }
    }
  }
}
```Rate Limit Errors_LIMITED Respect `retryAfter header backoff```typescript
async function handleRateLimit(error: ZyncBaseError) {
  if (error.retryAfter) {
    // Server told us exactly when to retry
    await sleep(error.retryAfter)
  } else {
    // Use exponential backoff
    const delay = 1000 * Math.pow(2, attempt)
    await sleep(delay)
  }
  
  // Retry the request
  return retry()
}
```-retry** Enabled default disabled```typescript
const client = createClient({
  url: 'ws://localhost:3000',
  retryRateLimits: true  // Default: true
})
```Server Errors_ERROR_BUSY_LOCKED `CHECKPOINT_FAILED Bounded retry exponential backoff```typescript
const serverErrorRetry = {
  maxAttempts: 3,
  initialDelay: 1000,
  multiplier: 2
}

// Retry delays: 1s, 2s, 4s (then give up)
```

**Implementation**:
```typescript
async function retryServerError(operation: () => Promise<any>) {
  let attempt = 0
  
  while (attempt < serverErrorRetry.maxAttempts) {
    try {
      return await operation()
    } catch (error) {
      if (isServerError(error)) {
        attempt++
        if (attempt >= serverErrorRetry.maxAttempts) {
          throw error
        }
        const delay = serverErrorRetry.initialDelay * Math.pow(serverErrorRetry.multiplier, attempt)
        await sleep(delay)
      } else {
        throw error
      }
    }
  }
}
```Hook Server Errors_SERVER_UNAVAILABLE_BREAKER_OPEN No automatic retry fast```typescript
// Circuit breaker prevents cascading failures
if (error.code === 'CIRCUIT_BREAKER_OPEN') {
  // Don't retry - circuit breaker will auto-recover after timeout
  throw error
}

if (error.code === 'HOOK_SERVER_UNAVAILABLE') {
  // Hook Server is down - don't retry
  // Operations are denied by default for security
  throw error
}
```Authentication Errors_EXPIRED Fire event token refresh```typescript
client.on('tokenExpired', async () => {
  // Get new token from your auth service
  const newToken = await refreshAuthToken()
  
  // Reconnect with new token
  await client.reconnect({ token: newToken })
})
```retry `AUTH_FAILED_UNAUTHORIZED_DENIED configuration permission issues Retrying help Surface error user Validation Errors `SCHEMA_VALIDATION_FAILED `INVALID_MESSAGE `MSGPACK_EXCEEDED No retry-side bug```typescript
// These errors indicate bugs in client code
// Retrying won't help - fix the code
if (isValidationError(error)) {
  console.error('Client validation error:', error)
  // Revert optimistic update
  revertOptimisticUpdate(error.requestId)
  // Surface error to developer
  throw error
}
```Retry Decision Matrix Error Category Auto-Retry Max Attempts Backoff User Action Required Connection Yes Infinite Exponential None - automatic Rate-Limit Yes (opt-out Infinite Respect `retryAfter` None - automatic Server Yes 3 Exponential Alert persistent Hook Server No 0 N/A Check Hook Server Authentication Partial 1 None Refresh token Authorization No 0 N/A Fix permissions Validation No N/A Fix client code SDK Error Object errors SDK use consistent typed object `ZyncBaseError` interface SDK package fields server-side contract```typescript
interface ZyncBaseError extends Error {
  code: string;           // Machine-readable code from the catalog above
  retryable: boolean;     // Whether the SDK will automatically retry
  retryAfter?: number;    // ms to wait before retry (server-provided)
  requestId?: number;     // Echoed request id from the failed message
  path?: string[];        // Affected data path, if applicable
}
```See [Security Model Rate limiter circuit breaker [Wire Protocol Error envelope wire format [ADR-019.md-error-taxonomy-handling-strategy Decision record taxonomy