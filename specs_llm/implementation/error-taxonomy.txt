ZyncBase Error Taxonomy Handling Strategy Finalized defines error taxonomy ZyncBase error categories retry semantics SDK-level handling follows decisions [ADR-019 Formal Error Taxonomy Error Categories grouped 7 functional categories automatic SDK behavior reverts Typical Codes Retryable SDK Behavior `CONNECTION_FAILED `TIMEOUT backoff **Authentication** `AUTH_FAILED `TOKEN_EXPIRED `authRefresh **Authorization** `NAMESPACE_UNAUTHORIZED `PERMISSION_DENIED error update **Validation**_VALIDATION_FAILED `INVALID_MESSAGE error **Rate-Limit** `RATE_LIMITED `MESSAGE_TOO_LARGE Auto-retry backoff `RATE_LIMITED `INTERNAL_ERROR` Retry 3 times backoff**Hook_UNAVAILABLE_DENIED Surface error developer hook logic Error Catalog Code Category Description Trigger HTTP Status Retry Strategy `AUTH_FAILED Authentication Identity verification failed Invalid ticket expired JWT new token `TOKEN_EXPIRED Session expired Connection closed re-auth Refresh token `NAMESPACE_UNAUTHORIZED Authorization No access namespace `setStoreNamespace restricted path 403 `PERMISSION_DENIED Authorization Rule blocked `authorization.json Server `false `SCHEMA_VALIDATION_FAILED Data shape mismatchinvalid fields 400 Fix data `INVALID_MESSAGE Malformed frame Failed decode missing Fix message format `RATE_LIMITED Threshold exceeded many messages second 429 backoff `MESSAGE_TOO_LARGE Payload `maxMessageSize 413 Reduce message size `CONNECTION_FAILED Transport failure WebSocket DNS failure Reconnect `TIMEOUT No server response Request exceededtimeout 10s 408 Retry backoff_ERROR Zig core failure Crash Disk I/O error unhandled exception 500 Retry 3 times_SERVER_UNAVAILABLE Logic runtime Zig reach Hook Server 503 No - Check Hook Server `HOOK_DENIED Logic rejected Developer TS hook returned error 403 No - Check `MSGPACK_MAX_DEPTH_EXCEEDED exceeds depth 32 levels 400 - Reduce `MSGPACK_MAX_SIZE_EXCEEDED large exceeds 10MB limit 413 - Reduce_MAX_STRING_LENGTH_EXCEEDED exceeds 1MB 400 Reduce_ARRAY_LENGTH_EXCEEDED large exceeds 100k elements No - Reduce_MAX_MAP_SIZE_EXCEEDED Map large exceeds 100k entries `CHECKPOINT_FAILED Database checkpoint SQLite checkpoint 500 Automatic retry `DATABASE_BUSY lockedSQLite database busy 503 Retry backoff `DATABASE_LOCKED locked process Retry backoff `DATABASE_CORRUPT` corruption integrity check failed 500 Restore backup `CACHE_REF_COUNT_OVERFLOW concurrent readers exceeded u32 max 500 bug `CIRCUIT_BREAKER_OPEN failures 503 Wait timeout `SUBSCRIPTION_LIMIT_EXCEEDED subscriptions exceeded max subscriptions 429 Reduce subscriptions `WAL_SIZE_EXCEEDED WAL file large exceeded threshold 500 Automatic checkpoint `PATH_NOT_FOUND` removed Reads empty paths return `null new paths SDK Implementation Details `ZyncBaseError` Object errors consistent typed object```typescript
interface ZyncBaseError extends Error {
  code: string;           // Machine-readable code (e.g., 'RATE_LIMITED')
  category: string;       // Category for grouping logic
  retryable: boolean;     // Whether the SDK can/will retry this
  retryAfter?: number;    // ms suggested by server to wait
  requestId?: number;     // ID of the failed request
  path?: string[];        // Affected data path (if applicable)
  details?: Record<string, string[]>; // Field-level validation errors
}
```Retry Semantics **Exponential retryable errors SDK uses `initialDelay base attempt + jitter-Limit Enabled server provides `retryAfter SDK respects.set.remove error response SDK revert local state change synchronization Error Propagation Flow Server sends Reverts optimistic state request `id matches write `await calls throw `ZyncBaseError Continuous streams fire global `client.on('error event/Vue/Svelte Populate `error state hook result```typescript
// Example: React handling
const { data, error } = useStore('tasks.123');

if (error) {
  if (error.code === 'PERMISSION_DENIED') return <NoAccess />;
  return <ErrorMessage message={error.message} />;
}
```Retry Strategies Connection Errors_FAILED `TIMEOUT backoff jitter```typescript
const retryConfig = {
  initialDelay: 1000,      // 1 second
  maxDelay: 30000,         // 30 seconds
  multiplier: 2,           // Double each time
  jitter: 0.1,             // ±10% randomness
  maxAttempts: Infinity    // Keep trying
}

// Retry delays: 1s, 2s, 4s, 8s, 16s, 30s, 30s, ...
```

**Implementation**:
```typescript
async function connectWithRetry() {
  let attempt = 0
  
  while (true) {
    try {
      await client.connect()
      return
    } catch (error) {
      if (error.code === 'CONNECTION_FAILED' || error.code === 'TIMEOUT') {
        attempt++
        const delay = Math.min(
          retryConfig.initialDelay * Math.pow(retryConfig.multiplier, attempt),
          retryConfig.maxDelay
        )
        const jitter = delay * retryConfig.jitter * (Math.random() * 2 - 1)
        await sleep(delay + jitter)
      } else {
        throw error
      }
    }
  }
}
```Rate Limit Errors_LIMITED Respect header backoff```typescript
async function handleRateLimit(error: ZyncBaseError) {
  if (error.retryAfter) {
    // Server told us exactly when to retry
    await sleep(error.retryAfter)
  } else {
    // Use exponential backoff
    const delay = 1000 * Math.pow(2, attempt)
    await sleep(delay)
  }
  
  // Retry the request
  return retry()
}
```-retry** Enabled disabled```typescript
const client = createClient({
  url: 'ws://localhost:3000',
  retryRateLimits: true  // Default: true
})
```Server Errors_ERROR_BUSY_LOCKED_FAILED Bounded retry exponential backoff```typescript
const serverErrorRetry = {
  maxAttempts: 3,
  initialDelay: 1000,
  multiplier: 2
}

// Retry delays: 1s, 2s, 4s (then give up)
```

**Implementation**:
```typescript
async function retryServerError(operation: () => Promise<any>) {
  let attempt = 0
  
  while (attempt < serverErrorRetry.maxAttempts) {
    try {
      return await operation()
    } catch (error) {
      if (isServerError(error)) {
        attempt++
        if (attempt >= serverErrorRetry.maxAttempts) {
          throw error
        }
        const delay = serverErrorRetry.initialDelay * Math.pow(serverErrorRetry.multiplier, attempt)
        await sleep(delay)
      } else {
        throw error
      }
    }
  }
}
```Hook Server Errors_UNAVAILABLE_BREAKER_OPEN No automatic retry```typescript
// Circuit breaker prevents cascading failures
if (error.code === 'CIRCUIT_BREAKER_OPEN') {
  // Don't retry - circuit breaker will auto-recover after timeout
  throw error
}

if (error.code === 'HOOK_SERVER_UNAVAILABLE') {
  // Hook Server is down - don't retry
  // Operations are denied by default for security
  throw error
}
```Authentication Errors_EXPIRED Fire event refresh```typescript
client.on('tokenExpired', async () => {
  // Get new token from your auth service
  const newToken = await refreshAuthToken()
  
  // Reconnect with new token
  await client.reconnect({ token: newToken })
})
```retry `AUTH_FAILED_UNAUTHORIZED_DENIED configuration permission issues Retrying help error Validation Errors_VALIDATION_FAILED_MESSAGE `MSGPACK_EXCEEDED No retry-side bug```typescript
// These errors indicate bugs in client code
// Retrying won't help - fix the code
if (isValidationError(error)) {
  console.error('Client validation error:', error)
  // Revert optimistic update
  revertOptimisticUpdate(error.requestId)
  // Surface error to developer
  throw error
}
```Retry Decision Matrix Error Category Auto-Retry Max Attempts Backoff User Action Required Connection Yes Infinite Exponential None automatic Rate-Limit Yes (opt-out Respect `retryAfter None automatic Server Yes Exponential Alert persistent Hook Server Check Hook Server Authentication Partial Refresh token Authorization No Fix permissions Validation Fix client code SDK Error Object errors consistent typed object `ZyncBaseError interface SDK package fields server-side contract```typescript
interface ZyncBaseError extends Error {
  code: string;           // Machine-readable code from the catalog above
  category: string;       // Functional category (connection | auth | authorization | validation | rate-limit | server | hook-server)
  retryable: boolean;     // Whether the SDK will automatically retry
  retryAfter?: number;    // ms to wait before retry (server-provided)
  requestId?: number;     // Echoed request id from the failed message
  path?: string[];        // Affected data path, if applicable
  details?: Record<string, string[]>; // Field-level validation errors
}
```Model Rate limiter circuit breaker Protocol Error envelope wire format [ADR-019-error-taxonomy-strategy Decision record taxonomy