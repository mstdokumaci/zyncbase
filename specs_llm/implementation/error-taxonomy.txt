ZyncBase Error Taxonomy Handling Strategy implementation follows decisions [ADR-022 Formal Error Taxonomy-error-taxonomy-handling-strategy Error Categories Errors grouped 6 functional categories automatic SDK behavior retries user-facing surfacing Category Typical Codes Retryable SDK Behavior **Connection** `CONNECTION_FAILED` `TIMEOUT` backoff jitter until reconnected **Authentication** `AUTH_FAILED `TOKEN_EXPIRED `SESSION_NOT_READY Fire `tokenExpired event wait `authRefresh readiness **Authorization** `NAMESPACE_UNAUTHORIZED `PERMISSION_DENIED` **Validation** `SCHEMA_VALIDATION_FAILED `INVALID_MESSAGE` **Rate-Limit** `RATE_LIMITED` `MESSAGE_TOO_LARGE` Auto-retry backoff if `RATE_LIMITED` **Server** `INTERNAL_ERROR` Retry 3 times backoffError Catalog Code Category Description Trigger HTTP Status Retry Strategy `AUTH_FAILED` Authentication Identity verification failed Invalid ticket expired JWT 401 new token `TOKEN_EXPIRED` Authentication Session expired Connection closed requires re-auth Refresh token `SESSION_NOT_READY` session ready Store/presence operation before namespace user resolution 409 Wait namespace acknowledgement `NAMESPACE_UNAUTHORIZED` Authorization No access namespace `setStoreNamespace restricted path 403 Check permissions `PERMISSION_DENIED` Authorization Rule blocked operation `authorization.json returned `false same-row guard 403 Check permissions `SCHEMA_VALIDATION_FAILED Validation Data shape mismatch `storeinvalid fields 400 No - Fix data `COLLECTION_NOT_FOUND` Collection missing Path table defined 403 No - Fix schema path `FIELD_NOT_FOUND` Validation Field missing Path not 400 No - Fix schema data `IMMUTABLE_FIELD` Validation field protected modify protected field 400 No - Fix data `INVALID_FIELD_NAME` Validation Field name forbidden characters Path/value `__` sequence 400 No - Fix path data `INVALID_ARRAY_ELEMENT` Validation Array field non-literal valuearray nested 400 No - Fix data `INVALID_MESSAGE Validation Malformed frame Failed decode MessagePack missing 400 - Fix message format `RATE_LIMITED Threshold exceeded many messages second IP 429 backoff `MESSAGE_TOO_LARGE Rate-Limit Payload big Exceeding `maxMessageSize 413 No - Reduce message size `CONNECTION_FAILED Transport failure WebSocket closed DNS failure Reconnect `TIMEOUT Connection No server response Request exceededtimeout 10s 408 Retry backoff `INTERNAL_ERROR Server Zig core failure Crash Disk I/O error unhandled exception 500 Retry 3 times `MSGPACK_MAX_DEPTH_EXCEEDED exceeds depth 32 levels 400 No - Reduce nesting `MSGPACK_MAX_SIZE_EXCEEDED large exceeds 10MB limit 413 No - Reduce size_MAX_STRING_LENGTH_EXCEEDED String long exceeds 1MB limit 400 No - Reduce string length_MAX_ARRAY_LENGTH_EXCEEDED Array large exceeds 100k elements 400 No - Reduce array size `MSGPACK_MAX_MAP_SIZE_EXCEEDED Map large exceeds 100k entries No - Reduce map size `CHECKPOINT_FAILED Database checkpoint failed SQLite checkpoint failed 500 Automatic retry `DATABASE_BUSY Server Database locked SQLite busy 503 Retry backoff `DATABASE_LOCKED locked process 503 Retry backoff `DATABASE_CORRUPT Server corruption detected SQLite integrity check failed 500Restore backup `CACHE_REF_COUNT_OVERFLOW Server many readers exceeded u32 max 500 bug `SUBSCRIPTION_LIMIT_EXCEEDED subscriptions Client exceeded max subscriptions 429 No - Reduce subscriptions `RESOURCE_EXHAUSTED Subscription engine memory budget reached New subscription rejected.maxMemoryMB 429 No - Reduce subscriptions `WAL_SIZE_EXCEEDED Server WAL file large exceeded threshold 500 Automatic checkpoint `MAX_CONNECTIONS_REACHED Server capacity Pool exhausted 503 No Try later `INVALID_MESSAGE_FORMAT Missing fields Message missing 400 No - Fix payload `INVALID_ARRAY_ELEMENT Non-literal array map nested array 400 No - Fix data SDK Implementation Details `ZyncBaseError` errors consistent typed object```typescript
interface ZyncBaseError extends Error {
  code: string;           // Machine-readable code (e.g., 'RATE_LIMITED')
  retryable: boolean;     // Whether the SDK can/will retry this
  retryAfter?: number;    // ms suggested by server to wait
  requestId?: number;     // ID of the failed immediate request
  writeId?: string;       // ID of a tracked or confirmed write, if applicable
  path?: string[];        // Affected data path (if applicable)
  details?: {
    phase?: 'accept' | 'write';
    batchIndex?: number;
    [key: string]: unknown;
  };
}
```Retry Semantics **Exponential retryable errors SDK uses `initialDelay * base attempt + jitter-Limit Enabled server provides `retryAfter SDK respects Confirmation** Default mutations resolve after request acceptance Confirmed mutations surface writer-thread failures `ZyncBaseError `details.phase "write Error Propagation Flow Server sends code message request failures `WriteError tracked failures Rejects promise immediate request errors confirmed write failures No optimistic subscription state reverted `await calls throw `ZyncBaseError Continuous streams fire `client.on('error event **Framework Populate `error state hook result```typescript
// Example: React handling
const { data, error } = useStore('tasks.123');

```4.1 Write Error Reporting server `WriteError processing fails after request acceptance server sends `WriteError `writeId matching write errors related to `DATABASE_BUSY_LOCKED `DISK_FULL `SCHEMA_VIOLATION background batch processing confirmed writes reject pending promise tracked errors surface error through SDK error channel revert subscription state remain authoritative channel 5. Retry Strategies Connection Errors `CONNECTION_FAILED `TIMEOUT` backoff jitter```typescript
const retryConfig = {
  initialDelay: 1000,      // 1 second
  maxDelay: 30000,         // 30 seconds
  multiplier: 2,           // Double each time
  jitter: 0.1,             // ±10% randomness
  maxAttempts: Infinity    // Keep trying
}

// Retry delays: 1s, 2s, 4s, 8s, 16s, 30s, 30s, ...
```

**Implementation**:
```typescript
async function connectWithRetry() {
  let attempt = 0
  
  while (true) {
    try {
      await client.connect()
      return
    } catch (error) {
      if (error.code === 'CONNECTION_FAILED' || error.code === 'TIMEOUT') {
        attempt++
        const delay = Math.min(
          retryConfig.initialDelay * Math.pow(retryConfig.multiplier, attempt),
          retryConfig.maxDelay
        )
        const jitter = delay * retryConfig.jitter * (Math.random() * 2 - 1)
        await sleep(delay + jitter)
      } else {
        throw error
      }
    }
  }
}
```Rate Limit Errors_LIMITED Respect `retryAfter header backoff```typescript
async function handleRateLimit(error: ZyncBaseError) {
  if (error.retryAfter) {
    // Server told us exactly when to retry
    await sleep(error.retryAfter)
  } else {
    // Use exponential backoff
    const delay = 1000 * Math.pow(2, attempt)
    await sleep(delay)
  }
  
  // Retry the request
  return retry()
}
```-retry** Enabled default disabled```typescript
const client = createClient({
  url: 'ws://localhost:3000',
  retryRateLimits: true  // Default: true
})
```Server Errors_ERROR_BUSY_LOCKED `CHECKPOINT_FAILED Bounded retry exponential backoff```typescript
const serverErrorRetry = {
  maxAttempts: 3,
  initialDelay: 1000,
  multiplier: 2
}

// Retry delays: 1s, 2s, 4s (then give up)
```

**Implementation**:
```typescript
async function retryServerError(operation: () => Promise<any>) {
  let attempt = 0
  
  while (attempt < serverErrorRetry.maxAttempts) {
    try {
      return await operation()
    } catch (error) {
      if (isServerError(error)) {
        attempt++
        if (attempt >= serverErrorRetry.maxAttempts) {
          throw error
        }
        const delay = serverErrorRetry.initialDelay * Math.pow(serverErrorRetry.multiplier, attempt)
        await sleep(delay)
      } else {
        throw error
      }
    }
  }
}
```Authentication Errors_EXPIRED Fire event token refresh```typescript
client.on('tokenExpired', async () => {
  // Get new token from your auth service
  const newToken = await refreshAuthToken()
  
  // Reconnect with new token
  await client.reconnect({ token: newToken })
})
```retry `AUTH_FAILED_UNAUTHORIZED_DENIED configuration permission issues Retrying help Surface error user Validation Errors `SCHEMA_VALIDATION_FAILED `INVALID_MESSAGE `MSGPACK_EXCEEDED No retry-side bug```typescript
// These errors indicate bugs in client code
// Retrying won't help - fix the code
if (isValidationError(error)) {
  console.error('Client validation error:', error)
  // Surface error to developer or user
  throw error
}
```Retry Decision Matrix Error Category Auto-Retry Max Attempts Backoff User Action Required Connection Yes Infinite Exponential None - automatic Rate-Limit Yes (opt-out) Infinite Respect `retryAfter` None - automatic Server Yes 3 Exponential Alert persistent Authentication Partial 1 None Refresh token Authorization No 0 N/A Fix permissions Validation No 0 N/A Fix client code SDK Error Object errors use consistent typed object `ZyncBaseError` interface defined SDK package fields server-side contract```typescript
interface ZyncBaseError extends Error {
  code: string;           // Machine-readable code from the catalog above
  retryable: boolean;     // Whether the SDK will automatically retry
  retryAfter?: number;    // ms to wait before retry (server-provided)
  requestId?: number;     // Echoed request id for immediate request failures
  writeId?: string;       // Correlation id for tracked/confirmed writer outcomes
  path?: string[];        // Affected data path, if applicable
  details?: {
    phase?: 'accept' | 'write';
    batchIndex?: number;
    [key: string]: unknown;
  };
}
```See [Security Model Rate limiter circuit breaker [Wire Protocol Error envelope wire format [ADR-019.md-error-taxonomy-handling-strategy Decision record taxonomy