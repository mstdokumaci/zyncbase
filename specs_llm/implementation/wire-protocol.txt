ZyncBase Wire Protocol Specification Table of Contents [Overview [Transport & Serialization [Message Envelope [Client→Server Messages [Server→Client Messages [Ticket Endpoints (HTTP) [Connection Lifecycle [Error Format [Extensibility [Message Type Summary specifies client-server contract ZyncBase WebSocket message **MessagePack-encoded JSON message conforms to pseudo-literals MessagePack(16) values public SDK surface document IDs reference fields remain strings wire packed into 16-byte binary IDs principles **1:1 SDK mapping** client SDK method maps to one message type No overloaded messages **Correlate by ID** client request unique `id` server response echoes unsolicited** Server-initiated messages own types carry request `id` **Extend, never break** New fields added any message type Unknown fields ignored [Extensibility Transport & Serialization| Property | Value |
|----------|-------|
| Transport | WebSocket (RFC 6455) |
| Frame type | Binary (`opcode 0x02`) |
| Serialization | MessagePack (production), JSON (debug mode via config flag) |
| Max message size | 10 MB (configurable, see `security.rateLimit.maxMessageSize`) |
| Max nesting depth | 32 levels (per ADR-008) |
| Compression | None (per ADR-008) |
Message Envelope MessagePack `type field Client-originated messages carry `id request-response correlation Client→Server envelope```
{
  "type": <string>,     // Message type (see catalog below)
  "id":   <uint64>,     // Unique request ID, generated by client
  ...                   // Type-specific fields (inline, not nested in a "payload" key)
}
```response envelope```
{
  "type": "ok" | "error",  // Result type
  "id":   <uint64>,        // Echoed request ID
  ...                      // Type-specific fields
}
```envelope```
{
  "type": <string>,     // Push type (e.g., "StoreDelta", "PresenceBroadcast")
  ...                   // Type-specific fields (no "id" — pushes are unsolicited)
}
```flat payload under key adds indirection no benefit Keeping fields inline simpler compact over wire Client→Server Messages Store operations read operations through `StoreQuery `StoreSubscribe SDK translates path reads.get into collection-level queries id filters before transmission no `StoreGet message types on wire `StoreSet` Write value at path Default confirmation accepted response confirms server accepted mutation write pipeline not committed SDK update local subscription state```
{
  "type":    "StoreSet",
  "id":      2,
  "path":    [0, <bin16("rect-1")>],
  "value":   { 1: 100, 2: 200, 3: 50, 4: 50 }, // Integer-keyed map for field indices
  "confirm": "accepted"                         // Optional: "accepted" (default) | "committed"
}
```"ok"```
{
  "type":    "ok",
  "id":      2,
  "writeId": "wr_..." // Present for tracked/committed writes
}
````confirm "accepted mutation accepted write pipeline `confirm "committed SDK resolves mutation after commits accepted no-op Writer failures `WriteError response `details.phase "write `StoreRemove` Remove document path Deleting missing document success/no-op```
{
  "type":    "StoreRemove",
  "id":      3,
  "path":    [0, <bin16("rect-1")>],
  "confirm": "accepted" // Optional: "accepted" (default) | "committed"
}
```"ok"```
{
  "type":    "ok",
  "id":      3,
  "writeId": "wr_..." // Present for tracked/committed writes
}
````StoreBatch write operations See Operations Specification wire format details execution logic```
{
  "type":    "StoreBatch",
  "id":      4,
  "ops":     [
    ["s", [1, <bin16("123")>], { 3: "assigned" }],
    ["s", [0, <bin16("bob")>, 8], 5],
    ["r", [2, <bin16("123")>]]
  ],
  "confirm": "accepted" // Optional: "accepted" (default) | "committed"
}
```codes `"s"` Set `path `value `"r"` Remove `path "ok"```
{
  "type":    "ok",
  "id":      4,
  "writeId": "wr_..." // Present for tracked/committed writes
}
```"committed batch resolves full batch commits produces no-op outcomes failure `details error `batchIndex failing operation identified `StoreQuery Execute one-off filtered query-real-time collection Query conditions sort descriptors sent compact positional tuples Grammar encoding```
{
  "type":  "StoreQuery",
  "id":    6,
  "table_index": 0,
  "conditions": [
    [5, 4, 18],              // [field_index, op_code, value] — op 4 = gte
    [2, 0, "active"]         // op 0 = eq
  ],
  "orderBy": [3, 1],  // [field_index, desc_flag] — 1 = desc
  "limit":   50,
  "after":   "..." // Opaque Base64(MessagePack([sort_value, doc_id_bin]))
}
```"ok"```
{
  "type":       "ok",
  "id":         6,
  "value":      [ ... ],    // Array of matching items
  "nextCursor": "..." // Opaque Base64(MessagePack([sort_value, doc_id_bin])) or null
}
````StoreSubscribe filtered query results real `store.subscribe(collection opts cb```
{
  "type":    "StoreSubscribe",
  "id":      7,
  "table_index": 1,
  "conditions": [
    [2, 0, "active"]      // [field_index, op_code, value] — op 0 = eq
  ],
  "orderBy": [8, 1],  // [field_index, desc_flag] — 1 = desc
  "limit":   50
}
```"ok" includes snapshot```
{
  "type":       "ok",
  "id":         7,
  "subId":      456,
  "value":      [ ... ],
  "hasMore":    true,
  "nextCursor": "..."
}
````StoreLoadMore historical data subscription```
{
  "type":       "StoreLoadMore",
  "id":         8,
  "subId":      456,
  "nextCursor": "..."
}
```"ok"```
{
  "type":       "ok",
  "id":         8,
  "subId":      456,
  "value":      [ ... ],
  "hasMore":    true,
  "nextCursor": "..."
}
```subscription server pushes `StoreDelta query result changes `StoreUnsubscribe Stop updates subscription```
{
  "type":  "StoreUnsubscribe",
  "id":    9,
  "subId": 456
}
```"ok"```
{
  "type": "ok",
  "id":   9
}
```Presence operations `PresenceSet local user presence data Integer-keyed field-level merge fields preserved Sends `PresenceBroadcast user-presence subscribers Field indices `presenceUserFields message```
{
  "type": "PresenceSet",
  "id":   8,
  "data": { 0: 100.0, 1: 200.0 }   // cursor__x=0, cursor__y=1 — only changed fields
}
```"ok"```
{
  "type": "ok",
  "id":   8
}
```Fire-forget server validates field indices value types Unknown index wrong type_VALIDATION_FAILED `PresenceSetShared fields namespace shared state Integer-keyed field-level merge fields preserved Sends `SharedStateBroadcast subscribers Field indices `presenceSharedFields message```
{
  "type": "PresenceSetShared",
  "id":   9,
  "data": { 0: 5 }    // slide=0
}
```"ok"```
{
  "type": "ok",
  "id":   9
}
```Fire-forget `presenceSharedWrite authorization Rejected_DENIED rule satisfied Unknown field index wrong type `SCHEMA_VALIDATION_FAILED `PresenceSubscribe presence changes Returns user snapshot Shared state obtained `PresenceSubscribeShared```
{
  "type": "PresenceSubscribe",
  "id":   10
}
```"ok" includes user snapshot```
{
  "type":  "ok",
  "id":    10,
  "subId": 789,              // Integer subscription ID
  "users": [
    {
      "userId":   <bin16>,   // Internal users.id as 16-byte binary
      "data":     { 0: 100.0, 1: 200.0, 4: "Alice" },  // Integer-keyed user field map
      "joinedAt": 1234567890
    }
  ]
}
```snapshot reflects state subscription time Live updates batch interval SDK decodes `presenceUserFields indices unflattens nested objects before delivering application `PresenceUnsubscribe Stop receiving updates subscription```
{
  "type":  "PresenceUnsubscribe",
  "id":    11,
  "subId": 789
}
```"ok"```
{
  "type": "ok",
  "id":   11
}
````PresenceSubscribeShared Subscribe state changes presence namespace Returns shared state```
{
  "type": "PresenceSubscribeShared",
  "id":   12
}
```"ok" includes current shared state```
{
  "type":   "ok",
  "id":     12,
  "subId":  790,
  "shared": { 0: 5, 1: true }    // Current shared state (integer-keyed); null if empty
}
```Stop state updates```
{
  "type":  "PresenceUnsubscribeShared",
  "id":    13,
  "subId": 790
}
```"ok"```
{
  "type": "ok",
  "id":   13
}
````PresenceRemove local user presence data done disconnect```
{
  "type": "PresenceRemove",
  "id":   14
}
```"ok"```
{
  "type": "ok",
  "id":   14
}
````AuthRefresh Update connection session context new JWT```
{
  "type":  "AuthRefresh",
  "id":    11,
  "token": "<new_external_jwt>"
}
```"ok"```
{
  "type":    "ok",
  "id":      11,
  "session": { ... } // The newly resolved base $session context
}
```session claims token expiry updated Active store presence scopes continue JWT invalid server sends `ServerDisconnect_FAILED closes WebSocket connection Namespace operations `StoreSetNamespace Switch active store namespace resolve store scope```
{
  "type":      "StoreSetNamespace",
  "id":        12,
  "namespace": "tenant:acme:workspace:ws-2"
}
```"ok"```
{
  "type":    "ok",
  "id":      12,
  "session": { "userId": "018f..." } // Store-scoped session fragment
}
```store subscriptions invalidated namespace changes client re-subscribe Store operations rejected_NOT_READY until response sent Switch presence namespace resolve scope Clears joins new namespace```
{
  "type":      "PresenceSetNamespace",
  "id":        13,
  "namespace": "tenant:acme:document:doc-456"
}
```"ok"```
{
  "type":    "ok",
  "id":      13,
  "session": { "userId": "018f..." } // Presence-scoped session fragment
}
```Presence rejected `SESSION_NOT_READY` until response sent Server→Client Messages responses client requests use "ok "error| `type` | Meaning |
|--------|---------|
| `"ok"` | Request succeeded. Fields depend on the original request type (see above). |
| `"error"` | Request failed. See [Error Format](#error-format). |
include `id` original request Server push messages unsolicited messages request `id` `StoreDelta` Notifies subscribed client subscription's result set changed```
{
  "type":  "StoreDelta",
  "subId": 456,                  // Subscription ID (u64)
  "ops":   [                           // Array of atomic operations
    {
      "op":    "set",                  // "set" | "remove"
      "path":  [0, "rect-2"],
      "value": { 1: 50, 2: 75 }       // Present for "set", absent for "remove"
    },
    {
      "op":   "remove",
      "path": [0, "rect-3"]
    }
  ]
}
```

**Operation types:**

| `op` | Description |
|------|-------------|
| `"set"` | A record was created or updated. `value` contains the full record. |
| `"remove"` | A record was deleted. No `value` field. |
note (ADR-014) Deltas record-level records `remove SDK handles field-level projection change detection client maintains state applies operations note (ADR `StoreDelta committed-state channel subscriptions Mutation responses write-result messages report outcome deliver subscription state `WriteCommitted SDK-result push tracked writes resolve mutation promises not public application event```
{
  "type":    "WriteCommitted",
  "writeId": "wr_..."
}
````WriteError` Notifies SDK write failed after not rollback command```
{
  "type":    "WriteError",
  "writeId": "wr_...",
  "code":    "PERMISSION_DENIED",
  "message": "Rule blocked operation",
  "path":    [0, <bin16("rect-1")>], // Optional, best-effort
  "details": {
    "phase":      "write",
    "batchIndex": 2                  // Optional, batch failures only
  }
}
````PresenceBroadcast Notifies subscribers changes namespace Delivered 50ms batches user events grouped single message `users` array```
{
  "type":   "PresenceBroadcast",
  "subId":  789,           // Integer subscription ID
  "users":  [
    {
      "userId":   <bin16>,   // Internal users.id as 16-byte binary
      "event":    "join",    // "join" | "update" | "leave"
      "data":     { 0: 101.0, 1: 201.0 },  // Integer-keyed field map. Present for "join" and "update", absent for "leave"
      "joinedAt": 1234567890               // Unix timestamp ms. Present only for "join" events
    }
  ]
}
```

**Event types:**

| `event` | `data` | `joinedAt` | Description |
|---------|--------|------------|-------------|
| `"join"` | Full user field map (all set fields) | Present | A user entered the namespace. |
| `"update"` | Merge delta (only changed fields) | Absent | A user called `presence.set()`. SDK merges into local cache. |
| `"leave"` | Absent | Absent | A user disconnected or called `presence.remove()`. |
SDK decodes `presenceUserFields unflattens nested objects() callbacks `SharedStateBroadcast Notifies subscribers namespace-level state changed Delivered 50ms batches```
{
  "type":  "SharedStateBroadcast",
  "subId": 790,                  // Integer subscription ID (from PresenceSubscribeShared)
  "data":  { 0: 6 }             // Integer-keyed merge delta — only changed fields
}
```SDK decodes indices merges patch local shared state cache unflattens nested objects fires `subscribeShared() callbacks updated state `ServerDisconnect-initiated disconnection shutdown token```
{
  "type":    "ServerDisconnect",
  "code":    "TOKEN_EXPIRED",
  "message": "Your authentication token has expired."
}
```Ticket Endpoints (HTTP Ticket Exchange WebSocket client obtains single-use ticket```
POST /auth/ticket
Authorization: Bearer <external_jwt>
````HTTP 200 OK```json
{
  "ticket":    "zyc_tk_abc123...",
  "expiresAt": 1741551234
}
```Connection Lifecycle WebSocket Upgrade opens WebSocket connection ticket```
GET /ws?ticket=zyc_tk_abc123... HTTP/1.1
Upgrade: websocket
Connection: Upgrade
Origin: https://app.example.com
```server validates Origin header prevention Ticket signature expiry `$session` success HTTP 101 Switching Protocols failure HTTP 401 403. SchemaSync Connected WebSocket established server pushes two messages `SchemaSync dictionary integer routing store presence operations SDK hash payload track queue compatibility `fields array matches Zig server memory layout system columns`id_id_id `updated_at included SDK indices align```
{
  "type":   "SchemaSync",
  "tables": ["users", "tasks"],
  "fields": [
    ["id", "namespace_id", "owner_id", "email", "created_at", "updated_at"],
    ["id", "namespace_id", "owner_id", "title", "status", "created_at", "updated_at"]
  ],
  "fieldFlags": [
    [7, 5, 7, 0, 5, 5],  // users:    id=7(0b111), namespace_id=5(0b101), owner_id=7(0b111), email=0, created_at=5(0b101), updated_at=5(0b101)
    [7, 5, 7, 0, 0, 5, 5] // tasks:    id=7(0b111), namespace_id=5(0b101), owner_id=7(0b111), title=0, status=0, created_at=5(0b101), updated_at=5(0b101)
  ],
  "presenceUserFields":   ["cursor__x", "cursor__y", "status", "typing", "name"],
  "presenceSharedFields": ["slide", "playing"]
}
```Presence field arrays flat lists field names expanded `__ array index integer wire No companion flags system-column doc_id semantics arrays omitted no `presence section `Connected transport-level session context authorize scoped store presence operations```
{
  "type":               "Connected",
  "externalUserId":     "user-123",       // External identity string (JWT sub or SDK client ID)
  "session":            { ... },          // Base $session context, before per-scope userId injection
  "storeReady":         false,
  "presenceReady":      false
}
```client wait message before namespace selection messages Store presence data operations require acknowledgement 4. Heartbeat server sends WebSocket frames 30 client responds **pong** frames no pong received timeout 10 server closes connection No application-level heartbeat messages needed WebSocket ping/pong Graceful close Client sends WebSocket close frame Server cleans subscriptions presence sends `ServerDisconnect message closes WebSocket 6. Reconnection Strategy connection lost exponential backoff strategy optional jitter prevent restart formula delay next attempt `min(reconnectDelay * 2^attempt + jitter maxReconnectDelay Error Format errors consistent envelope```
{
  "type":       "error",
  "id":         2,                          // Echoed request ID (if responding to a request)
  "writeId":    "wr_...",                   // Optional, tracked/committed writes
  "code":       "SCHEMA_VALIDATION_FAILED", // Machine-readable error code
  "message":    "Field 'priority' must be an integer, got string.",
  "details":    { "phase": "accept" },      // Optional structured context
  "retryAfter": 5000                        // (Optional) ms to wait before retry
}
```

### Error codes

| Code | Category | Description |
|------|----------|-------------|
| `AUTH_FAILED` | auth | Invalid ticket or expired initial JWT |
| `TOKEN_EXPIRED` | auth | Session expired; client should re-authenticate |
| `SESSION_NOT_READY` | state | Scoped operation sent before namespace and user resolution completed |
| `NAMESPACE_UNAUTHORIZED` | authorization | Not authorized to access this namespace |
| `NAMESPACE_SWITCH_REJECTED` | state | Namespace switching is not allowed when `users.namespaced` is enabled |
| `COLLECTION_NOT_FOUND` | validation | Table/Collection name in path is not in the schema |
| `FIELD_NOT_FOUND` | validation | Field name in path or value is not in the schema |
| `INVALID_FIELD_NAME` | validation | Forbidden `__` sequence in field name |
| `IMMUTABLE_FIELD` | validation | Attempted to modify a system-protected field (e.g., `id`) |
| `PERMISSION_DENIED` | authorization | `authorization.json` rejected the operation |
| `SCHEMA_VALIDATION_FAILED` | validation | Data doesn't match the schema definition |
| `INVALID_MESSAGE` | validation | Malformed MessagePack or missing `type` field |
| `INVALID_MESSAGE_FORMAT` | validation | Missing required fields: type or id |
| `INVALID_MESSAGE_TYPE` | validation | Only binary MessagePack frames are supported |
| `INVALID_ARRAY_ELEMENT` | validation | Non-literal element in array |
| `RATE_LIMITED` | rate-limit | Too many messages; respect `retryAfter` if present |
| `MESSAGE_TOO_LARGE` | client | Payload exceeds `maxMessageSize` |
| `REQUEST_SUPERSEDED` | state | Scope superseded by newer request |
| `BATCH_TOO_LARGE` | client | Batch exceeds 500 operations |
| `ENGINE_UNHEALTHY` | server | Write engine is in degraded state |
| `CONNECTION_FAILED` | connection | Transport-level failure (WebSocket closed) |
| `TIMEOUT` | connection | Server-side processing exceeded timeout |
| `INTERNAL_ERROR` | server | Unexpected server failure (crash/bug) |
| `MAX_CONNECTIONS_REACHED` | connection | Server at capacity |
| `SUBSCRIPTION_NOT_FOUND` | state | `subId` not recognized (stale subscription) |
| `RESOURCE_EXHAUSTED` | rate-limit | Subscription engine memory budget reached; reduce active subscriptions |
Extensibility Adding new message types client→server message types added future versions ignore unknown values Adding new fields fields existing message types ignore unknown fields allows non-breaking evolution Protocol version `Connected message `protocolVersion field if version negotiation necessary clients servers use documented protocol Message Type Summary| Direction | Type | SDK method | Response? |
|-----------|------|------------|-----------|
| C→S | `StoreSet` | `store.set(path, value)` | `ok` |
| C→S | `StoreRemove` | `store.remove(path)` | `ok` |
| C→S | `StoreBatch` | `store.batch(ops)` | `ok` |
| C→S | `StoreQuery` | `store.get(path)`, `store.query(collection, opts)` | `ok` with `value` + `nextCursor` |
| C→S | `StoreSubscribe` | `store.listen(path, cb)`, `store.subscribe(collection, opts, cb)` | `ok` with `subId`, `value`, `hasMore` |
| C→S | `StoreUnsubscribe` | (unsubscribe function) | `ok` |
| C→S | `StoreLoadMore` | `subscription.loadMore()` | `ok` with `subId`, `value`, `hasMore`, `nextCursor` |
| C→S | `PresenceSet` | `presence.set(data)` | `ok` |
| C→S | `PresenceSetShared` | `presence.setShared(data)` | `ok` |
| C→S | `PresenceSubscribe` | `presence.subscribe(cb)` | `ok` with `subId`, `users` (current snapshot) |
| C→S | `PresenceUnsubscribe` | (unsubscribe function from `subscribe`) | `ok` |
| C→S | `PresenceSubscribeShared` | `presence.subscribeShared(cb)` | `ok` with `subId`, `shared` |
| C→S | `PresenceUnsubscribeShared` | (unsubscribe function from `subscribeShared`) | `ok` |
| C→S | `PresenceRemove` | `presence.remove()` | `ok` |
| C→S | `StoreSetNamespace` | `client.setStoreNamespace(ns)` | `ok` with store-scoped `session` |
| C→S | `PresenceSetNamespace` | `client.setPresenceNamespace(ns)` | `ok` with presence-scoped `session` |
| C→S | `AuthRefresh` | `client.authRefresh(token)` | `ok` with `session` |
| S→C | `SchemaSync` | — | Push (no `id`); includes `presenceUserFields`, `presenceSharedFields` |
| S→C | `Connected` | — | Push (no `id`) |
| S→C | `StoreDelta` | — | Push (no `id`) |
| S→C | `WriteCommitted` | — | Push (no `id`, SDK-consumed) |
| S→C | `WriteError` | — | Push (no `id`) |
| S→C | `PresenceBroadcast` | — | Push (no `id`); `users` array with `userId` (bin16), `event`, `data` (integer-keyed), `joinedAt` |
| S→C | `SharedStateBroadcast` | — | Push (no `id`); integer-keyed shared state delta |
| S→C | `ServerDisconnect` | — | Push (no `id`) |
