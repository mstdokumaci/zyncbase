ZyncBase Wire Protocol Specification 2026-03-09 v1 Table of Contents [Overview [Transport Serialization [Message Envelope [Client→Server Messages [Server→Client Messages [Ticket Endpoints (HTTP) [Connection Lifecycle [Error Format [Extensibility [Internal Messages (Zig Hook Server 11. [Message Type Summary specifies client-server contract ZyncBase WebSocket message **MessagePack-encoded JSON Each message conforms spec principles **1:1 SDK mapping** client SDK method maps to one message type No overloaded messages **Correlate by client request unique `id` server response echoes **Pushes unsolicited** Server-initiated messages own types carry request `id` **Extend, never break** New fields added any message type Unknown fields ignored[Extensibility Transport Serialization Property Value Transport WebSocket (RFC 6455) Frame type Binary`opcode 0x02 Serialization MessagePack JSON (debug Max message size 10 MB (configurable.rateLimit.maxMessageSize Max nesting depth 32 levels ADR-010 Compression None v1 ADR-012) Message Envelope MessagePack `type` field Client-originated messages carry `id` request-response correlation Client→Server envelope```
{
  "type": <string>,     // Message type (see catalog below)
  "id":   <uint64>,     // Unique request ID, generated by client
  ...                   // Type-specific fields (inline, not nested in a "payload" key)
}
```response envelope```
{
  "type": "ok" | "error",  // Result type
  "id":   <uint64>,        // Echoed request ID
  ...                      // Type-specific fields
}
```envelope```
{
  "type": <string>,     // Push type (e.g., "StoreDelta", "PresenceBroadcast")
  ...                   // Type-specific fields (no "id" — pushes are unsolicited)
}
```flat payload under key adds indirection no benefit Keeping fields inline simpler compact wire Client→Server Messages Store operations `StoreGet` Retrieve value path```
{
  "type": "StoreGet",
  "id":   1,
  "path": ["tasks", "task-1", "title"]   // Array of strings (canonical format)
}
```"ok"```
{
  "type":  "ok",
  "id":    1,
  "value": <any>    // Array for collections, Object for documents, scalar for properties, null if not found
}
```#### `StoreSet` value path Applied client.```
{
  "type":  "StoreSet",
  "id":    2,
  "path":  ["elements", "rect-1"],
  "value": { "x": 100, "y": 200, "width": 50, "height": 50 }
}
```"ok"```
{
  "type": "ok",
  "id":   2
}
```failure error response triggers optimistic revert client `StoreRemove value path```
{
  "type": "StoreRemove",
  "id":   3,
  "path": ["elements", "rect-1"]
}
```"ok"```
{
  "type": "ok",
  "id":   3
}
````StoreBatch write operations See Operations Specification wire format details execution logic```
{
  "type": "StoreBatch",
  "id":   4,
  "ops": [
    ["s", ["tasks", "123"], { "status": "assigned" }],
    ["s", ["users", "bob", "taskCount"], 5],
    ["r", ["temporary_locks", "123"]]
  ]
}
```codes `"s"` Set `path `value `"r"` Remove `path "ok"```
{
  "type": "ok",
  "id":   4
}
```failure `details` object error include `batchIndex` first operation failure `StoreSubscribe` receiving real-time updates path```
{
  "type": "StoreSubscribe",
  "id":   4,
  "path": ["elements"]
}
```"ok" includes snapshot```
{
  "type":  "ok",
  "id":    4,
  "subId": "sub-abc-123",      // Server-assigned subscription ID (used for unsubscribe and delta matching)
  "value": [ ... ]             // Current snapshot at path
}
```subscription server pushes `StoreDelta messages Pushes `StoreUnsubscribe Stop updates subscription```
{
  "type":  "StoreUnsubscribe",
  "id":    5,
  "subId": "sub-abc-123"
}
```"ok"```
{
  "type": "ok",
  "id":   5
}
````StoreQuery-off filtered query-real-time).```
{
  "type":  "StoreQuery",
  "id":    6,
  "path":  ["users"],
  "where": {
    "age":    { "gte": 18 },
    "status": { "eq": "active" }
  },
  "orderBy": { "created_at": "desc" },
  "limit":   50,
  "after":   "eyJpZCI6... "
}
```"ok"```
{
  "type":       "ok",
  "id":         6,
  "value":      [ ... ],    // Array of matching items
  "nextCursor": "eyJpZCI6... " // Opaque token for next page, null if end
}
````StoreQuerySubscribe filtered query results real-time```
{
  "type":    "StoreQuerySubscribe",
  "id":      7,
  "path":    ["tasks"],
  "where":   { "status": { "eq": "active" } },
  "orderBy": { "created_at": "desc" },
  "limit":   50
}
```"ok" includes snapshot```
{
  "type":       "ok",
  "id":         7,
  "subId":      "sub-def-456",
  "value":      [ ... ],
  "hasMore":    true
}
````StoreLoadMore historical data subscription```
{
  "type":  "StoreLoadMore",
  "id":    8,
  "subId": "sub-def-456"
}
```"ok server responds `ok` message pushes `StoreDelta additional items subscription pushes `StoreDelta query result changes Presence operations `PresenceSet Update local user presence data Partial updates merge state```
{
  "type": "PresenceSet",
  "id":   8,
  "data": { "cursor": { "x": 100, "y": 200 }, "color": "#ff0000" }
}
```"ok"```
{
  "type": "ok",
  "id":   8
}
```processing server broadcasts `PresenceBroadcast users namespace `PresenceSubscribe` Subscribe presence changes current namespace```
{
  "type": "PresenceSubscribe",
  "id":   9
}
```"ok" includes current presence snapshot```
{
  "type":  "ok",
  "id":    9,
  "subId": "sub-pres-789",
  "users": [
    {
      "userId":   "user-123",
      "data":     { "cursor": { "x": 100, "y": 200 }, "color": "#ff0000" },
      "joinedAt": 1234567890
    }
  ]
}
````PresenceUnsubscribe Stop updates```
{
  "type":  "PresenceUnsubscribe",
  "id":    10,
  "subId": "sub-pres-789"
}
```"ok"```
{
  "type": "ok",
  "id":   10
}
````PresenceRemove local user presence data done disconnect).```
{
  "type": "PresenceRemove",
  "id":   11
}
```"ok"```
{
  "type": "ok",
  "id":   11
}
````AuthRefresh Update connection session context JWT```
{
  "type":  "AuthRefresh",
  "id":    11,
  "token": "<new_external_jwt>"
}
```"ok"```
{
  "type":    "ok",
  "id":      11,
  "session": { ... } // The newly resolved $session context
}
```Namespace operations `StoreSetNamespace Switch store namespace```
{
  "type":      "StoreSetNamespace",
  "id":        12,
  "namespace": "tenant:acme:workspace:ws-2"
}
```"ok"```
{
  "type": "ok",
  "id":   12
}
```store subscriptions invalidated namespace changes client re-subscribe Switch namespace Clears old joins new```
{
  "type":      "PresenceSetNamespace",
  "id":        13,
  "namespace": "tenant:acme:document:doc-456"
}
```"ok"```
{
  "type": "ok",
  "id":   13
}
```Server→Client Messages Response messages client requests use "ok"` or "error"` `type` `"ok"` Request succeeded Fields depend on original request type `"error"` Request failed [Error Format] include `id` original request Server push messages unsolicited messages request `id` `StoreDelta` Notifies subscribed client data path query changed```
{
  "type":  "StoreDelta",
  "subId": "sub-abc-123",              // Which subscription this delta belongs to
  "ops":   [                           // Array of atomic operations
    {
      "op":    "set",                  // "set" | "remove"
      "path":  ["elements", "rect-2"],
      "value": { "x": 50, "y": 75 }   // Present for "set", absent for "remove"
    },
    {
      "op":   "remove",
      "path": ["elements", "rect-3"]
    }
  ]
}
```**Operation types `op` Description `"set"` value created updated contains new state `"remove"` value deleted No `value` field note send per-operation deltas snapshots client maintains local state applies operations query subscriptions full re-evaluation if delta individual ops sort-order change). `PresenceBroadcast` Notifies clients presence changes```
{
  "type":  "PresenceBroadcast",
  "subId": "sub-pres-789",
  "event": "update",                  // "join" | "update" | "leave"
  "userId": "user-123",
  "data":   { "cursor": { "x": 101, "y": 201 } }    // Present for "join" and "update"
}
```types `event` Description `"join" user entered presence namespace `data` initial presence"update" user updated presence `data` contains changed fields"leave" user left presence No `data` field `ServerDisconnect` Server-initiated disconnection shutdown token expiry).```
{
  "type":    "ServerDisconnect",
  "code":    "TOKEN_EXPIRED",
  "message": "Your authentication token has expired."
}
```Ticket Endpoints (HTTP Ticket Exchange WebSocket client obtains single-use ticket```
POST /auth/ticket
Authorization: Bearer <external_jwt>
````HTTP 200 OK```json
{
  "ticket":    "zyc_tk_abc123...",
  "expiresAt": 1741551234
}
```Connection Lifecycle WebSocket Upgrade opens WebSocket connection ticket```
GET /ws?ticket=zyc_tk_abc123... HTTP/1.1
Upgrade: websocket
Connection: Upgrade
Origin: https://app.example.com
```server validates Origin header Ticket signature expiry$session success HTTP 101 Switching Protocols failure HTTP 401 403. Connected message WebSocket server sends `Connected push```
{
  "type":               "Connected",
  "userId":             "user-123",       // Authenticated user ID (from sub claim)
  "session":            { ... },          // The full resolved $session context
  "storeNamespace":     "tenant:acme",    
  "presenceNamespace":  "tenant:acme"     
}
```client wait message before messages 4. Heartbeat server sends WebSocket frames interval 30 client responds frames no pong received timeout 10 server closes connection No application-level heartbeat messages needed WebSocket ping/pong 5. close sends WebSocket close frame Server cleans subscriptions presence-initiated sends `ServerDisconnect message closes WebSocket 6. Reconnection Strategy connection lost exponential backoff strategy optional jitter prevent restart recommended formula delay next attempt `min(reconnectDelay * 2^attempt + jitter maxReconnectDelay) Error Format errors consistent envelope```
{
  "type":       "error",
  "id":         2,                          // Echoed request ID (if responding to a request)
  "code":       "SCHEMA_VALIDATION_FAILED", // Machine-readable error code
  "category":   "validation",               // Functional category (see ERROR_TAXONOMY.md)
  "message":    "Field 'priority' must be an integer, got string.",
  "retryAfter": 5000,                       // (Optional) ms to wait before retry
  "details":    { "priority": ["must be integer"] } // (Optional) field-level error context
}
```Error codes Code Category Description `AUTH_FAILED` auth Invalid ticket expired JWT `TOKEN_EXPIRED` auth Session expired re-authenticate `NAMESPACE_UNAUTHORIZED` authorization Not authorized access namespace `COLLECTION_NOT_FOUND` authorization Table/Collection name not schema `FIELD_NOT_FOUND` validation Field name path value not schema `PERMISSION_DENIED` authorizationHook Server rejected operation `SCHEMA_VALIDATION_FAILED Data match schema definition `INVALID_MESSAGE Malformed MessagePack missing `type field `RATE_LIMITED` Too many messages `retryAfter `MESSAGE_TOO_LARGE` Payload exceeds `maxMessageSize `CONNECTION_FAILED` Transport-level failure (WebSocket closed `TIMEOUT Server processing exceeded timeout `INTERNAL_ERROR` Unexpected server failure `HOOK_SERVER_UNAVAILABLE` Zig reach Hook Server process `HOOK_DENIED` Developer TypeScript hook returned error `SUBSCRIPTION_NOT_FOUND` `subId not recognized subscription Extensibility Adding new message types client→server types future versions ignore unknown `type values Adding new fields existing message types ignore unknown fields non-breaking evolution Protocol version `Connected` message include `protocolVersion field future versions v1 protocol version implicitly `1` version negotiation necessary added to `Connected response without changing handshakeInternal Messages (Zig ↔ Hook Server IPC/WebSocket channel Zig core Hook Server `HookServerOnConnect` Sent ticket requested session refreshed```json
{
  "type":      "HookServerOnConnect",
  "jwt":       { ...claims... },  // Already verified by Zig
  "namespace": "tenant:acme"      // Initial namespace context
}
```"ok"```json
{
  "type":    "ok",
  "session": { "role": "admin", "tenant_id": "acme" } // Becomes the $session
}
````HookServerAuthCheck Sent.json delegates rule hook server```json
{
  "type":      "HookServerAuthCheck",
  "function":  "checkDocumentAccess",
  "session":   { ... },
  "namespace": "tenant:acme",
  "path":      ["docs", "123"],
  "value":     { ... } // The data being written (if applicable)
}
```"ok"```json
{
  "type":    "ok",
  "allowed": true
}
```Message Summary Direction SDK method Response C→S `StoreGet `store.get(path) `ok with `value C→S `StoreSet `store.set(path value `ok C→S `StoreRemove `store(path) `ok C→S `StoreBatch `store.batch(ops) `ok C→S `StoreSubscribe(path `ok with `subId `value` C→S `StoreUnsubscribe` `ok C→S `StoreQuery.query(path `ok with `value `nextCursor C→S `StoreQuerySubscribe `store.subscribe(path with `subId `value `hasMore C→S `StoreLoadMore` `subscription.loadMore( `StoreDelta` C→S `PresenceSet` `presence.set(data) `ok C→S `PresenceSubscribe.subscribe(cb) `ok with `subId `users` C→S `PresenceUnsubscribe` function `ok C→S `PresenceRemove` `presence.remove() `ok` C→S `StoreSetNamespace `client.setStoreNamespace(ns) `ok C→S `PresenceSetNamespace.setPresenceNamespace(ns) `ok` C→S `AuthRefresh `client.reauthenticate(token) `ok `session S→C `Connected` Push (no `id S→C `StoreDelta` Push `id S→C `PresenceBroadcast` `id S→C `ServerDisconnect` Push `id Z↔H `HookServerOnConnect` Request/Response Z↔H `HookServerAuthCheck` Request/Response