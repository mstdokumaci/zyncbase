ZyncBase Wire Protocol Specification 2026-03-09 v1 Table of Contents [Overview [Transport Serialization [Message Envelope [Client→Server Messages [Server→Client Messages [Ticket Endpoints (HTTP) [Connection Lifecycle [Error Format [Extensibility [Internal Messages (Zig Hook Server [Message Type Summary specifies client-server contract ZyncBase WebSocket message **MessagePack-encoded JSON Each message conforms to defined spec pseudo-literals for MessagePack values public SDK surface document IDs reference fields remain strings wire packed into 16-byte binary IDs principles **1:1 SDK mapping** client SDK method maps to one message type No overloaded messages **Correlate by client request unique `id` server response echoes unsolicited** Server-initiated messages own types carry request `id` **Extend, never break** New fields added to any message type Unknown fields ignored[Extensibility Transport Serialization Property Value Transport WebSocket (RFC 6455) Frame type Binary`opcode 0x02 Serialization MessagePack JSON (debug Max message size 10 MB (configurable.rateLimit.maxMessageSize Max nesting depth 32 levels ADR-010 Compression None v1 ADR-012) Message Envelope MessagePack `type` field Client-originated messages carry `id` request-response correlation Client→Server envelope```
{
  "type": <string>,     // Message type (see catalog below)
  "id":   <uint64>,     // Unique request ID, generated by client
  ...                   // Type-specific fields (inline, not nested in a "payload" key)
}
```response envelope```
{
  "type": "ok" | "error",  // Result type
  "id":   <uint64>,        // Echoed request ID
  ...                      // Type-specific fields
}
```envelope```
{
  "type": <string>,     // Push type (e.g., "StoreDelta", "PresenceBroadcast")
  ...                   // Type-specific fields (no "id" — pushes are unsolicited)
}
```flat payload under key adds indirection no benefit Keeping fields inline simpler compact over wire Client→Server Messages Store operations (ADR-023) read operations through `StoreQuery `StoreSubscribe SDK translates path reads.get into collection-level queries id filters before transmission no `StoreGet message types on wire `StoreSet` value at path Applied client```
{
  "type":  "StoreSet",
  "id":    2,
  "path":  [0, <bin16("rect-1")>],
  "value": { 1: 100, 2: 200, 3: 50, 4: 50 } // Integer-keyed map for field indices
}
```"ok"```
{
  "type": "ok",
  "id":   2
}
```failure error response triggers optimistic revert client `StoreRemove value path```
{
  "type": "StoreRemove",
  "id":   3,
  "path": [0, <bin16("rect-1")>]
}
```"ok"```
{
  "type": "ok",
  "id":   3
}
````StoreBatch write operations See Operations Specification wire format details execution logic```
{
  "type": "StoreBatch",
  "id":   4,
  "ops": [
    ["s", [1, <bin16("123")>], { 3: "assigned" }],
    ["s", [0, <bin16("bob")>, 8], 5],
    ["r", [2, <bin16("123")>]]
  ]
}
```codes `"s"` Set `path `value `"r"` Remove `path "ok"```
{
  "type": "ok",
  "id":   4
}
```failure `details` object error `batchIndex` first operation failure `StoreQuery Execute one-off filtered query-real-time collection Query conditions sort descriptors sent compact positional tuples [Query Grammar encoding```
{
  "type":  "StoreQuery",
  "id":    6,
  "table_index": 0,
  "conditions": [
    [5, 4, 18],              // [field_index, op_code, value] — op 4 = gte
    [2, 0, "active"]         // op 0 = eq
  ],
  "orderBy": [3, 1],  // [field_index, desc_flag] — 1 = desc
  "limit":   50,
  "after":   "..." // Opaque Base64(MessagePack([sort_value, doc_id_bin]))
}
```"ok"```
{
  "type":       "ok",
  "id":         6,
  "value":      [ ... ],    // Array of matching items
  "nextCursor": "..." // Opaque Base64(MessagePack([sort_value, doc_id_bin])) or null
}
````StoreSubscribe filtered query results real `store.subscribe(collection opts cb```
{
  "type":    "StoreSubscribe",
  "id":      7,
  "table_index": 1,
  "conditions": [
    [2, 0, "active"]      // [field_index, op_code, value] — op 0 = eq
  ],
  "orderBy": [8, 1],  // [field_index, desc_flag] — 1 = desc
  "limit":   50
}
```"ok" includes snapshot```
{
  "type":       "ok",
  "id":         7,
  "subId":      456,
  "value":      [ ... ],
  "hasMore":    true,
  "nextCursor": "..."
}
````StoreLoadMore historical data subscription```
{
  "type":       "StoreLoadMore",
  "id":         8,
  "subId":      456,
  "nextCursor": "..."
}
```"ok"```
{
  "type":       "ok",
  "id":         8,
  "subId":      456,
  "value":      [ ... ],
  "hasMore":    true,
  "nextCursor": "..."
}
```subscription server pushes `StoreDelta query result changes `StoreUnsubscribe Stop updates subscription```
{
  "type":  "StoreUnsubscribe",
  "id":    9,
  "subId": 456
}
```"ok"```
{
  "type": "ok",
  "id":   9
}
```Presence operations Update user presence data updates merge state```
{
  "type": "PresenceSet",
  "id":   8,
  "data": { "cursor": { "x": 100, "y": 200 }, "color": "#ff0000" }
}
```"ok"```
{
  "type": "ok",
  "id":   8
}
```processing server broadcasts `PresenceBroadcast users namespace `PresenceSubscribe` Subscribe presence changes current namespace```
{
  "type": "PresenceSubscribe",
  "id":   9
}
```"ok" includes current presence snapshot```
{
  "type":  "ok",
  "id":    9,
  "subId": "sub-pres-789",
  "users": [
    {
      "userId":   "user-123",
      "data":     { "cursor": { "x": 100, "y": 200 }, "color": "#ff0000" },
      "joinedAt": 1234567890
    }
  ]
}
````PresenceUnsubscribe Stop updates```
{
  "type":  "PresenceUnsubscribe",
  "id":    10,
  "subId": "sub-pres-789"
}
```"ok"```
{
  "type": "ok",
  "id":   10
}
````PresenceRemove local user presence data done disconnect).```
{
  "type": "PresenceRemove",
  "id":   11
}
```"ok"```
{
  "type": "ok",
  "id":   11
}
````AuthRefresh Update connection session context JWT```
{
  "type":  "AuthRefresh",
  "id":    11,
  "token": "<new_external_jwt>"
}
```"ok"```
{
  "type":    "ok",
  "id":      11,
  "session": { ... } // The newly resolved $session context
}
```Namespace operations `StoreSetNamespace Switch store namespace```
{
  "type":      "StoreSetNamespace",
  "id":        12,
  "namespace": "tenant:acme:workspace:ws-2"
}
```"ok"```
{
  "type": "ok",
  "id":   12
}
```store subscriptions invalidated namespace changes client re-subscribe Switch namespace Clears old joins new```
{
  "type":      "PresenceSetNamespace",
  "id":        13,
  "namespace": "tenant:acme:document:doc-456"
}
```"ok"```
{
  "type": "ok",
  "id":   13
}
```Server→Client Messages Response messages client requests use "ok"` or "error"`` `"ok"` Request succeeded Fields depend original request type `"error"` Request failed [Error Format] include `id` original request Server push messages unsolicited messages request `id` `StoreDelta` Notifies subscribed client subscription result set changed```
{
  "type":  "StoreDelta",
  "subId": "sub-def-456",             // Subscription ID
  "ops":   [                           // Array of atomic operations
    {
      "op":    "set",                  // "set" | "remove"
      "path":  [0, "rect-2"],
      "value": { 1: 50, 2: 75 }       // Present for "set", absent for "remove"
    },
    {
      "op":   "remove",
      "path": [0, "rect-3"]
    }
  ]
}
```types `op` `"set" record created updated `value full record"remove" record deleted No `value field note (ADR-023) Deltas record-level only full records `set ID `remove SDK handles field-level projection change detection client maintains state applies operations `PresenceBroadcast Notifies clients changes```
{
  "type":  "PresenceBroadcast",
  "subId": "sub-pres-789",
  "event": "update",                  // "join" | "update" | "leave"
  "userId": "user-123",
  "data":   { "cursor": { "x": 101, "y": 201 } }    // Present for "join" and "update"
}
```types `event` Description `"join" user entered presence namespace `data` initial presence"update" user updated presence `data` contains changed fields"leave" user left presence No `data` field `ServerDisconnect` Server-initiated disconnection shutdown token expiry).```
{
  "type":    "ServerDisconnect",
  "code":    "TOKEN_EXPIRED",
  "message": "Your authentication token has expired."
}
```Ticket Endpoints (HTTP Ticket Exchange WebSocket client obtains single-use ticket```
POST /auth/ticket
Authorization: Bearer <external_jwt>
````HTTP 200 OK```json
{
  "ticket":    "zyc_tk_abc123...",
  "expiresAt": 1741551234
}
```Connection Lifecycle WebSocket Upgrade opens WebSocket connection ticket```
GET /ws?ticket=zyc_tk_abc123... HTTP/1.1
Upgrade: websocket
Connection: Upgrade
Origin: https://app.example.com
```server validates Origin header prevention Ticket signature expiry `$session` success HTTP 101 Switching Protocols failure HTTP 401 403. SchemaSync Connected WebSocket established server pushes two messages `SchemaSync dictionary integer routing SDK hash payload track queue compatibility `fields` array matches Zig server memory layout system columns`id_id_at_at included SDK indices align```
{
  "type":   "SchemaSync",
  "tables": ["users", "tasks"],
  "fields": [
    ["id", "namespace_id", "email", "created_at", "updated_at"],
    ["id", "namespace_id", "title", "status", "created_at", "updated_at"]
  ],
  "fieldFlags": [
    [3, 1, 0, 1, 1], // bit 0 = system column, bit 1 = doc_id
    [3, 1, 0, 0, 1, 1]
  ]
}
````Connected session context```
{
  "type":               "Connected",
  "userId":             "user-123",       // Authenticated user ID (from sub claim)
  "session":            { ... },          // The full resolved $session context
  "storeNamespace":     "tenant:acme",    
  "presenceNamespace":  "tenant:acme"     
}
```client wait message before messages 4. Heartbeat server sends WebSocket frames interval 30 client responds frames no pong received timeout 10 server closes connection No application-level heartbeat messages needed WebSocket ping/pong 5. close sends WebSocket close frame Server cleans subscriptions presence-initiated sends `ServerDisconnect message closes WebSocket 6. Reconnection Strategy connection lost exponential backoff strategy optional jitter prevent restart recommended formula delay next attempt `min(reconnectDelay * 2^attempt + jitter maxReconnectDelay) Error Format errors consistent envelope```
{
  "type":       "error",
  "id":         2,                          // Echoed request ID (if responding to a request)
  "code":       "SCHEMA_VALIDATION_FAILED", // Machine-readable error code
  "message":    "Field 'priority' must be an integer, got string.",
  "retryAfter": 5000                        // (Optional) ms to wait before retry
}
```Error codes Code Category Description `AUTH_FAILED` Invalid ticket expired JWT `TOKEN_EXPIRED` auth Session expired re-authenticate `NAMESPACE_UNAUTHORIZED` authorization Not authorized access namespace `COLLECTION_NOT_FOUND` authorization Table/Collection name not schema `FIELD_NOT_FOUND` validation Field name not schema `INVALID_FIELD_NAME` validation Forbidden `__` sequence field name `PERMISSION_DENIED` authorizationHook Server rejected operation `SCHEMA_VALIDATION_FAILED` Data doesn match schema definition `INVALID_MESSAGE` Malformed MessagePack missing `type field `RATE_LIMITED` Too many messages `retryAfter `MESSAGE_TOO_LARGE` Payload exceeds `maxMessageSize` `CONNECTION_FAILED` Transport-level failure (WebSocket closed `TIMEOUT` Server processing exceeded timeout `INTERNAL_ERROR` Unexpected server failure `MAX_CONNECTIONS_REACHED` Server at capacity `INVALID_MESSAGE_FORMAT` Missing fields type id `INVALID_ARRAY_ELEMENT` Non-literal element array `HOOK_SERVER_UNAVAILABLE` Zig reach Hook Server process `HOOK_DENIED` Developer's TypeScript hook returned error `SUBSCRIPTION_NOT_FOUND` `subId not recognized Extensibility Adding new message types message types future versions ignore unknown `type values Adding new fields fields to existing message types ignore unknown fields allows non-breaking evolutionProtocol version `Connected message `protocolVersion field future versions v1 protocol version `1` version negotiation added `Connected response without changing handshake Internal Messages (Zig ↔ Hook Server IPC/WebSocket channel Zig core Hook Server `HookServerOnConnect` ticket requested session refreshed```json
{
  "type":      "HookServerOnConnect",
  "jwt":       { ...claims... },  // Already verified by Zig
  "namespace": "tenant:acme"      // Initial namespace context
}
```"ok"```json
{
  "type":    "ok",
  "session": { "role": "admin", "tenant_id": "acme" } // Becomes the $session
}
````HookServerAuthCheck Sent.json delegates rule hook server```json
{
  "type":      "HookServerAuthCheck",
  "function":  "checkDocumentAccess",
  "session":   { ... },
  "namespace": "tenant:acme",
  "path":      ["docs", "123"],
  "value":     { ... } // The data being written (if applicable)
}
```"ok"```json
{
  "type":    "ok",
  "allowed": true
}
```Message Summary Direction SDK method Response C→S `StoreSet` `store.set(path value) `ok` C→S `StoreRemove `store.remove(path) `ok` C→S `StoreBatch `store.batch(ops `ok` C→S `StoreQuery` `store.get(path).query(collection opts `ok `nextCursor C→S `StoreSubscribe` `store.listen(path.subscribe(collection `ok `subId `value `hasMore C→S `StoreUnsubscribe` function `ok C→S `StoreLoadMore`.loadMore() `ok `subId `value `hasMore `nextCursor C→S `PresenceSet`.set(data) `ok C→S `PresenceSubscribe`.subscribe(cb)` `ok `subId `users` C→S `PresenceUnsubscribe` `ok C→S `PresenceRemove`.remove() `ok` C→S `StoreSetNamespace` `client.setStoreNamespace(ns) `ok` C→S `PresenceSetNamespace.setPresenceNamespace(ns `ok` C→S `AuthRefresh `client.authRefresh(token)` `ok` `session S→C `SchemaSync` Push (no `id S→C `Connected` Push `id S→C `StoreDelta` Push `id S→C `PresenceBroadcast` Push `id S→C `ServerDisconnect` Push `id Z↔H `HookServerOnConnect` Request/Response Z↔H `HookServerAuthCheck` Request/Response