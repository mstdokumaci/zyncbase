Schema Grammar [ADR-003]-003-configuration-design-zero [ADR-011]-011-data-ownership-namespace [ADR-012]-array-fields-sorted [Storage Grammar]-grammar.md defines schema configuration format system table behavior metadata keys serialization layout storage wire communication Source Files| File | Responsibility |
|------|----------------|
| `src/schema/types.zig` | Defines table, field, array, index, and validation structure models. |
| `src/schema/parse.zig` | Deserializes `schema.json`, validates naming rules, and performs dependency validation. |
| `src/schema/format.zig` | Serializes schema definitions for JSON export or remote sync payloads. |
| `src/schema/system.zig` | Injects implicit system tables (e.g. `users`) and implicit presence models. |
| `src/schema/index.zig` | Builds dense table/field lookup maps used by storage and wire dictionaries. |
| `src/schema/field_path.zig` | Normalizes dotted paths and nested field names to flat column names. |
| `src/sql/ddl.zig` | Translates parsed schema state into relational SQLite `CREATE TABLE` and `CREATE INDEX` queries. |
| `src/sql/build.zig`, `src/sql/buf.zig` | SQL fragment builders and quoting helpers shared by storage. |

## Important Types

| Type | Dependencies | Responsibility |
|------|--------------|----------------|
| `Schema` | `Table` map, `PresenceSchema` | Root runtime schema context. |
| `Table` | `Field` map, `UniqueConstraint` list, required field list | Metadata for a persistent collection (e.g. `namespaced` state). |
| `Field` | type enum, metadata | Metadata for a table column, foreign keys, array element type, and indices. |
| `UniqueConstraint` | user-field indexes | One declared unique constraint; stores indexes into `Table.userFields()` in schema order and owns its allocation. Constraint metadata never changes canonical field order or wire dictionary indexes. |
| `PresenceSchema` | user field map, shared field map | Typed ephemeral presence layout definitions. |
| `SchemaIndex` | table and field maps | Dense lookup structure used by integer wire routing and query parsing. |
Root Schema configuration rooted not table map| Key | Type | Required | Description |
|:---|:---:|:---:|:---|
| `version` | `string` | yes | Schema version string. |
| `store` | `object` | yes | Map of table names to table definitions. |
| `presence` | `object` | no | User/shared presence field definitions. If omitted, a minimal implicit presence schema is synthesized. |
| `metadata` | `object` | no | Preserved schema metadata for operators/tools. |
`Config.schema` inline object or string path JSON file Table & Field Properties Reference Table Properties| Key | Type | Default | Description |
|:---|:---:|:---|:---|
| `fields` | `object` | - | Map of field names to field definitions. |
| `required` | `array<string>` | `[]` | List of required field names (supports dot notation for nested fields). |
| `unique` | `array<array<string>>` | `[]` | Unique constraints as arrays of field paths (dot notation). A one-element inner array is a single-field constraint; multiple elements form one compound constraint. Formats back as an omitted property when empty. |
| `namespaced` | `boolean` | `true` | If `true`, rows are scoped to the active namespace. If `false`, rows are stored globally under namespace ID `0`. |

### Field Properties

| Key | Type | Default | Description |
|:---|:---:|:---|:---|
| `type` | `string` | - | **Required.** One of: `string`, `integer`, `number`, `boolean`, `array`, `object`. |
| `indexed` | `boolean` | `false` | Creates a database index for this column. Reference fields are indexed regardless of this setting. |
| `references` | `string` | `null` | Target table name for an immediately enforced foreign key to that table's `id`. Stored as `BLOB(16)` internally. |
| `onDelete` | `string` | `"restrict"` | Enforced delete rule: `set_null`, `cascade`, or `restrict`. `set_null` is invalid for required fields. |
| `items` | `string` | - | **Required for `array` type.** Primitive element type (e.g., `"string"`, `"integer"`). |
| `fields` | `object` | - | **Required for `object` type.** Map of sub-fields (arbitrary nesting allowed). |
| `metadata` | `object` | `null` | Preserved field metadata for tooling and generated clients. |
| `enum`, `pattern`, `format`, `minLength`, `maxLength`, `minimum`, `maximum` | mixed | `null` | Accepted as reserved validation keywords by the parser, but not represented in runtime `Field` state or enforced on writes. |
Unique Constraint Validation `unique accepted table-level key field outer item array empty arrays component string field path Dots normalize same path rules `required`.handle resolves field path stored user field same table System fields_id_id.external_id named field repeat within constraint same field set in two constraints Field order constraint preserved controls SQLite index prefix usability Constraints allowed on optional reference array fields no field-level `"unique" true alias Malformed definitions fail schema startup error no wire code Unique Equality SQLite equality column representation text case-sensitive default BINARY collation constraints compare ordered key arrays compare canonical encoded representation constrained component `NULL` SQLite permits multiple present-and-unique requires `required` No normalization collation selection partial predicate expression support Supported Field Types| JSON Type | SQLite Storage Type | Flattening Behavior | Description |
|:---|:---:|:---|:---|
| `string` | `TEXT` | Flat column | UTF-8 string value. |
| `integer` | `INTEGER` | Flat column | 64-bit signed integer value. |
| `number` | `REAL` | Flat column | 64-bit floating point value. |
| `boolean` | `INTEGER` | Flat column | Boolean value (stored as 0 or 1). |
| `array` | `BLOB` | Flat column | Persistent canonical sorted-set representation. |
| `object` | (None) | Flat columns | Parser-only container. Nested leaves are flattened using `__` separator. |
System Table `users` hybrid system table Defaults"namespaced false under namespace ID `0 `id `BLOB(16) UUIDv7 (Primary `external_id` `TEXT` `owner_id` equal to `id`-Namespace Constraint** `users configured"namespaced true `SetNamespace binds connection single Changing rejected `NAMESPACE_SWITCH_REJECTED **Identity Resolver** scoped session auto-upserts identity mapping UUIDv7 user row missing```json
{
  "version": "1.0.0",
  "store": {
    "users": {
      "namespaced": false,
      "fields": {}
    }
  }
}
```Naming Storage Invariants/Field match[A-Za Nested objects flattened into database columns double-underscore `__` separators `profile__userId Namespaces with `_zync_` for internal database systems Identifier keys not contain `__`-in table includes `id `namespace_id `owner_id `created_at `updated `created_at` immutable indexed unique `updated_at` unique table supports maximum 1024 fields flattened fields excluding system Index Name Prefixes** ZyncBase-managed indexes named `idx__<table `uidx__>__...` double-underscore table unambiguous contain `__` prefixes reserved per table migration reconciliation drop matching index Unrelated manual index names untouched Typed Arrays Sorted Sets Fields `array require primitive type declaration via `items` Nested arrays objects prohibited Arrays serialized stored as unique sorted arrays return canonical representation Presence Schema Root| Key | Type | Description |
|:---|:---:|:---|
| `user` | `object` | Map of presence fields owned per connected user. |
| `shared` | `object` | Map of namespace-level shared presence fields. |
presence root closed key fails schema startup Presence object fields accept `type `fields primitive fields `type` validation constraints Store-only properties`indexed `onDelete `required Presence fields optional sparse merges Implicit Presence Schema Layout `presence` omitted `schema.json server synthesizes layout```json
{
  "presence": {
    "user": {
      "status": { "type": "string" }
    },
    "shared": {}
  }
}
```Wire Index Derivation server flattens presence definitions index arrays| Phase | Target | Iteration / Rule | Output |
|:---|:---|:---|:---|
| **Phase 1** | `presence.user` | Recursively iterates keys in definition order. Object sub-fields are joined with `__`. | Array `presenceUserFields[]`. (Position = Wire Index). |
| **Phase 2** | `presence.shared` | Recursively iterates keys in definition order. Object sub-fields are joined with `__`. | Array `presenceSharedFields[]`. (Position = Wire Index). |
Metadata Reserved Validation objects preserved schema table store-field boundaries clients operator tooling parser validates stores `enum `minLength `maxLength Store presence writes enforce constraints before operation Reference values `null field optional Non-null values identify existing table failed writes restricted deletes use `SCHEMA_VALIDATION_FAILED error contract References ID-based cross IDs table-wide keys Foreign keys immediate batch create parent before child UPDATE actions deferred constraints not supported Related Specifications [Error Taxonomy [Storage Grammar