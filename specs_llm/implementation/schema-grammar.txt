ZyncBase Schema Grammar defines grammar property specification for `schema.json` Root Structure `version` `string` version schema`MAJOR.MINOR.PATCH[PLANNED]` migration logic `store` `object` Map table names to definitions Table Definition `fields` `object` field names to definitions `required` `array<string>` field names dot notation `namespaced` `boolean` `true` rows scoped to active namespace `false` rows stored under global namespace ID `0` visible across namespaces `users` collection defaults to `false` Table Name Constraints valid JSON key match SQL-safe identifier pattern `[A-Za not contain `__` start with `_zync_` tables name `users` reserved for hybrid system collection SQLite reserved keywords allowed ZyncBase quotes identifiers SQL Reserved System Collections `users` special hybrid system table map external identity claims to internal `BLOB(16)` UUIDv7s `owner_id compact binary format JSON```json
{
  "users": {
    "namespaced": false,
    "fields": {}
  }
}
```Global defaults to"namespaced false stores rows under global namespace ID `0` Columns implicitly created `external_id`TEXT column `id standard `BLOB(16) UUIDv7 `owner_id equal to `id` Key mapping unique by(namespace_id, external_id) default global mode global uniqueness"namespaced true identity string different users-Upsert WebSocket authenticates Zig looks JWT `external_id namespace new UUIDv7 row if missing ensures relational Foreign Keys to `users.id satisfied Users `users configured"namespaced true$session.userId resolved for active namespace Namespace switching re-resolving ownership checks namespace-local user ID define `users` in `schema.json append custom fields omitted engine treats implicit JSON Field Definition contain `type` property Field Name Constraints match SQL-safe identifier pattern `[A-Za not contain `__` flattened object paths not use reserved system field names `id `namespace_id `owner_id `created_at `updated_at`users` collection use `external_id` reserved identity-provider subject mapping Supported Types SQLite Mapping `string` `TEXT` UTF-8 text string `integer` 64-bit signed integer `number `REAL 64-bit floating point number `boolean` `INTEGER` Boolean (0 or 1) `array `BLOB` canonical JSON array (sorted unique primitive-typed `items `object` Logical grouping fields Shared Properties Key Type Default Description `type` `string` `indexed` `boolean` `false` Creates SQLite index column `references` `string` `null` Target table name foreign key relationship Referenced fields stored as packed `doc_id` values SDK exposes strings `onDelete` `string` `"restrict"` `set_null` `restrict` `set_null` requires field optional Array Properties field `type: "array" contain `items` propertyKey Type Default Description `items` `string` Type items array primitive Array Semantics (Canonical Sorted Set field "array ZyncBase enforces sorted-set behavior Elements match primitive type `null` array elements Nested arrays objects rejected`INVALID_ARRAY_ELEMENT write arrays normalized to sorted unique form Reads return canonical sorted unique representation Nested Objects Flattening ZyncBase relational storage Nested objects grouped schema flattened database `__` (double Restriction** Field names match `[A-Za] contain `__` reserved system field names Unlimited depth supported `object types `fields property Example```json
"profile": {
    "type": "object",
    "fields": {
        "userId": { "type": "string" }
    }
}
```Flattens SQLite column `profile__userId TEXT` wire flattened string names bypassed SDK maps into integer `field_index routing payloads Columns storage table includes five system columns `id` Stored `BLOB(16) transmitted wire MessagePack `bin(16)` SDK converts user string IDs (UUIDv7) to representation `namespace_id` Stored `INTEGER` foreign key `_zync_namespaces` Namespaced tables use active namespace ID global tables use reserved global ID `0` `owner_id` Stored `BLOB(16)` populated server with `$session.userId` upon document creation equal to `id` `created `updated_at` Stored `INTEGER` timestamps Validation Constraints properties part north star spec by implementation Applicable Types `enum` `integer List allowed values `pattern` Regex pattern validation `format Known formats `minLength Minimum character length `maxLength Maximum character length `minimum Minimum numeric value`maximum` `integer `number` Maximum numeric value Error Catalog errors returned by `SchemaParser` Error `InvalidSchema` not valid JSON object `MissingVersion` `version key missing `InvalidVersion `version not string `MissingStore` `store key missing `InvalidStore` `store not object `InvalidTableName` name empty non-letter non-alphanumeric/non-underscore `__` `InvalidTableDefinition table value `store not object `MissingFieldType` lacks `type` property `InvalidFieldDefinition field value not object `InvalidFieldType` `type value not string `InvalidFieldName empty non-letter non-alphanumeric/non-underscore `__` `UnknownFieldType` `type` string not recognized `InvalidOnDelete` `onDelete value not `cascade `restrict `set_null used on `required` field `MissingArrayItems` `items` property missing for `array` field `InvalidArrayItems` `items value not a string`UnsupportedArrayItemsType` `items` not allowed primitive types