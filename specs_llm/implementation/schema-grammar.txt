ZyncBase Schema Grammar defines grammar property specification `schema.json` Root Structure `version` `string` version schema`MAJOR.MINOR.PATCH `store `object` table names to definitions `presence` `object` Optional presence field definitions absent server synthesizes implicit minimal schema Presence Schema Contains `user `shared` keys Table Definition `fields` `object` field names to definitions `required` `array<string>` field names dot notation `namespaced` `boolean` `true` rows scoped to active namespace `false` rows stored under global namespace ID `0` visible across namespaces `users` collection defaults to `false` Table Name Constraints valid JSON key match SQL-safe identifier pattern[A-Za not contain `__` start with `_zync_` name `users` reserved for hybrid system collection SQLite reserved keywords allowed ZyncBase quotes identifiers SQL Reserved System Collections `users` special hybrid system tablepresent database map external identity claims Auth0 `BLOB(16) UUIDv7s ensuring `owner_id tables compact binary JSON```json
{
  "users": {
    "namespaced": false,
    "fields": {}
  }
}
```Global defaults to"namespaced false stores rows under global namespace ID `0` Columns created `external_id column `id standard `BLOB(16) UUIDv7 `owner_id equal to `id` Key mapping unique by(namespace_id, external_id) default global mode global uniqueness"namespaced true identity string different users-Upsert scoped session Zig resolves identity through `external_id new UUIDv7 row if missing Anonymous SDK client IDs authenticated JWT subjects use same resolver ensures `owner_id `userId relational foreign keys `users.id reference persisted user rows **Namespaced Users configured"namespaced true identity resolution uses namespace ID scope Store presence scopes resolve to different user IDs namespaces differ Namespace switching re-resolving Gate Store operations require ready scoped session namespace user resolution lifecycle messages auth valid **Extensible define `users` in `schema.json append optional custom fields Custom fields `required identity rows auto-created before profile data omitted engine treats as implicit JSONField Definition contain `type` property Field Name Constraints match SQL-safe identifier pattern `[A-Za-z][A_]*` contain `__` for flattened nested object paths use reserved system field names `id `namespace_id `owner_id `created `updated `users` collection not use `external_id` reserved for identity-provider subject mapping Supported Types SQLite Mapping `string` `TEXT UTF-8 text string `integer` 64-bit signed integer `number 64-bit floating point number `boolean Boolean (0 or 1) `array canonical JSON array (sorted unique primitive-typed via `items `object` Logical grouping of fields Shared Properties Type Default `type` `string` `indexed` `false Creates SQLite index for column `references` `string `null Target table name for foreign key relationship Referenced fields stored as packed `doc_id` values SDK exposes as strings`onDelete` `string` `"restrict"` `set_null` `cascade `restrict` `set_null` requires field optional Array Properties field "array" contain `items` property Key Type Default Description `items` `string` Type items array primitive Array Semantics (Canonical Sorted Set) ZyncBase enforces canonical sorted-set behavior Elements match primitive type `null` array elements rejected Nested arrays objects rejected`INVALID_ARRAY_ELEMENT write arrays normalized to sorted unique form Reads return sorted unique representation Nested Objects Flattening **flat relational storage Nested objects grouped schema flattened database **Separator** `__` (double underscore). Restriction** Field names match `[A-Za contain `__` reserved system field names Unlimited depth supported `object` types `store` field definitions `presence` field definitions one level nesting `cursor: { x, y valid y rejected at```json
"profile": {
    "type": "object",
    "fields": {
        "userId": { "type": "string" }
    }
}
```Flattens SQLite column `profile__userId TEXT flattened string names bypassed SDK maps into integer `field_index routing payloads Columns storage table includes five system columns `id` Stored `BLOB(16) transmitted wire MessagePack `bin(16)` SDK converts user string IDs (UUIDv7) collection-wide document identity KEY `namespace_id not part document key `namespace_id` Stored `INTEGER` foreign key `_zync_namespaces Namespaced tables use active namespace ID global tables use global ID `0` `owner_id` Stored `BLOB(16)` populated server `$session.userId` document creation equal to `id` `created_at `updated_at` Stored `INTEGER` timestamps Presence Grammar optional top-level key defines typed presence fields contain `user` key `shared` key optional Presence Root Key Type Description `object Map presence field names definitions per user `shared Optional definitions Namespace-level statePresence Field Definition fields support store field grammar Property Applicable Types Description `type **Required `string `integer `number `boolean `object` `fields` `object` type sub-field names scalar field definitions Max one level nesting `enum` `string` `integer` allowed values `pattern` `string` Regex pattern validation `minLength` `string` Minimum length `maxLength` `string Maximum character length `minimum `integer `number` Minimum numeric value `maximum Maximum numeric value fields support `indexed `references `onDelete `items` `required` no Presence Definition Key Type Description `user` `object` Required `presence defined user-owned ephemeral field definitions One record per user per namespace `shared` `object` Optional namespace-level ephemeral field definitions One record entire namespace Presence Field Name Constraints match SQL-safe pattern `[A-Za`not contain `__` separator for flattened nested object paths use reserved system field names `id `namespace_id `updated Presence Nesting One level `object` nesting supported Sub-fields flattened `__` separator wire encoding Deeper nesting at startup Implicit Presence Schema `presence key absent `schema.json server synthesizes minimal implicit schema```json
{
  "presence": {
    "user": {
      "status": { "type": "string", "enum": ["active", "idle", "away"] }
    },
    "shared": {}
  }
}
```produces `presenceUserFields `presenceSharedFields in `SchemaSync message Presence usable call `presence.set( status "active without defining schema implicit schema default for common presence use case/offline explicit `presence section in `schema.json replaces implicit schema Explicit schemas recommended for custom presence fields typing Wire Index Derivation server startup flattens `presence.user.shared into index arrays Iterate `presence.user keys definition order field expand sub-fields `parentName__subFieldName scalar field emit name resulting flat array `presenceUserFields[]` Array position = wire integer index Repeat `presence.shared` `presenceSharedFields[]` arrays emitted in `SchemaSync message new connection Presence Error Catalog `InvalidPresence key present not object `MissingPresenceUser present key missing `InvalidPresenceUser not object `InvalidPresenceShared present not object `InvalidPresenceFieldName violates naming constraints`InvalidPresenceFieldType field `type missing not string supported `PresenceNestingTooDeep` presence `object field contains another `object field (max one `InvalidPresenceObjectField` presence field missing `fields or not object Validation Constraints Applicable Types Description `enum` `string `integer` List allowed values `pattern` `string` Regex pattern validation `format` `string` Known formats`email `uuid `ipv4 `minLength` `string Minimum length `maxLength` `string Maximum character length `minimum `integer `number Minimum numeric value `maximum` `integer Maximum value Error Catalog errors returned by `Schema.init` `InvalidSchema` File not valid JSON object `MissingVersion` `version key missing `InvalidVersion `version not string `MissingStore` `store key missing `InvalidStore` `store not object`InvalidTableName empty starts non-letter non-alphanumeric/non-underscore `__` `InvalidTableDefinition` table value `store not object `MissingFieldType field definition lacks `type` property `InvalidFieldDefinition field value not object `InvalidFieldType `type value not string `InvalidFieldName Field name empty starts non-letter non-alphanumeric/non-underscore `__` `UnknownFieldType` `type string not recognized `InvalidOnDelete` value not `cascade `restrict `set_null` used `required` field `MissingArrayItems` `items` property missing `array` field `InvalidArrayItems` `items not string `UnsupportedArrayItemsType `items` type not allowed primitive types