ZyncBase Authentication Exchange Draft v1 Principle ZyncBase Server (Validator not Identity Provider manage accounts passwords OAuth flows trusts external tokens developer Hook identity$session` Context source truth authorization replaces raw$jwt `authorization.json rules Populated `onConnect hook Hook Server no Hook Server$session defaults standard claims validated external JWT Fixed WebSocket connection Connection Lifecycle Exchange security flexibility ZyncBase uses mandatory ticket-based handshake Step Ticket Request client SDK sends external JWT ZyncBase HTTP endpoint```http
POST /auth/ticket
Authorization: Bearer <external_jwt>
```Step Enrichment Hook Server validates signature external JWT Hook Server configured sends```json
// Zig -> Hook Server
{ "type": "HookServerOnConnect", "jwt": { ...claims... }, "namespace": "..." }
```Hook Server object```json
// Hook Server -> Zig
{ "type": "ok", "session": { "tenant_id": "acme", "role": "admin" } }
```Step 3: Ticket Issuance generates short-lived single-use signed resolved```json
// Response to Client
{ "ticket": "zyc_tk_8s2k...", "expiresAt": 1741551234 }
```4: WebSocket SDK WebSocket ticket query string```
ws://server/ws?ticket=zyc_tk_8s2k...
```Zig verifies ticket signature extracts$session hydrates connection context accepts upgrade Hook Server call required Token Refresh external JWTs ZyncBase tickets short-lived WebSockets long-lived SDK session without disconnecting Sends `AuthRefresh WebSocket new JWT Re-validates JWT Calls Hook Server updated enrichment Updates$session context Sends `ok response client operations evaluate against updated$session Security Performance-Trust Zig accepts WebSocket connection without valid ticket$session connection.json evaluation no foreign calls Hook Server Latency Relational lookups cached in$session sync fastError Conditions HTTP/WS Response Ticket Request JWT signature invalid expired HTTP 401 `AUTH_FAILED Hook Server unreachable HTTP 503 `HOOK_SERVER_UNAVAILABLE error HTTP 403 `HOOK_DENIED Ticket expired 5 min HTTP 401 `AUTH_FAILED Ticket used Origin `allowedOrigins 403 `AuthRefresh JWT invalid error `AUTH_FAILED Server error `HOOK_DENIED` failure connection not established closed No partial session state retained