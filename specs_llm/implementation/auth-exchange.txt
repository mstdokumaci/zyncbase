ZyncBase Authentication Exchange_EXCHANGE.md Draft v1 Core Principle ZyncBase **Resource Server (Validator not Identity Provider manage accounts passwords OAuth flows trusts external tokens relies developer **Bun Hook resolve identity local session context `$session` Context unified source truth authorization replaces raw$jwt `authorization.json rules Populated via `onConnect hook Hook Server no Hook Server$session defaults to standard claims validated external JWT Fixed duration WebSocket connection Connection Lifecycle (Ticket Exchange security flexibility ZyncBase uses mandatory ticket-based handshake Step Ticket Request (HTTP client SDK sends external JWT to ZyncBase HTTP endpoint```http
POST /auth/ticket
Authorization: Bearer <external_jwt>
```Step 2: Enrichment Hook Server validates signature external JWT Hook Server configured Zig sends message```json
// Zig -> Hook Server
{ "type": "HookServerOnConnect", "jwt": { ...claims... }, "namespace": "..." }
```Hook Server session object```json
// Hook Server -> Zig
{ "type": "ok", "session": { "tenant_id": "acme", "role": "admin" } }
```Step 3: Ticket Issuance (Zig generates short-lived single-use signed blob resolved$session```json
// Response to Client
{ "ticket": "zyc_tk_8s2k...", "expiresAt": 1741551234 }
```Step 4: WebSocket SDK connects WebSocket ticket query string```
ws://server/ws?ticket=zyc_tk_8s2k...
```Zig verifies ticket signature extracts `$session hydrates connection context accepts upgrade Hook Server call required Token Refresh external JWTs ZyncBase tickets short-lived WebSockets long-lived SDK rotate session without disconnecting Sends `AuthRefresh message WebSocket new JWT Re-validates new JWT Calls Hook Server `onConnect for updated enrichment Updates `$session` context Sends `ok` response client operations evaluate against updated `$session` Security Performance **Zero-Trust Handshake** Zig accepts WebSocket connection without valid ticket **Microsecond Authorization** `$session` baked connection `authorization.json evaluation Zig no foreign calls Hook Server Latency Relational lookups done cached in$session sync loop fastError Conditions HTTP/WS Response Ticket Request JWT signature invalid expired HTTP 401 `AUTH_FAILED` Hook Server unreachable HTTP 503 `HOOK_SERVER_UNAVAILABLE` Hook Server `onConnect error HTTP 403 `HOOK_DENIED` WebSocket Upgrade Ticket expired> 5 min HTTP 401 `AUTH_FAILED` Ticket used Origin not `allowedOrigins` HTTP 403 `AuthRefresh` JWT invalid WS error `AUTH_FAILED` Hook Server `onConnect error WS error `HOOK_DENIED` failure connection not established closed No partial session state retained