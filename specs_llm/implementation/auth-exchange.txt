ZyncBase Authentication Exchange_EXCHANGE Draft v1 Principle ZyncBase Server (Validator not Identity Provider manage accounts passwords OAuth flows trusts external tokens Hook identity$session` Context unified source truth authorization replaces raw$jwt `authorization.json rules Populated `onConnect hook Hook Server no Hook Server$session defaults standard claims validated external JWT Fixed WebSocket connection Connection Lifecycle Exchange security flexibility ZyncBase uses mandatory ticket-based handshake Step Ticket Request client SDK sends external JWT ZyncBase endpoint Authorization_jwt Enrichment Hook Server validates signature JWT Bun Hook Server configured sends `HookServerOnConnect messageZig Hook Server Server returns enriched session object Zig_id Step 3: Ticket Issuance generates short-lived single-use signed blob resolved `$session` Response Client_tk_8s2k..." 1741551234 Step 4: WebSocket Connection SDK connects WebSocket ticket query string_tk_8s2k... Zig verifies ticket signature extracts$session hydrates connection context accepts upgrade Hook Server call required Token Refresh JWTs ZyncBase tickets short-lived WebSockets long-lived SDK rotate session without Sends `AuthRefresh new JWT Re-validates JWT Calls Hook Server `onConnect updated enrichment Updates internal `$session` context Sends `ok` response client operations evaluate against updated `$session` Security Performance-Trust Zig accepts WebSocket connection without valid ticket**Microsecond Authorization**$session into connection `authorization.json evaluation Zig no foreign calls Hook Server Latency Relational lookups done cached in$session sync fast Error Conditions HTTP/WS Response JWT signature invalid expired HTTP 401 `AUTH_FAILED` Hook Server unreachable HTTP 503 `HOOK_SERVER_UNAVAILABLE error 403 `HOOK_DENIED Ticket expired 5 min HTTP 401 `AUTH_FAILED Ticket used Origin not `allowedOrigins 403 `AuthRefresh JWT invalid `AUTH_FAILED Server `onConnect returns error `HOOK_DENIED failure connection not established closed No partial session state retained