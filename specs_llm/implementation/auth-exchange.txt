ZyncBase Authentication Exchange Principle **resource not identity provider manage accounts passwords OAuth flows memberships billing permission truth accepts identity from **External JWTs** **Anonymous SDK authentication server validates identity projects trusted claims into$session issues connection ticket enforces `authorization.json in Zig$session` unified source truth for authorization Rules inspect raw JWTs Configured claim projection from external JWT **Anonymous SDK-generated high-entropy subject `isAnonymous = true$session.userId internal.id resolved SQLite never raw JWT anonymous Permission data exist in$session claims namespace parts target row incoming value load user-row fields perform relationship lookups for authorization Fixed for resolved scope until auth refresh switching re-resolution JWT-backed session```json
{
  "sub": "auth0|123",
  "tenant_id": "acme",
  "role": "admin",
  "read_projects": ["docs", "wiki"],
  "write_projects": ["docs"]
}
```anonymous```json
{
  "sub": "anon:6c6f8b0d0f1c4d4c9d61b7fdc4f80f52",
  "isAnonymous": true,
  "role": "anonymous"
}
```Connection Lifecycle ZyncBase ticket handshake JWTs WebSocket URLs Step Ticket Request authenticated SDK sends JWT ZyncBase HTTP endpoint```http
POST /auth/ticket
Authorization: Bearer <external_jwt>
```anonymous users SDK sends subject authentication enabled```http
POST /auth/ticket
Content-Type: application/json

{ "anonymousSubject": "anon:6c6f8b0d0f1c4d4c9d61b7fdc4f80f52" }
```anonymous subject bearer credential SDK high-entropy randomness locally Step Identity Validation Session Projection JWT authentication Zig validates signature issuer audience expiration not-before times algorithm key constraints ZyncBase projects claims$session anonymous authentication Zig validates auth subject format entropy creates$session No user-row fields loaded Ticket Issuance Zig generates short single-use ticket signed opaque blob subject base$session issue time expiry replay-prevention identifier```json
{
  "ticket": "zyc_tk_8s2k...",
  "expiresAt": 1741551234
}
```Step 4: WebSocket SDK connects WebSocket ticket query```text
ws://server/ws?ticket=zyc_tk_8s2k...
```Zig verifies ticket signature expiry single-use state transport state$session Store presence operations gated until namespaces selected `users.id values resolved Step 5 Scoped Readiness SDK sends initial store presence namespace selections Zig resolves namespace string_zync_namespaces.id external subject `users.id$session.userId() resolves after scopes ready If `users.namespaced false identity resolution uses global namespace ID `0` true uses namespace ID Token Refresh External JWTs tickets short-lived WebSockets long-lived SDK rotate session without disconnecting Sends `AuthRefresh new JWT Re-validates JWT expiry issuer Projects new$session` JWT updates token expiry Sends `ok` response updated$session` scopes continue validation fails server sends `ServerDisconnect `AUTH_FAILED closes connection operations evaluate against updated$session` Short-lived tokens plus refresh normal permission-update revocation path ZyncBase maintain server-side permission database revocation list Security Performance **Zero-Trust Zig accepts WebSocket connection without valid ticketJWTs exchanged over HTTP headers refresh frames not WebSocket query strings$session connection state.json evaluation Zig no foreign calls Hidden Auth `users table resolved internal identity ownership Profile fields application data not authorization inputs Permission claims human-manageable use roles groups scoped grants active-context tokens unbounded permission lists Error Conditions| Step | Condition | HTTP/WS Response |
|------|-----------|-----------------|
| Ticket Request | JWT signature invalid or expired | HTTP 401 `AUTH_FAILED` |
| Ticket Request | JWT issuer, audience, or algorithm invalid | HTTP 401 `AUTH_FAILED` |
| Ticket Request | Anonymous auth disabled | HTTP 401 `AUTH_FAILED` |
| Ticket Request | Anonymous subject invalid | HTTP 400 `INVALID_MESSAGE` |
| WebSocket Upgrade | Ticket expired | HTTP 401 `AUTH_FAILED` |
| WebSocket Upgrade | Ticket already used | HTTP 401 `AUTH_FAILED` |
| WebSocket Upgrade | Origin not in `allowedOrigins` | HTTP 403 |
| `AuthRefresh` | New JWT invalid or expired | `ServerDisconnect` `AUTH_FAILED`, connection closed |
| `AuthRefresh` | Subject mismatch (different user) | `ServerDisconnect` `AUTH_FAILED`, connection closed |
failure cases connection established terminated connected No partial session state retained