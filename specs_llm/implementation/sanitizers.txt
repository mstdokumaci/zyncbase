Sanitizer Configuration [Threading Implementation [Memory Management specifies sanitizers test targets invariants passing CI run ZyncBase uses sanitizer strategies memory concurrency correctness **ThreadSanitizer enforces lock-free cache SWMR invariant no unsynchronized writes no read/write races shared state **GeneralPurposeAllocator safety enforces arena/GPA invariant GPA allocation freed once no-free double-free Valgrind supplementary tool for C interop paths (uWebSockets SQLite Invariants Enforced Sanitizer No data race on `LockFreeCache.entries pointer `CacheEntry field `write_mutex held when mutating cache entries Types internal synchronization remain address-stable once use bit-copies safe initialization moving locked mutex SIGABRT/corruption Mac `PresenceManager.pending_updates` written from single flush goroutine GPA safety  Every `alloc` from GPA has one matching `free` Arena memory never accessed after `resetArena()` Pool `release` never called on item not acquired Test Targets & Sanitizer Matrix Test file  TSan GPA safety `src/cache_stress_test.zig`/core_engine_test.zig`/presence_manager_test.zig`/memory_strategy_test.zig`/request_handler_test.zig`/query_engine_test.zig TSan required on all tests shared mutable state across threads GPA safety on in debug builds CI Pass / Fail Contract run passes when `zig build test -Dsanitize=thread` exits 0 no TSan warnings exits no GPA leak report at exit run fails when TSan warning appears treated as build failures GPA reports leak at exit memory leaks test panics or returns non-zero exit code TSan warnings false positives suppressed via `tsan_suppressions.file checked repo comment explaining suppression Verification Commands```bash
# TSan — must be clean
zig build test -Dsanitize=thread

# GPA leak check — must be clean (debug build, default)
zig build test

# Supplementary: Valgrind for C interop paths
valgrind --leak-check=full --error-exitcode=1 ./zig-out/bin/zyncbase-test
```See [Threading Implementation lock-free cache SWMR model [Memory Management GPA arena pool strategy-Free Cache ref_count invariants