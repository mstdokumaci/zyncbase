Sanitizers [Threading Strategy-Free Cache ZyncBase uses sanitizers concurrency safety prevent data races leak memory operations testing execution Source Files| File/Context | Responsibility |
|------|----------------|
| `build.zig` | Configures compiler instrumentation options for sanitizers (`-fsanitize=thread`). |
| `src/memory_safety_property_test.zig` | Fuzzes boundary conditions to trigger GPA leak detection. |
| `src/lock_free_cache_leak_test.zig` | Validates that retired cache nodes are correctly destroyed under stress load. |
| `src/subscription_engine_thread_safety_test.zig` | Stresses subscription engines to detect concurrency races under TSan. |
Sanitizer Invariants **ThreadSanitizer Enforces serialized writes lock-free cache unsynchronized memory access between read threads thread **GeneralPurposeAllocator Enforces leak-free execution debug builds logs error if memory released before exit Stability** Live threads at stable heap addresses synchronization objects bit-copied triggers panic failures **Self-Healing Releasing non items `IndexPool runtime assertions CI Verification Rules **Zero Race Tolerance** TSan warning CI failure Leak Tolerance** memory leak report from GPA exit fails build pipeline **Valgrind C interop interface boundaries trace allocations outside Zig runtime [Threading [Memory Strategy-Free Cache