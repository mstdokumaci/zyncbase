ZyncBase Authorization Format.json v1 Stable Replaces row-level security with high-performance JSON-declarative model by ZyncBase Zig core Evaluated on MessagePack frame [Configuration API Design/configuration.md Authorization rules hook management requirements Core Principles **Deny by Default** access denied unless allowed by rule **Variables Context** Rules evaluated against context$session resolved session context JWT Hook$namespace current string$path specific data path$doc` existing document database attribute-based access control **Query Language Conditions use same JSON structure as ZyncBase Query Language explicit `or`$session.role easy parsing safe evaluation without `eval() **Separation of Concerns** Namespaces handle path rules handle fine access Rule Format Structure file organized by **Namespaces** followed by **Paths** within namespaces```json
{
  "rules": [
    {
      "namespace": "public",
      "read": true,
      "write": false
    },
    {
      "namespace": "tenant:${tenant_id}:*",
      "condition": { "$session.tenantId": { "eq": "$namespace.tenant_id" } },
      
      "presence": {
        "read": true,
        "write": true
      },

      "paths": [
        {
          "path": "*",
          "read": true,
          "write": { "$session.role": { "in": ["admin", "editor"] } }
        },
        {
          "path": "users.${user_id}",
          "read": true,
          "write": { "$session.sub": { "eq": "$path.user_id" } }
        }
      ]
    }
  ]
}
```Evaluation Order Conflict Resolution **Top-Down Evaluation** Rules evaluated top-down first matching rule wins **Early Exit** rule grants access evaluation stops access permitted **Namespace-First** frame namespace checked first no namespace rule matches fails frame rejected bypassing path evaluation Namespace Wildcard Behavior Session Expectations Namespaces use colon-separated segment model Wildcards match segments extract variables$namespace context ZyncBase stateless for authorization hierarchical namespace authorization requires data in$session **Example 1: Tenant Isolation Session contains "tenant_id "acme Namespace `tenant:${tenant_id Rule$namespace.tenant_id$session.tenant_id User connects to namespace `tenant:acme ZyncBase extracts `acme$namespace.tenant_id compares to$session.tenant_id If match access granted **Example 2: Workspace Isolation namespace `tenant:acme:workspace:123` ZyncBase know user allowed in `workspace:123? A (Injected arrays `$session contains array allowed workspaces Rule$namespace.workspace$session.workspaces **Option B (Document-level check If `$session` user ID ZyncBase check database`$doc user member workspace requires Hook Server delegation Section 7) initial implementation prioritize **Namespace matching against `$session hierarchy shallow until necessity `$doc` evaluation Presence API Authorization Unlike Store API Presence API flat-wide concept Users join presence namespace broadcast state listen others presence authorization defined at `namespace` level `paths``read`** subscribe presence channel see online`write broadcast presence object (schema validation applies). presence ephemeral no `$doc` equivalent no path-level routing **Hook Server Support Presence rules support Hook Server Delegation **Section 7 Hook relational lookups before user join presence channel paid subscriber?"), delegate like path rule```json
"presence": {
  "read": { "hook": "checkPresenceAccess" },
  "write": { "$session.role": { "in": ["admin", "paid"] } }
}
```Composition Model (Conditions mirror `QUERY_LANGUAGE.md` operators evaluation engine Zig **Boolean Values** `true (allow `false `eq `ne `in `contains `and` `or````json
"write": {
  "or": [
    { "$session.role": { "eq": "admin" } },
    { 
      "$session.role": { "eq": "editor" },
      "$session.sub": { "eq": "$path.user_id" }
    }
  ]
}
```Bun Hook Server (Managing Complexity tension in authorization between **stateless **complex relational permissions** user workspace folder ZyncBase line`authorization.json` for stateless checks$namespace.tenant $session.tenant_id fast evaluated in Zig. rule database lookup delegated to Bun Hook Server Turing-complete database lookup engine into `authorization.json ZyncBase provides out-of-box Bun WebSocket server Developers write TypeScript function handle complex auth logic **Example `authorization.json` delegation```json
{
  "namespace": "tenant:*",
  "paths": [
    {
      "path": "documents.*",
      "write": { "hook": "checkDocumentAccess" } 
    }
  ]
}
```MessagePack ZyncBase maintains high-speed connection to local Bun Hook Server write matches"hook rule ZyncBase streams MessagePack payload$session$namespace$path incoming `value requested function name"checkDocumentAccess Bun Hook Server executes developer's TypeScript function access to database Prisma Drizzle raw SQL fetch Hook Server responds `true or `false (deny MessagePack payload Developer Experience writes authorization logic file `zyncbase.auth.ts CLI spins Bun Hook Server Hook Server privileged ZyncBase developer queries ZyncBase `client.store API admin bypasses `authorization.json eliminates configure Prisma Drizzle raw SQL for authorization```typescript
// zyncbase.auth.ts
import { createAdminClient } from '@zyncbase/server';

// Automatically configured to talk to the local Zig core over IPC/WebSocket
const client = createAdminClient();

// The function name matches the `"hook"` rule in authorization.json
export async function checkDocumentAccess({ session, namespace, path, value }) {
  const workspaceId = namespace.split(':')[1];
  
  // Use the exact same Query API you use on the frontend
  const memberships = await client.store.query('workspace_members', {
    where: { 
      userId: { eq: session.sub },
      workspaceId: { eq: workspaceId }
    }
  });

  if (memberships.length === 0) return false;
  
  const role = memberships[0].role;
  return role === 'admin' || role === 'editor';
}
```architecture ensures ZyncBase core fast decoupled business logic smooth unified API experience