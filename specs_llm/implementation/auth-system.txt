ZyncBase Authorization Format.json v1 Stable Replaces row-level security with high-performance JSON-declarative model ZyncBase Zig core Evaluated on MessagePack frame [Configuration API Design/configuration.md Authorization rules hook management requirements Core Principles **Deny by Default** access denied unless allowed by rule **Variables Rules evaluated against limited context$session resolved session context JWT Hook$namespace current namespace string parts$doc` Columns existing target row AST Injection SQL$value incoming mutation payload$path table name `$doc` constrain same selected updated deleted rule requiring relationship traversal relational join lookup table forbidden in JSON delegated to Hook Server via"hook rule Language Conditions use same JSON structure as ZyncBase Query Language (implicit ANDs explicit `or`$session.role easy parsing AST injection **Decoupled Configuration** Namespaces handle horizontal presence store handles vertical collection-level access2. Rule Format Structure file organized two arrays`namespaces full grammar see [Auth Grammar-grammar.md).```json
{
  "namespaces": [
    {
      "pattern": "tenant:{tenant_id}",
      "storeFilter": { "$session.tenantId": { "eq": "$namespace.tenant_id" } },
      "presenceRead": { "$session.tenantId": { "eq": "$namespace.tenant_id" } },
      "presenceWrite": true
    }
  ],
  "store": [
    {
      "collection": "tasks",
      "read": true,
      "write": { "$doc.owner_id": { "eq": "$session.userId" } }
    }
  ]
}
```Evaluation Order Conflict Resolution-Down Evaluation** Rules `namespaces arrays evaluated first matching pattern wins **Early Exit** rule grants access evaluation stops access permitted **Namespace-First** frame namespace evaluated via `StoreSetNamespace `PresenceSetNamespace first If no namespace rule matches connection context not set subsequent path evaluations irrelevant Namespace Wildcard Behavior Session Expectations Namespaces use colon-separated segment model Wildcards match segments extract variables into$namespace context ZyncBase stateless for authorization hierarchical namespace authorization requires data in$session **Example 1: Tenant Isolation Session contains "tenant_id "acme Namespace `tenant:$_id Rule$namespace.tenant_id$session.tenant_id User connects to `tenant:acme ZyncBase extracts `acme as$namespace.tenant_id compares to$session.tenant_id If match access granted 2: Project Isolation (Stateless Arrays namespace `project:a` ZyncBase user project$session contains allowed project IDs Rule```json
  "namespaces": [
    {
      "pattern": "project:{project_id}",
      "storeFilter": { "$namespace.project_id": { "in": "$session.projects" } }
    }
  ]
  ```using arrays JWT/Session ZyncBase scope validation RAM without Hook Server database Presence API Authorization Unlike Store API data paths Presence API flat namespace-wide concept Users join presence namespace broadcast state listen others presence authorization defined at `namespace` level alongside `paths`read subscribe presence channel see online`write broadcast presence object (schema validation presence ephemeral no `$doc` equivalent no path-level routing **Hook Server Support Presence rules support Hook Server Delegation relational lookups before user channel paid delegate```json
{
  "namespaces": [
    {
      "pattern": "premium:*",
      "presenceRead": { "hook": "checkPresenceAccess" },
      "presenceWrite": { "$session.role": { "in": ["admin", "paid"] } }
    }
  ]
}
```Composition Model (Conditions mirror `QUERY_LANGUAGE.md` operators evaluation engine Zig **Boolean Values** `true (allow `false `eq `ne `in `contains `and` `or````json
"write": {
  "or": [
    { "$session.role": { "eq": "admin" } },
    { 
      "$session.role": { "eq": "editor" },
      "$path": { "eq": "tasks" },
      "$doc.owner_id": { "eq": "$session.userId" }
    }
  ]
}
```Bun Hook Server (Managing Complexity tension in authorization between relational permissions** user workspace folder ZyncBase`authorization.json` for RAM checks same-row SQL predicates$namespace.tenant $session.tenant_id$doc.owner_id.userId fast evaluated natively in Zig. rule relationship traversal join lookup outside target delegated to Bun Hook Server Turing-complete database lookup engine into `authorization.json ZyncBase provides out-of-box Bun WebSocket server Developers write TypeScript function handle complex auth logic `authorization.json` delegation```json
{
  "store": [
    {
      "collection": "documents",
      "write": { "hook": "checkDocumentAccess" } 
    }
  ]
}
```MessagePack ZyncBase maintains high-speed connection to local Bun Hook Server write matches"hook rule ZyncBase streams MessagePack payload$session$namespace$path incoming `value requested function name"checkDocumentAccess Bun Hook Server executes developer's TypeScript function access to database Prisma Drizzle raw SQL fetch Hook Server responds `true or `false (deny MessagePack payload Developer Experience writes authorization logic file `zyncbase.auth.ts CLI spins Bun Hook Server Hook Server privileged ZyncBase developer queries ZyncBase `client.store API admin bypasses `authorization.json eliminates configure Prisma Drizzle raw SQL for authorization```typescript
// zyncbase.auth.ts
import { createAdminClient } from '@zyncbase/server';

// Automatically configured to talk to the local Zig core over IPC/WebSocket
const client = createAdminClient();

// The function name matches the `"hook"` rule in authorization.json
export async function checkDocumentAccess({ session, namespace, path, value }) {
  const workspaceId = namespace.split(':')[1];
  
  // Use the exact same Query API you use on the frontend
  const memberships = await client.store.query('workspace_members', {
    where: { 
      userId: { eq: session.userId },
      workspaceId: { eq: workspaceId }
    }
  });

  if (memberships.length === 0) return false;
  
  const role = memberships[0].role;
  return role === 'admin' || role === 'editor';
}
```architecture ensures ZyncBase core fast decoupled business logic smooth unified API experience