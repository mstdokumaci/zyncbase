Message Handler [Memory Management Implementation Protocol `MessageHandler primary processing WebSocket messages implements MessagePack wire protocol handles per-connection memory lifecycle routes operations storage engine Interface Contract```zig
pub const MessageHandler = struct {
    allocator: Allocator,
    memory_strategy: *MemoryStrategy,
    violation_tracker: *ViolationTracker,
    storage_engine: *StorageEngine,
    subscription_manager: *SubscriptionManager,
    connection_registry: ConnectionRegistry,

    /// Process one WebSocket message.
    ///
    /// Guarantees:
    ///   - All temporary allocations (parse buffers, intermediate results) are 
    ///     allocated from a pooled arena via MemoryStrategy.
    ///   - Arena is acquired and released per message.
    ///   - Message processing is synchronized per connection via a mutex in Connection state.
    ///   - Operations are routed to StorageEngine after MessagePack decoding.
    pub fn handleMessage(
        self: *MessageHandler, 
        ws: *WebSocket, 
        message: []const u8, 
        msg_type: MessageType
    ) !void {
        // ... acquisition and mutex locking ...
        const arena = try self.memory_strategy.acquireArena();
        defer self.memory_strategy.releaseArena(arena);
        const arena_allocator = arena.allocator();

        var reader: std.Io.Reader = .fixed(message);
        const payload = try msgpack.decode(arena_allocator, &reader);
        try self.routeMessage(ws, arena_allocator, payload);
    }
};
```Scoped Session Gate `routeMessage enforces readiness before dispatching operations `StoreSet `StoreRemove `StoreLoadMore require ready store scope `PresenceSet require ready presence scope `AuthRefresh `StoreSetNamespace `PresenceSetNamespace ping/pong close accepted before readiness establish refresh operation arrives before ready handler returns `SESSION_NOT_READY` without touching storage subscriptions presence state Memory Management Strategy Allocation Lifecycle Parse buffers temp strings Pooled Arena Released `handleMessage Persistent state Until connection close Response buffers Allocated `buildSuccessResponse Caller sends via.send() frees Thread Safety Concurrency `MessageHandler shared across threads safety achieved through-Connection struct mutex `handleMessage locks mutex sequential message processing client **Atomic Reference Counting** Connections ref-counted safety during `handleOpen events **Thread-Safe Component `StorageEngine `SubscriptionManager handle internal synchronizationInvariants Error Conditions Description Arena released `defer releaseArena ensures cleanup error paths Processed under mutex Messages same connection run concurrently No leak on invalid MsgPack Faulty payloads caught by decoder arena released Error Cause Behaviour `error.OutOfMemory` ArenaPool GPA exhausted `INTERNAL_ERROR` sent connection throttled `error.InvalidMessage` MessagePack parse failure_MESSAGE violation tracked `error.Unauthorized` Permission check failed `error.SessionNotReady` Scoped operation before namespace/user resolution `SESSION_NOT_READY` sent Validation Success Criteria Arena released after every message Correct routing wire protocol operations Scoped store presence operations until scopes ready ThreadSanitizer passes for concurrent message processing multiple connections [Memory Management Arena GPA strategy [Threading Implementation Per-thread memory isolation Protocol Message format handled handler