ZyncBase Configuration Grammar defines grammar property specification for `zyncbase-config.json` General Rules Valid JSON duration/timeout values in **milliseconds** unless **Environment Variables** Values `string` support environment variable substitution `${VAR_NAME} syntax **Strictness** Unknown keys top level section objects ignored warning in logs). Properties Reference Root Level  Key Type Default Description `server` `object Network connection settings `authentication` Identity access tokens `security` Access control rate limiting `logging` Log verbosity formatting `performance` Tuning throughput latency `dataDir`/data Directory for persistence (SQLite `schema`/schema.json Path to schema file config object If omitted default path missing boots with implicit `users` collection `authorization` `string `null Path to `authorization.json` If omitted file missing server boots with safe implicit public playground rules `auth-grammar.md``environment` `string"development" Engine mode `production `server` Key Default Description `port` `number` `3000` Port listen (1-65535) `host` `string` `"0.0.0.0"` Bind address `authentication` Key Default `jwt.secret` `string `null` Secret key HS256/HS384/HS512. `jwt.algorithm` `string"HS256" Supported `HS256 `HS384 `HS512 `RS256` `jwt.issuer` `string `null Validates `iss` claim `jwt.audience` `string `null Validates `aud` claim `session.tokenGracePeriodSeconds` `number `30` Seconds after JWT expiry connection termination `security` Default Description `allowedOrigins` `array<string>` CORS `Access-Control-Allow-Origin` list `allowLocalhost` `boolean `true` allow connections local loopback`maxMessagesPerSecond` `100` Max messages per connection per second `maxConnections` `100000` Max simultaneous connections (global limit `maxMessageSize` `number `1048576` Max size single WebSocket message (bytes). `violationThreshold` `number `10` security violations before IP ban `logging` `level `string"info" `debug `warn `error `format` `string` `"json"` `text` `performance` `messageBufferSize` `number `1000` Size internal ring buffer for routing `batchWrites` `true` Group multiple writes into single transactions `batchSize` `number `200` Maximum number writes per storage transaction enabled `batchTimeout` `number `10` Wait time batching (ms). `statementCacheSize` `number `100` Max number prepared statements cache per connectionValidation Errors checks performed during `validateConfig` Error Condition `InvalidPort` Port 0 or > 65535 `InvalidDataDir` Parent directory `dataDir exist or not writable `InvalidSchemaFile` object/path malformed schema contains invalid JSON or fails validation Missing/omitted schema files synthesize users-only schema `InvalidAuthorizationFile` file exists or fails authorization grammar validation `InvalidBufferSize` `messageBufferSize 0. `InvalidBatchSize` 0 `InvalidMaxMessageSize 0