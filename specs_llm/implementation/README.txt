Implementation Specifications directory documents current ZyncBase implementation files not second copy source code explain stable contracts ownership dependencies invariants Documentation Implementation specs keep Source modules/types Ownership boundaries dependency direction Runtime lifecycle concurrency memory durability security invariants Wire schema auth SDK contracts Links to ADRs Performance contracts limits thresholds buffer sizes latency targets Threading model subsystem thread synchronization primitives ownership boundaries avoid Zig TypeScript snippets production code Public error-code tables outside Repeating grammar catalogs outside owner files Historical compatibility notes Step-step implementation narratives code comments Canonical catalogs Error codes retry behavior [Error Taxonomy Message names envelope shape server push names [Wire Protocol Schema fields validation rules Grammar Query operators translation rules Authorization config grammar [Auth Grammar Runtime configuration keys [Config Grammar] Specs Current ADRs source truth for architectural decisionsbehavior changes update `specs/architecture/adrs.md implementation specs spec name source files/types code change update matching doc Public contracts documented as tables bullets Use code fences for stable examples JSON wire payload SQL shape command lines Public error codes added `src/wire/errors.zig `sdk/typescript/src/errors.ts [Error Taxonomy-taxonomy.md editing Markdown `specs run `npm specs:compress` `specs_llm/ aligned Cross-Cutting Ownership concerns span subsystems canonical owner flow| Concern | Canonical Owner | Key Mechanism | Thread Boundary |
|---------|----------------|---------------|-----------------|
| Namespace resolution | `connection/session_resolver.zig` | `SessionResolutionBuffer` (lock-free ring) | Writer thread → Event loop |
| Subscription lifecycle | `subscription_engine.zig` | `ChangeBuffer` (lock-free ring, 8192 cap) | Writer thread → Event loop |
| Write acknowledgement | `storage_engine/write_worker.zig` | `SendQueue` (lock-free MPSC) | Writer thread (producer) → Event loop (consumer, drains in post-handler) |
| Notification fanout | `notification_worker_pool.zig` | `ChangeQueue` (sharded SPMC blocking queue) | Writer thread (producer) → `NotificationWorkerPool` workers (consumers) |
| Error propagation | `wire/errors.zig` | `getWireError()` switch table | Synchronous + async via `SendQueue` |
| Schema sync | `wire/encode.zig` + `connection/manager.zig` | Pre-encoded `[]const u8` at startup | Startup only, sent on `onOpen` |
owner file documents flow files link Testing Wire protocol changes require E2E updates New error codes require updates/wire/errors/errors.ts Taxonomy Performance-sensitive changes sizes timeouts batch include benchmark coverage justification Threading changes require sanitizer coverage data-race freedom API contract Core Systems uWebSockets server lifecycle connection callbacks transport rules Protocol MessagePack envelope message names response names compatibility rules Handler WebSocket message routing session gates response/error flow Worker topology serialized writes cross-thread notification synchronization ownership Strategy Allocator ownership request arenas connection state leak checks Engine Internals SQLite WAL setup connection roles schema-to-SQL generation write queue query execution-Free Cache Read-mostly cache contract handle lifetime Engine AST parser SQL lowering pagination subscription filtering Internalstwo-tier presence subscriptions snapshots broadcasts Pagination Cursor determinism live window behavior SDK Internals SDK Client module ownership requests stores presence SDK-local errors Security Reliability Parser limits rate origin policy authorization fail-closed behavior Exchange HTTP ticket exchange WebSocket connection session projection System Declarative authorization model presence authorization Taxonomy public error catalog retry [Sanitizers TSan/GPA/ASan expectations CI pass/fail rules