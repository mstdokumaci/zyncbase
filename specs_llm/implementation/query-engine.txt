Query Engine 2026-03-09 handles filtering sorting real-time subscriptions executes queries in-memory cache SQLite storage tracks clients Fine change detection in-memory subscriptions efficient real-time updates Structure path u8 filters sort limit Opaque token encoded cursor field u8 op value jsonValue FilterOp = enum { eq == ne != gt > gte >= lt < lte <= in contains %value% startsWith endsWith %value isNull NULL isNotNull NOT NULL const Sort = { field:]const u8 direction: enum { asc, desc Query Execution Execution Flow Parse Query Validate syntax Build AST Check Cache Look for cached results Check if cache fresh Query Build SQL (if needed Execute on SQLite reader filter in-memory cache Apply Filters Filter results Sort results Apply limit cursorReturn Results Serialize MessagePack Send client Implementation QueryEngine storage *StorageLayer cache *LockFreeCache execute *QueryEngine Query.Value Try cache self.executeFromCache(query return results Build SQL sql self.buildSQL SQLite reader reader.storage.getReader rows reader.query(sql); Parse results self.parseResults(rows); buildSQL *QueryEngine Query u8 buf = ArrayList(u8).init(self.allocator); Query path table buf.appendSlice("SELECT FROM.appendSlice(query.path); 'tasks' buf.appendSlice(_id = (query.filters) buf.appendSlice( self.appendFilter( (query.sort buf.appendSlice( ORDER.appendSlice(sort.direction .asc DESC (query.limit) std.fmt.format(buf.writer LIMIT.Implementation Decode token apply filters self.appendCursorFilters( return buf.toOwnedSlice Real-time Subscriptions Subscription Tracking Subscription struct id u64 connection_id query last_result.Value_version u64 SubscriptionManager subscriptions HashMap(u64_index HashMap( u8 ArrayList add *SubscriptionManager self.subscriptions.put(sub.id Index namespace lookup.namespace_index.getOrPut(sub list.value_ptr.append(sub remove *SubscriptionManager sub_id u64).subscriptions.fetchRemove(sub_id sub entry.value Remove namespace index_index.get.items sub_id list.swapRemove Change Detection ZyncBase **Fine-Grained Observation** strategy sub-100ms real-time sync without overloading SQLite reader pool Writer thread emits change event id operation old new_row`SubscriptionManager evaluates_row against active subscription filters SQLite queries re during broadcast for filter matches Updates pushed to clients if row matching query changed Fine-Grained Implementation notification pipeline in RAM notify *SubscriptionManager table changed_id new_row.Value.table_index.get.subscriptions.get Evaluate filters in RAM SQLite if self.evaluator.matches(new_row.query.filters Push delta to client self.sendDelta(sub.connection_id new_row); affectsSubscription changed_ids If no filters change affects if.query.filters.len == 0 true Check if changed ID matches filters for_ids handle self.cache.get defer.release self.matchesFilters return true false Presence Awareness ZyncBase presence system tracks user state real-time data stored in-memory details see [Presence InternalsAuthorization Optimization Problem SQL queries for authorization WebSocket bottleneck Cursor movement (100 SQL query (1ms = 100ms latency Solution: Permission Snapshots connection onConnect Execute SQL queries determine permissions.query room_id FROM room_members user_id Cache memory.permissions PermissionSnapshot 1 Fast path checkPermission room_id Memory lookup return.permissions.rooms.contains(room Invalidation onRoomMembershipChange(user_id Find connections user Re-query permissions refreshPermissions Common path Nanosecond memory lookup Rare path SQL query when permissions change Authorization doesn't bottleneck operations [Core Principles Design [Threading Model Parallel query execution [Storage Layer SQLite optimization Layer WebSocket protocol [Query Language-design-language Query syntax reference.md Performance validation