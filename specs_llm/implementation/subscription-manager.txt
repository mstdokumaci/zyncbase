Subscription Manager Implementation Spec v1 Overview `SubscriptionManager` ZyncBase's real-time filtering engine application memory matches queries against SQLite `RowChange events `StoreDelta messages synthesized inside Zig routes subscriptions evaluates edge transitions query Internal Data Structures requires high-performance structures 100,000+ concurrent listeners without blocking relies on memory isolation pointer iteration map lookups Subscription Registry `AutoHashMap of `SubscriptionId` -> `Subscription` tracks active list query bindings```zig
pub const Subscription = struct {
    id: SubscriptionId,
    namespace: []const u8,
    collection: []const u8,
    filter: QueryFilter, // Contains AST logic
    sort: ?SortSpec,     // Important for pagination bounds
    connection_id: u64,
};
```Namespace Indexing `StringHashMap` mapping{namespace:{collection to `ArrayList(SubscriptionId) primary router `RowChange` fires evaluates subscriptions inside namespace bucket Live Condition Evaluation `StorageEngine.insertOrReplace(.updateField.deleteDocument( commits transaction fires `RowChange` struct mutation```zig
pub const RowChange = struct {
    namespace: []const u8,
    collection: []const u8,
    operation: Operation, // .insert | .update | .delete
    old_row: ?Row,
    new_row: ?Row,
};
````RowChange `findMatchingSubscriptions routine executes against candidates{namespace{collection index Resolving Edge Transitions `RowChange four scenarios against active `QueryFilter AST `SubscriptionManager algorithm isolate outcomes Scenario 1: Inside to Inside Previous row matched New row matches Fire `StoreDelta` `op: set client applies patch over item Scenario 2: Outside to Inside Previous match New row matches Fire `StoreDelta` `op: set client inserts item into active array Scenario 3: Inside to Outside crucial document status changes filter parameters ZyncBase eviction signal Previous row matched New row match Synthesize `StoreDelta` `op: remove physical SQLite document exists strike from cache Scenario 4: Outside to Outside Previous row match New row match Omit `StoreDelta` connection Sort Evaluation Rules `Subscription` contains `SortSpec` evaluation engine carries more task document 1: Inside to Inside updated primary sorting value `priority column changed from 1 to 5)(old newsort_field engine fires `StoreDelta UI re-sort real-time data patch cosmetic mapping reducing overhead Memory Locking Context evaluation loop`findMatchingSubscriptions bound `lockShared( path subscription mutex No writes block reads scaling across event streams