Write Coordinator central handles write operations StoreRemove in ZyncBase between protocol`MessageHandler storage layer`StoreEngine subscriber notifications accurate performant capturing full row context during optimistic write Responsibility **Context Fetching** Fetch current state document from metadata cache before write **Change Merging** Construct full `new_row merging partial updates into `old_row-Row Persistence** Submit merged row to `StorageEngine.insertOrReplace partial SDK updates wipe out fields during SQLite `REPLACE operation **Optimistic Notification** Compute matches broadcast changes to subscribers using full `new_row` Component Architecture Data Flow `MessageHandler receives `StoreSet or `StoreRemove delegates to `WriteCoordinator.coordinateSet or.coordinateRemove selects current document from `StorageEngine metadata creates `RowChange` object merged `new_row` submits set columns to `StorageEngine.insertOrReplace calls `SubscriptionEngine.handleRowChange` for notification`WriteCoordinator iterates sends `StoreDelta messages via `ConnectionManager Memory Management uses `Arena-scoped for temporary allocations `RowChange `new_row Error Handling ZyncBase model If `selectDocument fails coordinator proceeds write blind insert best-effort merge Subscriber notifications best-effort "Fire and Forget". If write NACKed storage engine `MessageHandler handles NACK subscriber update not retracted real sync Related Components [Storage Engine [Subscription Engine [Message Handler