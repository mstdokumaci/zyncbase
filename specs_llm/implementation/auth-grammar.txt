ZyncBase Authorization Grammar defines JSON grammar `authorization.json` Root Structure `authorization.json` two decoupled arrays `namespaces `array` Rules for horizontal partitioning access presence operations `store` `array` Rules vertical partitioning (table access document ownership 1. Namespace Definition namespace definition governs access namespace presence channels `pattern` `string` namespace pattern `tenant:{tenant_id} Segments `{}` extracted into `$namespace` context `storeFilter` `Condition Evaluated `StoreSetNamespace` If `false` client rejected from namespace `presenceRead` `Condition Evaluated `PresenceSetNamespace` `PresenceSubscribe` `presenceWrite` `Condition Evaluated `PresenceSet` 2. Store Definition governs read write access to specific table `collection` `string` table name rules apply `tasks `*` catch-all rule collection not matched `read` `Condition` Evaluated on `StoreQuery` `StoreSubscribe``write `Condition` Evaluated `StoreSet `StoreRemove `StoreBatch Condition Grammar **Boolean `true or `false **Hook delegation** "functionName **Logical [Condition **Comparison "LHS" "Operator "RHS" Comparison Object **LHS (Left-Hand Side context variable$session.userId$doc.owner_id **Operator** `eq` `ne `gt `lt `in `notIn `contains` **RHS (Right-Hand Side raw literal (string number boolean array or context variable Variable Scope Evaluation Matrix predictability sub-microsecond performance ZyncBase restricts variables wire command Variables evaluated two execution paths **RAM Evaluation** check memory **Predicate Lowering**$doc` comparisons lowered into `FilterPredicate` shape `StoreQuery `StoreSubscribe` storage layer owns SQL renderingWire Command  `$session` `$namespace` `$path` (table) `$value` (payload) `$doc`  :---  :---: `StoreSetNamespace`  ✅ RAM `PresenceSetNamespace` ✅ RAM `StoreQuery` / `StoreSubscribe`  ✅ RAM Predicate Lowering  `StoreSet`  ✅ RAM Create: RAM candidate / Update: Predicate Lowering `StoreRemove`  ✅ RAM Predicate Lowering `PresenceSet`  ✅ RAM `PresenceSubscribe`  ✅ RAM If rule references forbidden variable for specific command evaluation fails access denied `StoreLoadMore` and `StoreUnsubscribe` inherit authorization `StoreSubscribe`. `StoreSet`, `$doc` in write rules interpreted by write kind: **Create** `$doc` candidate document evaluated in RAM without storage read **Update** `$doc` existing stored document enforced by predicate lowering. **Delete**: `$doc` existing stored document enforced by predicate lowering.create candidate includes document id `owner_id $session.userId normalized incoming fields server-managed fields/defaults create rule references `$doc field absent not server-injected defaulted create denied preserves invariant client create document Predicate Lowering Strategy `$doc` variable fetch document RAM before authorization authorization lowers condition into storage-neutral `FilterPredicate storage renders predicate into SQL `WHERE` fragment values server boot `AuthConfig.init validates store rule against schema Unsupported hooks unknown `$doc` fields invalid operators predicate shapes store-query model fail startup supported `$doc` shape expressive than StoreQuery zero AND conditions plus one OR group Nested `$doc` groups multiple OR groups invalid.```json
"write": { "$doc.owner_id": { "eq": "$session.userId" } }
```creating document server injects `owner_id from$session.userId treats document as$doc evaluates ownership rule updating deleting Zig evaluates$session.userId internal `BLOB(16) user ID recognizes$doc.owner_id as column reference lowers to filter condition storage renders binary parameter `UPDATE tasks SET id = ? owner_id = If user own document 0 rows affected extra overhead accepted writes perform extra reads classify zero-row outcome Confirmed writes classify report `PERMISSION_DENIED` Zero-Config Default `authorization.json missing ZyncBase boots with implicit "public playground" rule guarantees frictionless developer experience prototyping destroying data JSON```json
{
  "namespaces": [
    {
      "pattern": "public",
      "storeFilter": true,
      "presenceRead": true,
      "presenceWrite": true
    }
  ],
  "store": [
    {
      "collection": "*",
      "read": true,
      "write": { "$doc.owner_id": { "eq": "$session.userId" } }
    }
  ]
}
```default means accessible namespace `public` anonymous users read data broadcast in `public` namespace Users create records owned by$session.userId modify or delete records Creates satisfy$doc.owner_id == $session.userId server-side owner injection updates deletes enforce rule rows predicate lowering