ZyncBase Authorization Grammar defines JSON grammar `authorization.json` Root Structure `authorization.json` two decoupled arrays `namespaces `array` Rules for horizontal partitioning access presence operations `store` `array` Rules vertical partitioning (table access document ownership 1. Namespace Definition namespace definition governs access namespace presence channels `pattern` `string` namespace pattern `tenant:{tenant_id} Segments `{}` extracted into `$namespace` context `storeFilter` `Condition Evaluated `StoreSetNamespace` If `false` client rejected from namespace `presenceRead` `Condition Evaluated `PresenceSetNamespace` `PresenceSubscribe` `presenceWrite` `Condition Evaluated `PresenceSet` 2. Store Definition governs read write access to specific table `collection` `string` table name rules apply `tasks `*` catch-all rule collection not matched `read` `Condition` Evaluated on `StoreQuery` `StoreSubscribe``write `Condition` Evaluated `StoreSet `StoreRemove `StoreBatch Condition Grammar **Boolean `true or `false **Hook delegation** "functionName **Logical [Condition **Comparison "LHS "Operator "RHS" Comparison Object **LHS (Left-Hand Side context variable$session.userId$doc.owner_id **Operator** `eq` `ne `gt `lt `in `notIn `contains` **RHS (Right-Hand Side raw literal (string number boolean array or context variable Variable Scope Evaluation Matrix predictability sub-microsecond performance ZyncBase restricts variables wire command Variables evaluated two execution paths **RAM Evaluation** check memory **Predicate Lowering** `$doc` comparisons lowered `FilterPredicate` shape `StoreQuery `StoreSubscribe` storage layer owns SQL renderingWire Command  `$session` `$namespace` `$path` (table) `$value` (payload) `$doc` | :---  :---: `StoreSetNamespace`  ✅ RAM `PresenceSetNamespace` ✅ RAM `StoreQuery` / `StoreSubscribe`  ✅ RAM Predicate Lowering `StoreSet`  ✅ RAM Lowering `StoreRemove`  ✅ RAM Predicate Lowering `PresenceSet`  ✅ RAM `PresenceSubscribe`  ✅ RAM If rule references forbidden variable for specific command evaluation fails access denied `StoreLoadMore` `StoreUnsubscribe` inherit authorization during `StoreSubscribe`. `StoreSet`, `$doc` predicates applied only to existing-row update branch. create has no existing `$doc`; server injects `owner_id` from `$session.userId` evaluates only RAM-available variables (`$session`$namespace`$path` `$value`). Predicate Lowering Strategy `$doc` variable fetch document into RAM before authorizationauthorization layer lowers condition into storage-neutral `FilterPredicate storage renders predicate into SQL `WHERE` fragment values server boot `AuthConfig.init validates store rule against schema Unsupported hooks unknown$doc fields invalid operators shapes store-query fail startup supported `$doc` shape expressive than StoreQuery zero AND conditions plus one OR group Nested `$doc` groups multiple OR groups invalid```json
"write": { "$doc.owner_id": { "eq": "$session.userId" } }
```updating document Zig evaluates$session.userId internal `BLOB(16) user ID recognizes$doc.owner_id column reference lowers to filter condition storage layer renders bound binary parameter `UPDATE tasks SET id ? owner_id = ? If user document 0 rows affected operation fails overhead Zero-Config Default (Implicit Rules `authorization.json missing ZyncBase boots implicit "public playground" rule set guarantees frictionless developer experience prototyping destroying data JSON```json
{
  "namespaces": [
    {
      "pattern": "public",
      "storeFilter": true,
      "presenceRead": true,
      "presenceWrite": true
    }
  ],
  "store": [
    {
      "collection": "*",
      "read": true,
      "write": { "$doc.owner_id": { "eq": "$session.userId" } }
    }
  ]
}
```default means accessible namespace `public` anonymous users read data broadcast in `public` namespace Users create records owned by$session.userId` modify or delete records (enforced via AST Injection$doc.owner_id == $session.userId` on rows).