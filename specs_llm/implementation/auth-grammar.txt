ZyncBase Authorization Grammar defines JSON grammar `authorization.json` Root Structure `authorization.json two decoupled arrays `namespaces `array Rules for horizontal partitioning access presence operations `store `array Rules vertical partitioning (table access document ownership 1. Namespace Definition namespace definition governs access namespace presence channels `pattern` `string` namespace pattern `tenant:{tenant_id} Segments{} extracted into `$namespace` context `storeFilter` Evaluated on `StoreSetNamespace` If `false` client rejected from namespace `presenceRead` Evaluated on `PresenceSetNamespace `presenceWrite` Evaluated `PresenceSet `PresenceRemove` contains incoming field values `presenceSharedWrite` Evaluated `PresenceSetShared` contains values Defaults same value as `presenceWrite` when omitted 2. Store Definition governs read write access to specific tableKey Type Description `collection` `string` table name rules apply `tasks `*` catch-all rule collection not matched earlier rule `read` `Condition` Evaluated `StoreQuery `StoreSubscribe` `write` `Condition` Evaluated `StoreSet` `StoreRemove `StoreBatch` Condition Grammar `Condition` **Boolean literal** `true` or `false` **Logical grouping** "and" [Condition Condition] **Comparison object** "LHS" "Operator" "RHS" Comparison Object **LHS context variable$session.userId$doc.owner_id **Operator** `eq` `ne `in` `contains` **RHS raw literal (string number boolean array or context variable Variable Scope Evaluation Matrix predictability performance ZyncBase restricts variables available wire command Variables evaluated two execution paths **RAM Evaluation** check memory **Predicate Lowering** `$doc` comparisons lowered `FilterPredicate` shape `StoreQuery `StoreSubscribe` storage layer owns SQL rendering.$session` built authentication validated JWT SDK anonymous subject load fields `users` row authorizationWire Command  `$session`  `$namespace` | `$path` (table) `$value` (payload) `$data` (presence)  `$doc` | | :--- | :---:  `StoreSetNamespace` | ✅ RAM  | ❌  `PresenceSetNamespace` | ✅ RAM | ❌ `StoreQuery` / `StoreSubscribe` | ✅ RAM | RAM ❌ Predicate Lowering | `StoreSet` | ✅ RAM  Create: RAM candidate / Update: Predicate Lowering  `StoreRemove` | ✅ RAM  RAM ❌ Predicate Lowering `PresenceSet` | ✅ RAM   ❌ RAM `PresenceSetShared` | ✅ RAM ❌ RAM `PresenceSubscribe` | ✅ RAM || ❌  `PresenceSubscribeShared`  ✅ RAM || ❌(Note If rule references forbidden variable for specific command evaluation fails access denied.`StoreLoadMore `StoreUnsubscribe inherit authorization `PresenceUnsubscribe require no authorization check `$data` decoded presence field map from `PresenceSet message Fields identified by schema name.status.slide available on presence write commands `StoreSet `$doc` interpreted by write kind$doc candidate document evaluated RAM without storage read existing stored document enforced by predicate lowering document enforced predicate lowering create candidate includes document id `owner_id $session.userId normalized incoming fields server-managed fields/defaults If create rule references `$doc` field absent not server-injected defaulted create denied preserves invariant client create document Predicate Lowering Strategy checks `$doc` variable fetch document RAM before authorization authorization layer lowers condition into storage-neutral `FilterPredicate storage renders predicate into SQL `WHERE` fragment with values server boot `AuthConfig.init validates store rule against active schema Unknown `$doc` fields invalid operators predicate shapes store-query fail startup`$doc` shape expressive than StoreQuery zero AND conditions plus one OR group Nested$doc groups multiple OR groups OR branch invalid.```json
"write": { "$doc.owner_id": { "eq": "$session.userId" } }
```creating document server injects `owner_id from$session.userId treats document as$doc evaluates ownership rule updating deleting Zig evaluates$session.userId internal `BLOB(16) user ID recognizes$doc.owner_id as column reference lowers to filter condition storage renders binary parameter `UPDATE tasks SET id = ? owner_id = If user own document 0 rows affected extra overhead accepted writes perform extra reads classify zero-row outcome Confirmed writes classify report `PERMISSION_DENIED` Zero-Config Default `authorization.json missing ZyncBase boots with implicit "public playground" rule guarantees frictionless developer experience prototyping destroying data JSON```json
{
  "namespaces": [
    {
      "pattern": "public",
      "storeFilter":        true,
      "presenceRead":       true,
      "presenceWrite":      true,
      "presenceSharedWrite": true
    }
  ],
  "store": [
    {
      "collection": "*",
      "read": true,
      "write": { "$doc.owner_id": { "eq": "$session.userId" } }
    }
  ]
}
```default means accessible namespace `public` anonymous users read data broadcast presence in `public` namespace Users create records owned by$session.userId` modify delete records Creates satisfy$doc.owner_id == $session.userId` server-side owner injection updates deletes enforce rule predicate lowering