ZyncBase Authorization Grammar defines JSON grammar `authorization.json Root Structure.json two decoupled arrays| Key | Type | Description |
|:---|:---|:---|
| `namespaces` | `array` | Rules for horizontal partitioning (namespace access) and presence operations. |
| `store` | `array` | Rules for vertical partitioning (table access) and document ownership. |
Namespace Definition namespace governs access presence channels| Key | Type | Description |
|:---|:---|:---|
| `pattern` | `string` | The namespace pattern (e.g., `tenant:{tenant_id}`). Segments enclosed in `{}` are extracted into the `$namespace` context. |
| `storeFilter` | `Condition` | Evaluated on `StoreSetNamespace`. If `false`, the client is rejected from the store namespace. |
| `presenceRead` | `Condition` | Evaluated on `PresenceSetNamespace`, `PresenceSubscribe`, and `PresenceSubscribeShared`. |
| `presenceWrite` | `Condition` | Evaluated on `PresenceSet` and `PresenceRemove`. `$data` contains the incoming field values. |
| `presenceSharedWrite` | `Condition` | Evaluated on `PresenceSetShared`. `$data` contains the incoming field values. Defaults to the same value as `presenceWrite` when omitted. |
Store Definition governs read write access table| Key | Type | Description |
|:---|:---|:---|
| `collection` | `string` | The table name to which these rules apply (e.g., `tasks`), or `*` for a catch-all rule that applies to any collection not matched by an earlier rule. |
| `read` | `Condition` | Evaluated on `StoreQuery` and `StoreSubscribe`. |
| `write` | `Condition` | Evaluated on `StoreSet`, `StoreRemove`, and `StoreBatch`. |
Condition Grammar `Condition **Boolean `true or `false **Logical [Condition or **Comparison "LHS "Operator "RHS" **LHS (Left-Hand Side context variable$session.userId$doc.owner_id **Operator** `eq` `ne `gt `gte `lt` `in `contains` **RHS (Right-Hand Side raw literal (string number boolean array or context variable Variable Scope Evaluation Matrix predictability performance ZyncBase restricts variables during wire command Variables evaluated two execution paths Evaluation** check memory **Predicate Lowering**-row$doc comparisons lowered into `FilterPredicate shape `StoreQuery `StoreSubscribe storage layer owns SQL rendering$session` built authentication exchange validated JWT or SDK anonymous subject load fields `users` for authorization| Wire Command | `$session` | `$namespace` | `$path` (table) | `$value` (payload) | `$data` (presence) | `$doc` |
| :--- | :---: | :---: | :---: | :---: | :---: | :---: |
| `StoreSetNamespace` | ✅ RAM | ✅ RAM | ❌ | ❌ | ❌ | ❌ |
| `PresenceSetNamespace` | ✅ RAM | ✅ RAM | ❌ | ❌ | ❌ | ❌ |
| `StoreQuery` / `StoreSubscribe` | ✅ RAM | ✅ RAM | ✅ RAM | ❌ | ❌ | ✅ Predicate Lowering |
| `StoreSet` | ✅ RAM | ✅ RAM | ✅ RAM | ✅ RAM | ❌ | ✅ Create: RAM candidate / Update: Predicate Lowering |
| `StoreRemove` | ✅ RAM | ✅ RAM | ✅ RAM | ❌ | ❌ | ✅ Predicate Lowering |
| `PresenceSet` | ✅ RAM | ✅ RAM | ❌ | ❌ | ✅ RAM | ❌ |
| `PresenceSetShared` | ✅ RAM | ✅ RAM | ❌ | ❌ | ✅ RAM | ❌ |
| `PresenceSubscribe` | ✅ RAM | ✅ RAM | ❌ | ❌ | ❌ | ❌ |
| `PresenceSubscribeShared` | ✅ RAM | ✅ RAM | ❌ | ❌ | ❌ | ❌ |
rule references forbidden variable command evaluation fails access denied `StoreLoadMore `StoreUnsubscribe` inherit authorization `PresenceUnsubscribe` require no authorization check `$data` decoded presence field map from `PresenceSet message identified by schema name.status available on presence write commands `StoreSet `$doc` write rules interpreted by write kind candidate document evaluated in RAM without storage read existing stored document enforced by predicate lowering document enforced predicate lowering create candidate includes document id `owner_id $session.userId normalized incoming fields server-managed fields/defaults create rule references `$doc` field absent not server-injected or defaulted create denied preserves invariant client create document Predicate Lowering Strategy checks `$doc` variable fetch document RAM before authorization authorization layer lowers condition into storage-neutral `FilterPredicate` renders predicate into SQL `WHERE` fragment with values server boot `AuthConfig.init validates store rule against active schemaUnknown `$doc` fields invalid operators predicate shapes store-query model fail startup supported `$doc` shape expressive than StoreQuery zero AND conditions one OR group Nested `$doc` groups multiple OR groups branch invalid```json
"write": { "$doc.owner_id": { "eq": "$session.userId" } }
```creating document server injects `owner_id from$session.userId treats document as$doc evaluates ownership rule updating deleting Zig evaluates$session.userId internal `BLOB(16) user ID recognizes$doc.owner_id as column reference lowers to filter condition storage renders binary parameter `UPDATE tasks SET id = ? owner_id = If user own document 0 rows affected extra overhead accepted writes perform extra reads classify zero-row outcome Confirmed writes classify report `PERMISSION_DENIED` Zero-Config Default `authorization.json missing ZyncBase boots with implicit "public playground" rule guarantees frictionless developer experience prototyping destroying data JSON```json
{
  "namespaces": [
    {
      "pattern": "public",
      "storeFilter":        true,
      "presenceRead":       true,
      "presenceWrite":      true,
      "presenceSharedWrite": true
    }
  ],
  "store": [
    {
      "collection": "*",
      "read": true,
      "write": { "$doc.owner_id": { "eq": "$session.userId" } }
    }
  ]
}
```default means accessible namespace `public` anonymous users read data broadcast presence in `public` namespace Users create records owned by$session.userId` modify delete records Creates satisfy$doc.owner_id == $session.userId` server-side owner injection updates deletes enforce rule predicate lowering