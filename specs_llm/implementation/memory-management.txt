Memory Management Implementation [Threading Model Architecture-001 Zig Core Language document ZyncBase memory management manual allocation strategies request-scoped lifetimes object pooling ZyncBase avoids global garbage collector tiered allocator strategy memory based lifecycle Memory Long-lived State Connections Allocator Request-scoped data temporary buffers Pools High-frequency reusable buffers Tiered Allocator Strategy uses specialized allocators memory lifetimes minimize fragmentation overhead Server Long-lived allocations.GeneralPurposeAllocator Per-request arena bulk freeing Object pools reusable buffers scoped allocator_arena.ArenaAllocator.init Bulk free request memory allocator processing uses allocator msgdecode(allocator raw Object Pooling high objects ZyncBase uses lock-free object pool avoid allocation churn Pool Self items free_stack std.atomic.Value index top free slot usize_max empty acquire(self top = self.free_stack.load(.acquire); std.math.maxInt(usize next = self.items.next_free free-list index.free_stack.free_stack.load( return.items[top null pool exhausted release(self item *T idx.items @sizeOf top = self.free_stack.load(.acquire); item.next_free = top.free_stack top.free_stack.load( embed `next_free: usize field intrusive free-list link Pool capacity fixed init time `acquire` returns `null exhausted callers handle fall back GPAOperational Logic Allocator Types Use Cases Lifecycle **Arena** Fast single bulk-deallocation end **GPA** Manual `alloc/`free request **Pool** Zero allocation runtime Pre-allocated memory hot objects Memory Safety Practices Unbounded parsers enforce depth size limits OOM attacks GPA-backed state tracked freed disconnect-Fast ZyncBase allocation failure fatal request error `INTERNAL_ERROR Invariants Error Conditions Arena reset `defer GPA-backed state allocation `free disconnect eviction Pool exhaustion `acquire returns `null caller GPA OOM fatal request returns `errorOutOfMemory caller maps_ERROR wire Unbounded allocation parsers enforce_size before allocating allocation refused limit exceeded Validation Success Criteria Metrics Leak-free execution GPA safety checks Fragmentation < 10% load Average arena allocation latency < 5μs Verification Commands GPA leak detection thread sanitizer conditions Confirm no leaks request cycle Model Architecture Parser Implementation