uWebSockets Compatibility Implementation describes C binding interface uWebSockets version pinned compatibility contract Pinned uWebSockets not direct submodule/bun submodule```
vendor/bun/src/deps/libuwsockets.cpp   — C wrapper compiled into ZyncBase
vendor/bun/packages/bun-uws/src/       — uWebSockets C++ headers
vendor/bun/packages/bun-usockets/src/  — µSockets headers
```/bun submodule Bun commit.gitmodules uWebSockets version Bun's tree determine version```bash
git -C vendor/bun log --oneline -1
grep -r "UWS_VERSION" vendor/bun/packages/bun-uws/src/ 2>/dev/null | head -5
```ZyncBase calls uWebSockets `src/uws_wrapper.h included@cImport/uwebsockets_wrapper.zig functions```c
// App lifecycle
uws_app_t* uws_create_app(int ssl, us_bun_socket_context_options_t options);
void       uws_app_listen(int ssl, uws_app_t* app, int port, uws_listen_handler handler, void* user_data);
void       uws_app_run(int ssl, uws_app_t* app);

// WebSocket route registration
void uws_ws(int ssl, uws_app_t* app, void* ctx,
            const char* pattern, size_t pattern_len,
            int id, const uws_socket_behavior_t* behavior);

// WebSocket operations
int  uws_ws_send(int ssl, uws_websocket_t* ws,
                 const char* msg, size_t len, uws_opcode_t opcode);
void uws_ws_close(int ssl, uws_websocket_t* ws);
void* uws_ws_get_user_data(int ssl, uws_websocket_t* ws);
```Behavior Configuration```zig
behavior.compression          = c.UWS_COMPRESS_DISABLED;
behavior.maxPayloadLength     = 10 * 1024 * 1024; // 10 MB
behavior.idleTimeout          = 120;               // seconds
behavior.maxBackpressure      = 64 * 1024;         // 64 KB
behavior.sendPingsAutomatically = true;
```Compatibility Contract ZyncBase guarantees compatibility uWebSockets `vendor/bun commit No other version supported Guarantee functions called uWebSockets change safe `libuwsockets.cpp compiled no pre binary dependency SSL compiled `WebSocketServer.Config.ssl false SSL paths not exercised tests Compression Disabled Enabling.compression re-testing Updating Version update uWebSockets `vendor/bun submodule commit```bash
git -C vendor/bun fetch origin
git -C vendor/bun checkout <new-commit>
git add vendor/bun
zig build test   # must pass before committing
```update changes C interface/uws_wrapper.h/uwebsockets_wrapper.zig match Invariants Error Conditions Single app instance_server `uwebsockets_wrapper.zig one server pointer Multiple instances not supported App not destroyed_app_destroy exposed wrapper lives until process exit SSL not validated certificate paths accepted not tested CI Error.FailedToCreateApp null invalid SSL.ListenFailed Port permission denied Verification Commands```bash
# Build and link uWebSockets from source
zig build

# Run wrapper unit tests
zig test src/uwebsockets_wrapper_test.zig

# Run wrapper property tests
zig test src/uwebsockets_wrapper_property_test.zig
```Implementation Server struct connection lifecycle Implementation Event loop threading model