ZyncBase Server Configuration guide with JSON files Table of Contents [Configuration-First Approach [zyncbase-config.json] [schema.json [Schema Migrations [authorization.json] [Environment Variables 7. [Examples Configuration-First Approach ZyncBase follows"Zero-Zig" don't write server code data validation to security rules defined in declarative JSON files [Zero-Zig Philosophy] configuration files `zyncbase-config.json server settings `schema.json Data validation `authorization.json Security rules zyncbase-config.json configuration file Minimal Configuration```json
{
  "server": {
    "port": 3000
  },
  "schema": "./schema.json",
  "authorization": "./authorization.json",
  "authentication": {
    "jwt": {
      "secret": "${JWT_SECRET}"
    },
    "ticket": {
      "secret": "${TICKET_SECRET}"
    }
  },
  "dataDir": "./data"
}
```Configuration```json
{
  "environment": "development",
  "server": {
    "port": 3000,
    "host": "0.0.0.0"
  },
  
  "schema": "./schema.json",
  
  "authentication": {
    "ticket": {
      "secret": "${TICKET_SECRET}",
      "ttlSeconds": 60,
      "singleUse": true
    },
    "jwt": {
      "secret": "${JWT_SECRET}",
      "algorithm": "HS256",
      "issuer": "your-app",
      "audience": "zyncbase-server"
    },
    "session": {
      "claims": {
        "tenant_id": "tenant_id",
        "role": "role",
        "permissions": "permissions",
        "org_id": "org_id",
        "read_projects": "read_projects",
        "write_projects": "write_projects"
      }
    },
    "anonymous": {
      "enabled": true,
      "subjectPrefix": "anon:"
    }
  },
  
  "dataDir": "./data",
  
  "security": {
    "allowedOrigins": [
      "https://yourdomain.com",
      "https://app.yourdomain.com"
    ],
    "allowLocalhost": true,
    "maxMessagesPerSecond": 100,
    "maxConnections": 100000,
    "maxMessageSize": 1048576,
    "violationThreshold": 10
  },
  
  "logging": {
    "level": "info",
    "format": "json"
  },
  
  "performance": {
    "messageBufferSize": 1000,
    "batchWrites": true,
    "batchSize": 200,
    "batchTimeout": 10,
    "statementCacheSize": 100
  }
}
```Configuration Reference network configuration```json
{
  "server": {
    "port": 3000,              // Port to listen on
    "host": "0.0.0.0"          // Host to bind to
  }
}
````schema Path JSON file migration settings If omitted file missing server boots users-only schema schema invalid startup fails```json
{
  "schema": {
    "file": "./schema.json",
    "version": "1.0.0",
    "autoMigrate": true,
    "allowDestructive": false
  }
}
```simple string```json
{
  "schema": "./schema.json"
}
```**Migration `autoMigrate `true Auto-migrate all changes"additive-only auto-migrate additive changes `false Require migrations `allowDestructive `true Allow destructive changes with confirmation `false` Block destructive changes `version Semantic version schema Major changes require migrations Minor/patch versions auto-migrate `authorization.json file If omitted missing server boots safe "public playground default```json
{
  "authorization": "./authorization.json"
}
````authentication ticket settings ZyncBase validates identity issues short-lived tickets WebSocket manage accounts compute permissions```json
{
  "authentication": {
    "ticket": {
      "secret": "${TICKET_SECRET}",
      "ttlSeconds": 60,
      "singleUse": true
    },
    "jwt": {
      "secret": "${JWT_SECRET}",
      "algorithm": "HS256",
      "issuer": "your-app",
      "audience": "zyncbase-server",
      "subjectClaim": "sub"
    },
    "session": {
      "claims": {
        "tenant_id": "tenant_id",
        "role": "role",
        "permissions": "permissions",
        "org_id": "org_id",
        "read_projects": "read_projects",
        "write_projects": "write_projects"
      },
      "tokenGracePeriodSeconds": 30
    },
    "anonymous": {
      "enabled": true,
      "subjectPrefix": "anon:"
    }
  }
}
```Settings `secret Signing ZyncBase tickets omitted ephemeral secret invalidates tickets restart `ttlSeconds Ticket lifetime before WebSocket upgrade Tickets short-lived WebSocket URL `singleUse` ticket redeemed once enabled Settings `secret Shared secret JWT algorithms `jwksUrl public keys asymmetric JWT algorithms `algorithm Expected JWT algorithm `issuer `iss claim `audience `aud claim `subjectClaim external identity string `sub` Claim Projection `session.claims maps$session names verified JWT claim names permission inputs ZyncBase `users` row not loaded$session authorization reserved `users` collection maps external subject internal `users.id` `owner_id presence identity profile/display data `session.tokenGracePeriodSeconds Grace period after JWT expiry before connection SDK tokens before expiry safety slow refreshes Default 30 seconds **Anonymous Auth.enabled SDK request ticket high-entropy client-generated anonymous subject Anonymous subjects use same `users.id mapping authorization flow authenticated [README API overview [Store API Reference-api.md use store configured API Reference-api.md presence configuration context `dataDir Directory SQLite database data files```json
{
  "dataDir": "./data"
}
````security settings```json
{
  "security": {
    "allowedOrigins": [
      "https://yourdomain.com"
    ],
    "allowLocalhost": true,
    "maxMessagesPerSecond": 100,
    "maxConnections": 100000,
    "maxMessageSize": 1048576,
    "violationThreshold": 10
  }
}
````logging configuration```json
{
  "logging": {
    "level": "info",    // debug, info, warn, error
    "format": "json"    // json, text
  }
}
````performance tuning```json
{
  "performance": {
    "messageBufferSize": 1000,
    "batchWrites": true,
    "batchSize": 200,
    "batchTimeout": 10,
    "statementCacheSize": 100
  }
}
```schema.json data structure ZyncBase schema format two keys `store persistent relational data `presence ephemeral presence state **Naming Restriction** Field names double underscore sequence reserved system field names `__` sequence reserved internal flattening nested objects Invalid names `error.InvalidFieldName Presence Schema optional `presence key defines ephemeral presence fields definition authoritative rejects messages unknown field indices mismatched types no schemaless fallback Two tiers supported| Key | Description |
|---|---|
| `presence.user` | Fields owned per connected user. One record per user per namespace. Cleaned up on disconnect. |
| `presence.shared` | Fields representing namespace-level state. One record for the entire namespace. Persists until all users leave (5-second grace period). |
Presence field constraints Arbitrary depth object nesting supported x y limit 500 flat fields enforced per presence tier server startup Nested objects flattened using `__ SDK handles Presence fields required tiers use field-level merge semantics `string `number `integer `boolean standard constraints apply`enum `maximum `minLength `maxLength`presence.shared Omit no room-level shared state Example Collaborative Editor Shared Slide```json
{
  "version": "1.0.0",
  "store": {
    "documents": {
      "fields": {
        "title": { "type": "string" }
      }
    }
  },
  "presence": {
    "user": {
      "cursor": {
        "type": "object",
        "fields": {
          "x": { "type": "number" },
          "y": { "type": "number" }
        }
      },
      "status": { "type": "string", "enum": ["active", "idle", "away"] },
      "typing": { "type": "boolean" },
      "name":   { "type": "string", "maxLength": 64 },
      "color":  { "type": "string", "pattern": "^#[0-9a-fA-F]{6}$" }
    },
    "shared": {
      "slide":   { "type": "integer", "minimum": 0 },
      "playing": { "type": "boolean" }
    }
  }
}
```Presence schema SchemaSync server loads schema section derives two flat arrays flattening fields```
presenceUserFields:   ["cursor__x", "cursor__y", "status", "typing", "name"]
presenceSharedFields: ["slide", "playing"]
```included in `SchemaSync message client SDK builds index maps arrays encode presence messages integer at position N wire key field Presence authorization rules defined in `authorization.json store rules```json
{
  "namespaces": [
    {
      "pattern": "workspace:*",
      "storeFilter":        true,
      "presenceRead":        { "$session.userId": { "ne": null } },
      "presenceWrite":       { "$session.userId": { "ne": null } },
      "presenceSharedWrite": { "$session.role":   { "in": ["host", "presenter"] } }
    }
  ],
  "store": []
}
```

| Rule | Applies to |
|---|---|
| `presenceRead` | `presence.subscribe()` and `presence.subscribeShared()` |
| `presenceWrite` | `presence.set()` and `presence.remove()` |
| `presenceSharedWrite` | `presence.setShared()` — defaults to same value as `presenceWrite` when omitted |
$data` variable available in `presenceWrite rules exposes incoming field values See [Presence API Reference for usage details **Naming Restriction** Field names forbidden double underscore sequence reserved system field names `__` sequence reserved for internal flattening nested objects Invalid names with `error.InvalidFieldName` Example Collaborative Canvas```json
{
  "version": "1.0.0",
  "store": {
    "elements": {
      "fields": {
        "type": { "type": "string", "enum": ["rect", "circle", "text"] },
        "x": { "type": "number" },
        "y": { "type": "number" },
        "width": { "type": "number" },
        "height": { "type": "number" },
        "color": { "type": "string", "pattern": "^#[0-9a-fA-F]{6}$" }
      },
      "required": ["type", "x", "y", "width", "height"]
    }
  }
}
```Multi-tenant Projects```json
{
  "version": "1.0.0",
  "store": {
    "projects": {
      "fields": {
        "name": { "type": "string", "minLength": 1, "maxLength": 100 },
        "status": { "type": "string", "enum": ["active", "archived", "deleted"] },
        "createdBy": { "type": "string" }
      },
      "required": ["name", "status", "createdBy"]
    },
    "tasks": {
      "fields": {
        "title": { "type": "string", "minLength": 1 },
        "description": { "type": "string" },
        "status": { "type": "string", "enum": ["todo", "in_progress", "done"] },
        "projectId": {
          "type": "string",
          "references": "projects"
        },
        "assignedTo": { "type": "string" }
      },
      "required": ["title", "status", "projectId"]
    }
  }
}
```Global Master Data false```json
{
  "version": "1.0.0",
  "store": {
    "pricing_tiers": {
      "namespaced": false,
      "fields": {
        "tier_name": { "type": "string" },
        "monthly_price": { "type": "number" },
        "features": { "type": "array", "items": "string" }
      }
    }
  }
}
```Reserved `users` Collection hybrid table managed by ZyncBase defaults to"namespaced false `external_id column maps external identity string JWT internal `BLOB(16) UUIDv7 `owner_id$session.userId `userId foreign keys extend with optional custom fields```json
{
  "version": "1.0.0",
  "store": {
    "users": {
      "namespaced": false,
      "fields": {
        "name": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "preferences": {
          "type": "object",
          "fields": {
            "notifications": {
              "type": "object",
              "fields": {
                "email": { "type": "boolean" },
                "browser": { "type": "boolean" }
              }
            },
            "theme": { "type": "string" }
          }
        },
        "roles": {
          "type": "array",
          "items": "string"
        }
      }
    }
  }
}
```Custom fields `users `required server auto-creates identity rows session needs internal user ID before application profile data If `users.namespaced = true external identity different user IDs namespaces ZyncBase generates flattens nested objects into column names double underscore separator allows SQL queries document structure```sql
CREATE TABLE users (
    id BLOB NOT NULL CHECK(length(id) = 16),
    namespace_id INTEGER NOT NULL, -- 0 for the default global users collection
    owner_id BLOB NOT NULL CHECK(length(owner_id) = 16), -- equal to id for users
    external_id TEXT NOT NULL,
    name TEXT NOT NULL,
    email TEXT NOT NULL,
    preferences__notifications__email INTEGER, -- Boolean stored as int
    preferences__notifications__browser INTEGER,
    preferences__theme TEXT,
    roles BLOB,  -- canonical array storage
    created_at INTEGER NOT NULL,
    updated_at INTEGER NOT NULL,
    PRIMARY KEY (id)
);

CREATE UNIQUE INDEX idx_users_namespace_external_id ON users(namespace_id, external_id);
````id` primary key unique collection/table `namespace_id` visibility identity-provider lookup permit duplicate document IDs namespaces Schema Structure ZyncBase schema format Define data store structure```json
{
  "version": "1.0.0",
  "store": {
    "tasks": {
      "fields": {
        "title": { "type": "string" },
        "status": { "type": "string" },
        "priority": { "type": "integer" }
      }
    }
  }
}
```Nested Fields Flattening ZyncBase flattens objects querying```json
{
  "store": {
    "users": {
      "fields": {
        "name": { "type": "string" },
        "address": {
          "type": "object",
          "fields": {
            "street": { "type": "string" },
            "city": { "type": "string" },
            "zipCode": { "type": "string" }
          }
        }
      }
    }
  }
}
```API nested objects work```typescript
// Set nested fields (at any depth)
await zyncbase.set('users.user-1', {
  name: 'Alice',
  address: {
    street: '123 Main St',
    location: {
      lat: 37.7749,
      lng: -122.4194
    }
  }
})

// Field-level updates also support recursion
await zyncbase.set(['users', 'user-1', 'address', 'location', 'lat'], 37.8)

// Query nested fields using dot notation
const users = await zyncbase.query('users', {
  where: { 'address.location.lat': { gte: 37 } }
})
```Nested fields flattened to columns double underscore separator Base field names `__` collisions server reconstructs nested structure on `get `query operations string identifiers replaced by numeric index mappings Efficient querying indexes on flattened columns Unlimited nesting depth for `store fields column limit ~2000 Clean client-side experience manual flattening **Limitations Nested objects contain arrays at leaf level recursion depth limited by maximum SQLite columns 2000 Field names contain `__` flattening separator `presence` fields support **arbitrary nesting depth** to 500 flat fields per tier ZyncBase supports with constraints Simple arrays```json
{
  "store": {
    "tasks": {
      "fields": {
        "tags": {
          "type": "array",
          "items": "string"
        }
      }
    }
  }
}
```

**Client API:**
```typescript
await zyncbase.set(tasks.task-1', {
  tags: ["urgent", "backend"]
})
```array behavior sorted Elements match schema `items type `null nested arrays objects Arrays normalized sorted unique form write Reads query equality observe unique representation Arrays objects```json
{
  "store": {
    "projects": {
      "fields": {
        "members": {
          "type": "array",
          "items": {
            "type": "object",  // ❌ Not allowed
            "fields": {
              "userId": { "type": "string" },
              "role": { "type": "string" }
            }
          }
        }
      }
    }
  }
}
```separate store path references```json
{
  "store": {
    "projects": {
      "fields": {
        "name": { "type": "string" }
      }
    },
    "project_members": {
      "fields": {
        "projectId": {
          "type": "string",
          "references": "projects"
        },
        "userId": { "type": "string" },
        "role": { "type": "string" }
      }
    }
  }
}
```

**Client API:**
```typescript
// Create project
await zyncbase.set(projects.proj-1', { name: 'My Project' })

// Add members
await zyncbase.set(project_members.member-1', {
  projectId: 'proj-1',
  userId: 'user-1',
  role: 'admin'
})

// Query members
const members = await zyncbase.query(project_members', {
  where: { projectId: 'proj-1' }
})
```References ZyncBase supports references relational data```json
{
  "store": {
    "projects": {
      "fields": {
        "name": { "type": "string" }
      }
    },
    "tasks": {
      "fields": {
        "title": { "type": "string" },
        "projectId": {
          "type": "string",
          "references": "projects",
          "onDelete": "cascade"
        }
      }
    }
  }
}
```

**Client API:**
```typescript
// Create project
await zyncbase.set(projects.proj-1', { name: 'My Project' })

// Create task that references project
await zyncbase.set(tasks.task-1', {
  title: 'Build feature',
  projectId: 'proj-1'
})

// Delete project (cascades to tasks)
await zyncbase.remove(projects.proj-1')
// task-1 is automatically deleted
```**Reference Options `onDelete "cascade Delete tasks deleted "restrict Prevent deletion tasks "set_null Set projectId null deleted **Benefits Data integrity Cascading deletes Efficient queries Frontend uses IDs ZyncBase handles JSON Schema Tips `enum` for fixed values```json
{
  "status": { "type": "string", "enum": ["active", "pending", "deleted"] }
}
````format validation```json
{
  "email": { "type": "string", "format": "email" },
  "url": { "type": "string", "format": "uri" },
  "date": { "type": "string", "format": "date-time" }
}
```regex validation```json
{
  "color": { "type": "string", "pattern": "^#[0-9a-fA-F]{6}$" }
}
```**Use`maxLength` strings```json
{
  "name": { "type": "string", "minLength": 1, "maxLength": 100 }
}
````minimum/`maximum numbers```json
{
  "priority": { "type": "integer", "minimum": 1, "maximum": 5 }
}
```Schema Migrations ZyncBase generates SQLite tables schema.json handles migrations environment **Path-to-Table Mapping```
Path: 'tasks' → Table: tasks
Path: 'tasks.task-1' → Table: tasks, row with id='task-1'
Path: 'users' → Table: users
```DDL schema```json
{
  "store": {
    "tasks": {
      "fields": {
        "title": { "type": "string" },
        "status": { "type": "string" },
        "priority": { "type": "integer" }
      }
    }
  }
}
```

ZyncBase generates:
```sql
CREATE TABLE tasks (
    id BLOB NOT NULL CHECK(length(id) = 16),
    namespace_id INTEGER NOT NULL,
    owner_id BLOB NOT NULL CHECK(length(owner_id) = 16),
    title TEXT,
    status TEXT,
    priority INTEGER,
    created_at INTEGER NOT NULL,
    updated_at INTEGER NOT NULL,
    PRIMARY KEY (id)
);

CREATE INDEX idx_tasks_status ON tasks(status);
CREATE INDEX idx_tasks_namespace ON tasks(namespace_id);
```primary key `id not Namespace-aware tables require document IDs references cursors caches SDK single-key Auto-Migration changes auto-migrate zero friction```json
// Initial schema.json
{
  "store": {
    "tasks": {
      "fields": {
        "title": { "type": "string" },
        "status": { "type": "string" }
      }
    }
  }
}

// Updated schema.json - add a field
{
  "store": {
    "tasks": {
      "fields": {
        "title": { "type": "string" },
        "status": { "type": "string" },
        "assignee": { "type": "string" }  // ← New field
      }
    }
  }
}
```

**What happens:**
```bash
$ zyncbase-server start
✓ Schema change detected
✓ Adding column 'tasks.assignee'
✓ Creating index on 'tasks.assignee'
✓ Server started (2ms)
```migration friction Development vs Production Mode (Fast```json
// zyncbase-config.json
{
  "environment": "development",
  "schema": {
    "file": "./schema.json",
    "autoMigrate": true,
    "allowDestructive": true
  }
}
```

Behavior:
```bash
# Change field type
$ vim schema.json  # priority: integer → string

$ zyncbase-server start
⚠ Destructive schema change detected
⚠ Field 'tasks.priority' type changed: integer → string
⚠ This will DROP and recreate the table (data loss!)

Continue? [y/N]: y

✓ Dropping table 'tasks'
✓ Creating table 'tasks' with new schema
✓ Server started

⚠ All data in 'tasks' was lost
```Mode (Safety```json
// zyncbase-config.json
{
  "environment": "production",
  "schema": {
    "file": "./schema.json",
    "autoMigrate": "additive-only",
    "allowDestructive": false
  }
}
```

Behavior:
```bash
# Same change in production
$ zyncbase-server start
✗ Cannot start: destructive schema change detected
✗ Field 'tasks.priority' type changed: integer → string

This requires a manual migration.

Options:
1. Revert schema.json to previous version
2. Create migration: zyncbase migrate create change_priority_type
3. Force (data loss): zyncbase-server start --force-schema

See: https://zyncbase.dev/docs/MIGRATIONS.md
```Schema Versioning semantic track changes```json
{
  "version": "1.2.0",
  "properties": {
    "tasks": { ... }
  }
}
```Rules (1.2.0 Additive auto-migrate (1.2.0 1.3.0 Additive-migrate (1.2.0 2.0.0 Breaking migration```bash
# Patch/Minor - auto-migrates
$ zyncbase-server start
✓ Schema version: 1.2.0 → 1.3.0 (minor)
✓ Auto-migrating additive changes
✓ Server started

# Major - requires explicit migration
$ zyncbase-server start
✗ Schema version: 1.3.0 → 2.0.0 (major)
✗ Breaking changes require migration

Create migration:
  zyncbase migrate create v2_breaking_changes

Or force in development:
  zyncbase-server start --force-schema
```Auto-Migrates **Always safe all environments):** Add new field new table (path) index Make field optional **Requires migration Change field type (integer → string Remove field Rename field Make field required Migration Commands```bash
# Check migration status
zyncbase migrate status

# Create new migration
zyncbase migrate create add_assignee_field

# Apply migrations
zyncbase migrate up

# Rollback last migration
zyncbase migrate down

# Dry run (see what would happen)
zyncbase migrate up --dry-run
```Environment| Change Type | Development | Production |
|-------------|-------------|------------|
| Add field | ✓ Auto-migrate | ✓ Auto-migrate |
| Add table | ✓ Auto-migrate | ✓ Auto-migrate |
| Change type | ⚠ Allow with confirm | ✗ Require migration |
| Remove field | ⚠ Allow with confirm | ✗ Require migration |
| Major version | ⚠ Allow with confirm | ✗ Require migration |
Quick Start Guide **Adding field```bash
# 1. Edit schema
$ vim schema.json  # Add 'assignee' field

# 2. Start server
$ zyncbase-server start
✓ Auto-migrated: added column 'tasks.assignee'
```**Changing type```bash
# 1. Edit schema
$ vim schema.json  # priority: integer → string

# 2. Start server
$ zyncbase-server start --dev
⚠ Destructive change: field type changed
⚠ Data in 'tasks' will be lost
Continue? [y/N]: y
✓ Table recreated

# 3. Rebuild test data
$ node scripts/seed-dev-data.js
```**Changing type```bash
# 1. Edit schema and bump version
$ vim schema.json  # version: "1.3.0" → "2.0.0"

# 2. Create migration
$ zyncbase migrate create v2_change_priority_type

# 3. Write migration
$ vim migrations/003_v2_change_priority_type.sql

# 4. Apply migration
$ zyncbase migrate up

# 5. Deploy
$ zyncbase-server start
✓ Migrations applied
✓ Server started
```migration guides ZyncBase documentation.md removed aspirational authorization.json Define rules declarative JSON condition grammar [Auth Grammar/auth-grammar `authorization.json omitted missing server boots safe playground rules Simple Rules```json
{
  "namespaces": [
    {
      "pattern": "tenant:{tenant_id}",
      "storeFilter": { "$session.tenant_id": { "eq": "$namespace.tenant_id" } },
      "presenceRead": { "$session.tenant_id": { "eq": "$namespace.tenant_id" } },
      "presenceWrite": { "$session.role": { "in": ["admin", "editor"] } }
    }
  ],
  "store": [
    {
      "collection": "tasks",
      "read": true,
      "write": {
        "or": [
          { "$session.role": { "eq": "admin" } },
          { "$doc.owner_id": { "eq": "$session.userId" } }
        ]
      }
    },
    {
      "collection": "audit_logs",
      "read": { "$session.role": { "eq": "admin" } },
      "write": false
    }
  ]
}
```Condition Grammar variables$session. Resolved session context validated JWT anonymous identity.userId_id.role$namespace. Parsed namespace parts_id_id$path` Target table/collection name$value. Incoming mutation payload writes$doc. Same-row SQLite columns injected SQL reads updates/removes **Operators `eq` `ne` Equality `in` `notIn` Set membership `contains` Array/string containment `and` `or` Explicit logical composition object fields implicit```json
{
  "namespaces": [
    {
      "pattern": "public",
      "storeFilter": true,
      "presenceRead": true,
      "presenceWrite": true
    }
  ],
  "store": [
    {
      "collection": "*",
      "read": true,
      "write": { "$doc.owner_id": { "eq": "$session.userId" } }
    },
    {
      "collection": "admin_notes",
      "read": { "$session.role": { "eq": "admin" } },
      "write": { "$session.role": { "eq": "admin" } }
    }
  ]
}
```Permission Claims computation before ZyncBase request identity encodes-manageable grants into JWT claims `authorization.json enforces claims claims```json
{
  "sub": "user_123",
  "org_id": "acme",
  "role": "editor",
  "read_projects": ["docs", "wiki", "planning"],
  "write_projects": ["docs"]
}
```.json```json
{
  "namespaces": [
    {
      "pattern": "org:{org_id}:project:{project_id}",
      "storeFilter": {
        "and": [
          { "$namespace.org_id": { "eq": "$session.org_id" } },
          { "$namespace.project_id": { "in": "$session.read_projects" } }
        ]
      },
      "presenceRead": { "$namespace.project_id": { "in": "$session.read_projects" } },
      "presenceWrite": { "$namespace.project_id": { "in": "$session.write_projects" } }
    }
  ],
  "store": [
    {
      "collection": "pages",
      "read": { "$doc.project_id": { "in": "$session.read_projects" } },
      "write": { "$doc.project_id": { "in": "$session.write_projects" } }
    }
  ]
}
```Rules database joins membership-table lookups billing checks API calls outside ZyncBase authorization Applications represent decisions trusted token claims same-row data before supports variable substitution_NAME syntax.env```bash
# Server
PORT=3000
HOST=0.0.0.0

# Authentication
JWT_SECRET=your-secret-key-here
TICKET_SECRET=your-ticket-signing-secret-here

# Database
DATA_DIR=./data

# Security
ALLOWED_ORIGINS=https://yourdomain.com,https://app.yourdomain.com
```

### Using in config

```json
{
  "server": {
    "port": "${PORT}",
    "host": "${HOST}"
  },
  "authentication": {
    "ticket": {
      "secret": "${TICKET_SECRET}"
    },
    "jwt": {
      "secret": "${JWT_SECRET}"
    }
  },
  "dataDir": "${DATA_DIR}",
  "security": {
    "allowedOrigins": "${ALLOWED_ORIGINS}"
  }
}
```Examples 1: Collaborative Whiteboard-config.json```json
{
  "server": { "port": 3000 },
  "schema": "./schema.json",
  "authentication": {
    "ticket": { "secret": "${TICKET_SECRET}" },
    "jwt": { "secret": "${JWT_SECRET}" },
    "anonymous": { "enabled": true }
  },
  "dataDir": "./data",
  "namespaces": {
    "patterns": [{ "pattern": "room:*" }]
  }
}
```

**schema.json:**
```json
{
  "store": {
    "elements": {
      "fields": {
        "x": { "type": "number" },
        "y": { "type": "number" },
        "width": { "type": "number" },
        "height": { "type": "number" }
      }
    }
  }
}
```.json```json
{
  "namespaces": [
    {
      "pattern": "room:{room_id}",
      "storeFilter": { "$session.userId": { "ne": null } },
      "presenceRead": true,
      "presenceWrite": { "$session.userId": { "ne": null } }
    }
  ],
  "store": [
    {
      "collection": "elements",
      "read": true,
      "write": { "$session.userId": { "ne": null } }
    }
  ]
}
```2: Multi-tenant SaaS```json
{
  "server": { "port": 3000 },
  "schema": "./schema.json",
  "authentication": {
    "ticket": { "secret": "${TICKET_SECRET}" },
    "jwt": { "secret": "${JWT_SECRET}" }
  },
  "dataDir": "./data",
  "namespaces": {
    "patterns": [{ "pattern": "tenant:*" }]
  }
}
```.json```json
{
  "namespaces": [
    {
      "pattern": "tenant:{tenant_id}",
      "storeFilter": { "$session.tenant_id": { "eq": "$namespace.tenant_id" } },
      "presenceRead": { "$session.tenant_id": { "eq": "$namespace.tenant_id" } },
      "presenceWrite": { "$session.role": { "in": ["admin", "editor"] } }
    }
  ],
  "store": [
    {
      "collection": "*",
      "read": true,
      "write": { "$session.role": { "in": ["admin", "editor"] } }
    }
  ]
}
```Example 3: Project Grants JWT application Jira Confluence projects identity provider issue grants JWT```json
{
  "sub": "user_123",
  "org_id": "acme",
  "read_projects": ["docs", "wiki", "planning"],
  "write_projects": ["docs"]
}
```ZyncBase grants without```json
{
  "namespaces": [
    {
      "pattern": "org:{org_id}:project:{project_id}",
      "storeFilter": {
        "and": [
          { "$namespace.org_id": { "eq": "$session.org_id" } },
          { "$namespace.project_id": { "in": "$session.read_projects" } }
        ]
      },
      "presenceRead": { "$namespace.project_id": { "in": "$session.read_projects" } },
      "presenceWrite": { "$namespace.project_id": { "in": "$session.write_projects" } }
    }
  ],
  "store": [
    {
      "collection": "pages",
      "read": { "$doc.project_id": { "in": "$session.read_projects" } },
      "write": { "$doc.project_id": { "in": "$session.write_projects" } }
    }
  ]
}
```checks joins ACL traversal billing state external API calls application compute result before JWT Server restart changes No automatic hot reload```

**What triggers reload:**
- `zyncbase-config.json` changes (requires restart)
- `schema.json` changes (requires restart)
- `authorization.json` changes (requires restart)

**What doesn't trigger reload:**
- Environment variable changes (requires restart)
- Binary updates (requires restart)

---

## Validation

ZyncBase evaluates all config files on startup and provides clear error messages:

```bash/zyncbase-server Error Invalid configuration zyncbase-config.json Line 12 Missing field "schema" Add/schema.json config Next Steps API-api.md Learn data sync API Deploy production content [Examples/zyncbase/examples complete examples