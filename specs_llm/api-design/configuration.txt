ZyncBase Server Configuration 2026-03-09 guide with JSON files Table of Contents [Configuration-First Approach [zyncbase-config.json [schema.json Migrations [authorization.json [Environment Variables 7. [Examples ZyncBase follows"Zero-Zig" don write server code data validation to security rules defined in declarative JSON files details [Zero-Zig Philosophy configuration files `zyncbase-config.json server settings `schema.json Data validation `authorization Security rules zyncbase-config.json server configuration Minimal Configuration 3000 "schema "authorization "authentication "dataDir Complete Configuration 3000 "maxConnections 100000 "schema"authentication "jwt${JWT_SECRET "algorithm "issuer "your "zyncbase-server "dataDir. "namespaces "patterns "Default public: "Collaborative rooms-isolated data "allowedOrigins "https://yourdomain "allowLocalhost true "rateLimit "messagesPerSecond 100 "connectionsPerIP 10 "maxMessageSize 1048576 "logging "json "performance "messageBufferSize 1000 "batchWrites true "batchTimeout 10 Configuration Reference `server network configuration 3000 "0.0 "maxConnections 100000 concurrent connections `schema JSON Schema file migration settings "schema./schema.json", "1.0.0", "autoMigrate true "allowDestructive false simple string format "schema./schema.**Migration `autoMigrate `true Auto-migrate all changes"additive-only auto-migrate additive changes `false Require explicit migrations `allowDestructive `true Allow destructive changes with confirmation `false` Block destructive changes `version Semantic version schema Major changes require migrations Minor/patch versions auto-migrate `authorization` `authorization.json file rules "authorization [README] API overview entry [Store API Reference [Presence API Reference configuration `dataDir` Directory SQLite database data files `namespaces pattern definitions "Collaborative rooms `security Security settings "allowedOrigins "allowLocalhost true "rateLimit "messagesPerSecond 100 "connectionsPerIP 10 "maxMessageSize 1048576 `logging configuration`json "logging debug warn error "format "json text `performance Performance tuning "performance "messageBufferSize 1000 "batchWrites true "batchTimeout 10 schema.json Define data structure ZyncBase store schema format Example Collaborative Canvas "version "1.0.0", "elements "fields "type "enum "x "number "y "width "height "color "pattern^#[0-9a-fA-F]{6}$ "required "height Example Multi-tenant Projects Relations "version "1."store" "projects" "fields "name" "minLength 1 "maxLength 100 "status""active", "archived", "deleted "createdBy "string "required "tasks" "fields "title "minLength 1 "description "string" "status""todo", "in_progress", "done "projectId" "references "projects "assignedTo" "string" "required"title", "projectId" Example Nested Fields Simple Arrays "version "1."store" "users "fields "name" "string" "email" "format "address" "properties "street "string "city" "string "zipCode" "string "roles" "array", "items "string" "required""name", "email" ZyncBase generates`sql CREATE TABLE users id_id name email address_street_city_zipCode roles JSON"admin", "editor created updated INTEGER Schema Structure ZyncBase uses **store-based** schema format Define data store structure "version "1."store "tasks "fields "status "priority "integer Nested Fields (Automatic Flattening ZyncBase flattens nested objects efficient querying "store "users "fields "name "address "properties "street "city "zipCode "string **Client API (nested objects work Set nested field zyncbase.set(users name 'Alice address street '123 Main St city 'San Francisco zipCode '94102 Query nested field users zyncbase.query(users 'addresscity 'San Francisco Nested fields flattened to columns `address_street_city_zipCode query efficiently Frontend **Limitations one level nesting Nested objects contain arrays deeper nesting use separate store paths references ZyncBase supports arrays with constraints Simple arrays`json "store "tasks "fields "tags "items "string **Client API`typescript await zyncbase.set(tasks.task-1 tags "backend Arrays of objects (not "store "projects "fields "members "items Not allowed "properties "userId "string "role "string use separate store path with references`json "store "projects "fields "string "project_members "projectId "references "projects "userId "role "string **Client API`typescript Create project await zyncbase.set(projects.proj-1 'My Project Add members zyncbase.set(project_members projectId userId role 'admin Query members zyncbase.query(project_members projectId 'proj-1' References ZyncBase supports references between paths relational data "store "projects "tasks "projectId "references "onDelete "cascade **Client API Create project zyncbase.set(projects.proj-1 'My Project' Create task project zyncbase.set(tasks.task-1 title 'Build feature projectId 'proj-1 Delete project zyncbase.remove(projectsproj-1 task-1 deleted **Reference Options `onDelete "cascade Delete tasks project deleted `onDelete "restrict Prevent project deletion if tasks "set_null Set projectId null deleted **Benefits Data integrity enforced Cascading deletes work Efficient queries across paths Frontend uses IDs ZyncBase handles rest JSON Schema Tips `enum` for fixed values "status" "deleted `format` for validation "email" "url "date" `pattern` for regex validation "color" `minLength/`maxLength` for strings 1 100 `minimum`/`maximum` for numbers "priority" 1 5 Schema Migrations ZyncBase generates SQLite tables from schema.json handles migrations environment**Path-to-Table Mapping Path 'tasks' → Table 'tasks.task-1' → Table row id='task-1 'users' → Table users **Generated DDL schema "store "tasks "fields "title "status "priority ZyncBase generates CREATE TABLE tasks id_id title status priority created updated CREATE INDEX_tasks_status ON tasks_tasks_namespace tasks Auto-Migration **Additive changes auto-migrate zero friction schema.json v1 "store "tasks "title "status schema.json v2 add field "store "tasks "status "assignee New field zyncbase-server start Schema change detected Adding column 'tasks.assignee' Creating index 'tasks.assignee Server started (2ms migration file needed No frictionDevelopment vs Production Mode zyncbase-config.json/schema "autoMigrate "allowDestructive Change field type schema.json priority integer → string zyncbase-server start Destructive schema change detected 'tasks.priority changed integer → string DROP recreate table Dropping table Creating new schema Server started data lost Mode (Safety zyncbase-config.json "autoMigrate "allowDestructive false change production zyncbase-server start destructive schema change 'tasks.priority changed integer → string requires manual migration Revert schema.json previous version Create migration change_priority_type Force zyncbase-server start-schema https://zyncbase.dev/MIGRATIONS.md Schema Versioning semantic versioning track changes Rules (1.2.0 → Additive only auto-migrate (12.0 → 1.3.0 Additive auto-migrate (1.2.0 → 2.0.0 Breaking changes requires migration Patch/Minor auto-migrates zyncbase-server start Schema version 1.2.0 → 1.3.0 (minor Auto-migrating additive changes Server Major requires migration zyncbase-server start Schema version 1.3.0 → 2.0.0 (major Breaking changes require migration Create migration_breaking_changes development start Auto-Migrates Add field table index Make field optional migration Change field type Remove field Rename field Make field required Migration Commands Check migration status Create migration_field Apply migrations Rollback migration Dry run Environment-Specific Behavior Change Type Development Production Add field-migrate Add table Change type Require migration Remove field Require migration Major version migration Quick Start Guide field Edit schemaAdd 'assignee field Start server zyncbase-server start Auto-migrated added column 'tasks.assignee type Edit schema priority integer → string Start server zyncbase-server Destructive change field type changed Data 'tasks lost Table recreated Rebuild test data node scripts/seed-dev-data.js type Edit schema bump version "1.3.0" → "2.0.0" Create migration zyncbase v2_change_priority_type Write migration vim migrations_v2_change_priority_type.sql Apply migration zyncbase Deploy zyncbase-server start Migrations applied Server started detailed migration guides ZyncBase documentation (MIGRATIONS.md removed authorization.json Define authorization rules simple expression language Rules:.userId.tenantIdrole 'admin "isRoomMember "SELECT 1 FROM room_members user_id $1 room_id = "params"userId", "roomId Expression Language variables `jwt. Claims JWT token.role `namespace. Parsed parts.tenantId.roomId `operation operation performed `=== Equality&&`|` Logical AND/OR `in Check value array `!` Logical **Examples "rules "public: "write "jwt.userId null "private: "isRoomMember.userId.roomId "write "isRoomMember.userId.roomId jwt.role Hook Server Functions Complex authorization logic TypeScript functions `zyncbase.auth.ts not file Hook Server managed by ZyncBase CLI **zyncbase.auth.import createAdminClient@zyncbase/server configured local Zig core client createAdminClient Function names match "hook rules authorization async function isRoomMember( session path roomId =.split same Query API frontend memberships await client.store.query_members userId roomId return memberships.length > 0 async function( session permissions client.store.query userId return permissions.length > 0 **authorization.json "rules CLI spins Bun Hook Server manages IPC/WebSocket connection ZyncBase supports environment variable substitution${VAR_NAME syntax.env file Server PORT=3000 HOST=0.0.0.0 Authentication JWT_SECRET-secret-key Database DATA_DIR/data Security ALLOWED_ORIGINS=https://yourdomain.com.yourdomain"server "port${PORT "host{HOST "auth "jwt "secret{JWT_SECRET} "dataDir{DATA_DIR "security "allowedOrigins{ALLOWED_ORIGINS} Examples 1: Collaborative Whiteboard **zyncbase-config.json "server "port 3000 "schema/schema.json", "auth "jwt "secret{JWT_SECRET} "dataDir "namespaces "patterns "room:*" **schema.json "store "elements "fields "x "y "width "height **authorization.json "rules" "namespace "room: "allow" "read "jwt.userId", "write "jwt.userId Example 2: Multi-tenant SaaS-config.json "server "port 3000 "schema/schema.json", "auth "jwt "secret{JWT_SECRET} "dataDir""namespaces "patterns:* **authorization.json "rules:.tenantId "write.tenantId.tenantId.role Example 3: Complex Authorization with Hook Server `authorization.json rules database use Hook Server custom logic delegate complex authorization local Bun-powered TypeScript server guide writing TypeScript hooks Admin Client configuring `authorization.json delegation [Authorization Implementation Guide-hook-server-managing Server restart required for changes No automatic hot reload triggers reload `zyncbase-config.json `schema.json `authorization.json Environment variable changes Binary updates Validation ZyncBase evaluates config files startup error messages Invalid configuration zyncbase-config.json Line 12 Missing field "schema" Add "schema/schema.json config Next Steps [Store APILearn data sync API Deploy production aspirational content complete examples