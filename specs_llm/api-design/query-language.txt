ZyncBase Query Language Reference 2026-03-09 Prisma-inspired query language.store.query(.store.subscribe() Table of Contents Philosophy Operators [Combining Conditions [Pagination [Examples Design ZyncBase query language inspired Prisma-inspired TypeScript-first clean no prefixes root-level conditions AND **Explicit `or key conditions operator `eq` `gte` **Consistent operators keywords lowercase SQL `limit` **Cursor-based `after` real-time navigation Prisma-inspired evaluated MongoDB GraphQL/Hasura Prisma custom approacheschose Prisma syntax Matches target audience web developers No prefixes Developers learning Prisma-designed** Learned from MongoDB mistakes 15 years improvements over Prisma Aspect ZyncBase AND operator `AND: [...] Implicit root Simpler common case OR operator `OR: [...] Consistent lowercase Equality `equals: value `eq: value` Shorter clearer Not equal `not: value `ne: value` Explicit operator Pagination `take/`skip `limit/`after Standard SQL terms Cursor Comparison alternatives MongoDB style age 18 status 'active GraphQL/Hasura Prisma style age 18 status 'active ZyncBase style age 18 status eq 'active' Query Operators Equality Operators status 'active Equal status 'deleted Not equal `eq` Equal `ne` Not equal Comparison Operatorsage gt 18 Greater than 18 equal age lt 65 Less than lte 65 equal **Operators `gt` Greater `gte` equal `lt Less String Operators name 'john' substring name startsWith 'J' Starts endsWith 'son' Ends email '@example `contains` substring (case `startsWith` Starts prefix `endsWith` Ends with suffix String matching case-sensitive case-insensitive matching normalize data write use computed fieldsArray Operators`typescript role'admin' 'editor' notIn'guest' 'banned' Not in array `in` Value array `notIn` not Null Operators deleted_at isNull true verified isNotNull: true not null `isNull` Field null or undefined `isNotNull` Field value null/undefined Combining Conditions Implicit conditions root level combined with age >= 18 status = 'active' role'admin 'editor' age 18 status 'active' role'admin 'editor common case default keep queries clean Explicit OR `or` key for conditions status = 'active' 'pending' status eq 'active' 'pending' `or` key takes array of condition objectsComplex Nested (AND + OR Combine implicit with OR for complex queries age >= 18 (role = 'admin OR 'editor' age 18 role 'editor (status 'active OR 'pending priority = 'high' 'pending Multiple OR Groups multiple conditions with OR groups age >= 18 (role 'admin OR 'editor' status = 'active' age 18 status eq 'active 'editor one `or` key supported root level complex boolean logic restructuring query multiple queries Sorting Single Field Sort orderBy created_at 'desc' directions Ascending (A-Z 0-9 oldest Descending (Z-A 9-0 newest Multiple Field Sort (Future) orderBy priority 'desc created_at creation date Multi-field sorting planned for future release single-field sorting supported Pagination ZyncBase uses **cursor-based pagination** data consistency realOffset pagination supported causes skipped duplicated items scrolling Basic Pagination limit 50 Max results after '... Opaque token nextCursor Flow Execute initial query `limit` Save `nextCursor value result Pass `nextCursor `after parameter next page Page 1 items await.store.query( limit 20 Page 2 await.query limit 20.nextCursor Infinite Scroll Subscriptions query ZyncBase manages live window load older data without breaking sync.store.subscribe( limit 20 Load 20 more items 'items array await.loadMore( Form Validation Check username taken existing await client.store.query( isAvailable existing.length Date Range Query events within date range events await client.store.query('events' created_at startDate endDate status 'active' orderBy 'desc limit: 50 Multi-Status Filter tasks active or pending tasks await client.store.query('tasks' status 'active status 'pending' orderBy priority: 'desc' Role-Based Query users with admin or editor roles active users await client.store.query('users status 'active' role 'admin 'editor' Search by Substring users email domain users await client.store.query('users email '@example Exclude Deleted Items items soft-deleted items await client.store.query('items deleted_at isNull true Complex Business Logic high-priority tasks admins or editors not completed tasks await client.storequery('tasks' priority 'high status 'completed' assigned_role 'admin 'editor orderBy created 'desc limit: 100 Operator Reference Table Type Description Example `eq` Equal to status 'active' `ne` Not equal to status 'deleted' `gt` Number/Date Greater than `{ age gt 18 `gte` Greater{ age 18 `lt` Number/Date Less than{ age lt 65 `lte` Less than or equal `{ age lte 65 `contains` substring `{ name 'john' `startsWith` Starts with prefix{ name startsWith 'J' `endsWith` Ends with suffix `{ name endsWith 'son' `in` Value in array{ role'admin 'editor `notIn` Value not in array{ role notIn'guest' `isNull`null/undefined deleted_at isNull: true `isNotNull` value verified_at isNotNull: true Best Practices Use implicit AND for simplicity - clean readable age gte 18 status eq 'active' Avoid unnecessary complexity age 18 status eq 'active' Prefer `in` over multiple OR conditions - concise status'active' 'pending 'review' Avoid - verbose status 'active' paginate large result sets prevents memory issues status eq 'active' limit: 100 Risky - return thousands items Use specific queries over broad ones - targeted query project_id currentProject status eq 'active' Avoid fetches too much data status eq 'active' Performance Considerations Indexed Fields Queries perform best on indexed fields Common fields to index Primary keys Foreign keys `user_id `project_id Status fields Timestamp fields`updated Configure indexes `schema.json file Query Complexity Simple queries faster Single field equality Multiple conditions range queries **Slower** OR conditions string operations Real-time Subscriptions keep queries focused - specific subscription client.store.subscribe project_id status Avoid broad updates 'archived Limitations Current Limitations OR one `or key root level nested nest field Multi-field sorting future release aggregations** `count client-side separate endpoint joins** Query one path at a time (denormalize data Workarounds complex queries **Denormalize Duplicate avoid joins **Multiple Fetch related data separately **Client-side complex logic **Computed fields** Pre-compute values write [README API overview entry API queries SDK [Configuration Schema index configuration