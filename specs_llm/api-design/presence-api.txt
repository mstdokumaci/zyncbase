Presence API Reference tracks ephemeral state real Presence data typed against schema in.json stored in-memory never persisted to SQLite Two tiers of presence state **User state** one record per connected user cleaned on disconnect **Shared state** one record per namespace writable by authorized user persists until users leave 5-second grace period for reconnects). methods require ready presence `client.connect() `client.setPresenceNamespace(namespace)` resolve after server resolved namespace string returned internal presence `users.id` as bin16 SDK decodes canonical UUID before replaying subscriptions Calling presence methods before ready fails with `SESSION_NOT_READY` Table of Contents [Schema Definition [User Presence Methods].set(data.subscribe(callback.get(userId.getAll(options.remove [Shared State Methods.setShared(data.subscribeShared(callbackgetShared 4. [Namespaces-namespaces [Authorization 6. Cases Examples 7. Practices Schema Definition Presence fields defined `schema top-level `presence key parallel `store```json
{
  "version": "1.0.0",
  "store": { ... },
  "presence": {
    "user": {
      "cursor": {
        "type": "object",
        "fields": {
          "x": { "type": "number" },
          "y": { "type": "number" }
        }
      },
      "status": { "type": "string", "enum": ["active", "idle", "away"] },
      "typing": { "type": "boolean" },
      "name":   { "type": "string", "maxLength": 64 },
      "color":  { "type": "string", "pattern": "^#[0-9a-fA-F]{6}$" }
    },
    "shared": {
      "slide":   { "type": "integer", "minimum": 0 },
      "playing": { "type": "boolean" }
    }
  }
}
````presence key omitted from.json server synthesizes minimal default "user "status "type "shared Presence usable `presence.set( status "active works without schema configuration explicit `presence section recommended for custom fields typing indicators Arbitrary object nesting supported `cursor x y flattened to wire convention limit 500 flat fields enforced per presence tier Developers work with nested objects SDK handles flattening unflattening constraints** standard schema constraints apply `enum `minimum `maximum `minLength `maxLength `pattern Unknown field indices in `PresenceSet with `SCHEMA_VALIDATION_FAILED` Presence fields required tiers use merge semantics subset fields sent`presence.shared` omitted** no room-level shared state omit `shared section Only user presence active User Presence Methods `presence.set(data)` user presence data Merges with existing record fields not preserved unchanged broadcasts changed fields to subscribers```typescript
client.presence.set({ cursor: { x: 100, y: 200 } })
// cursor__x and cursor__y update. status, typing, name are untouched.

client.presence.set({ status: 'idle' })
// Only status changes. Cursor position is preserved.
```Fire-forget validates field types accept time SDK throttles `presence.set( calls to (16ms intervals network saturation `presence.subscribe Subscribe real-time user presence changes response includes snapshot users updates users join update leave```typescript
const unsubscribe = client.presence.subscribe((users) => {
  renderCursors(users)
})
````Array<PresenceEntry>` excluding self```typescript
interface PresenceEntry {
  userId:   string  // Internal users.id for the active presence scope
  data: {           // User's typed presence data (SDK unflattens nested objects)
    cursor?: { x: number, y: number }
    status?: 'active' | 'idle' | 'away'
    typing?: boolean
    name?:   string
    color?:  string
  }
  joinedAt: number  // Unix timestamp ms of when the user joined
}
```callback fresh snapshot array Presence entries unordered array order stable unsubscribe function sends `PresenceUnsubscribe server `presence.subscribeChanges user presence change deltas full presence snapshot array broadcast high-throughput consumers large rooms local stores incremental updates```typescript
const unsubscribe = client.presence.subscribeChanges((batch) => {
  if (batch.type === 'snapshot') {
    // Initial connection, reconnect, or namespace switch: replace entire local state
    presenceMap.clear()
    for (const user of batch.users) {
      presenceMap.set(user.userId, user)
    }
  } else {
    // Incremental changes from server broadcast batch
    for (const change of batch.changes) {
      switch (change.type) {
        case 'join':
        case 'update':
          presenceMap.set(change.entry.userId, change.entry)
          break
        case 'leave':
          presenceMap.delete(change.userId)
          break
      }
    }
  }
})
```

**Types**:
```typescript
type PresenceChange =
  | { type: 'join'; entry: PresenceEntry }
  | { type: 'update'; entry: PresenceEntry }
  | { type: 'leave'; userId: string }

type PresenceChangeBatch =
  | { type: 'snapshot'; users: PresenceEntry[] }
  | { type: 'changes'; changes: PresenceChange[] }
```**Initial replacement snapshots** first notification 'snapshot users Reconnects presence namespace switches emit another `snapshot replace state **Ordered per-server-batch notifications 'changes once per server batch wire-order preservation no extra client-side batching timer merged `join `update changes carry complete post-merge `PresenceEntry fields not partial wire patches-exclusion** Notifications exclude local user `subscribe() behavior server batch self-updates trigger delta callbacks subscription Snapshot delta listeners share single server subscription`PresenceSubscribe subscription opened first listener closed last listener unsubscribes **Consumer Consumers initializing/replacing local data structures on `snapshot applying `join `update `leave events incrementally on `changes` unsubscribe function `presence.get(userId)` lookup user presence Returns `undefined if user not present no active `subscribe() `subscribeChanges()` Zero-latency reads from SDK's in-memory cache `subscribe()(call `subscribe or( before `get()` returns```typescript
const alice = client.presence.get('018f3a...')
// { cursor: { x: 100, y: 200 }, status: 'active', joinedAt: 1234567890 }
````presence.getAll` Synchronous local lookup users presence namespace Excludes self comparing entry UUID returned presence-scope setup```typescript
const others   = client.presence.getAll()
const everyone = client.presence.getAll({ includeSelf: true })
```call returns fresh array unspecified entry order Returns `[]` no active `subscribe `presence.remove( presence record broadcast `leave event subscribers Called automatically disconnect invoke manually "invisible without```typescript
client.presence.remove()
```Shared State Methods single merged record namespace authorized user write Last-writer-wins field granularity record persists until users leave 5-second grace period before `presence.setShared(data) Merge fields namespace-level shared state Fire-and-forget Fields preserved unchanged fields subscribers```typescript
client.presence.setShared({ slide: 5 })
// Only slide changes. playing is preserved.
```validates field types Subject `presenceSharedWrite authorization Rejected `PERMISSION_DENIED rule satisfied.subscribeShared Subscribe changes namespace-level shared state initial response includes current shared state `null no user called updates authorized user calls```typescript
const unsubscribe = client.presence.subscribeShared((shared) => {
  goToSlide(shared.slide)
})
```typed state object optional fields unflattens nested```typescript
{ slide?: number, playing?: boolean }
```unsubscribe function sends `PresenceUnsubscribeShared server `subscribe()() independent calls hold active simultaneously prerequisite `presence.getShared() Synchronous local lookup shared state Returns `null no active `subscribeShared() or no shared state set Zero-latency reads SDK in-memory cache `subscribeShared() call `subscribeShared() before `getShared() returns data```typescript
const shared = client.presence.getShared()
// { slide: 5, playing: true }
```Presence Namespaces scoped to users same namespace see share state record `users.namespaced = true namespace determines `users.id for presence identity ID returned by `PresenceSetNamespace same UUID snapshots broadcasts```typescript
const client = createClient({
  presenceNamespace: 'workspace:acme:doc:123'
})
````client.setPresenceNamespace removes presence old namespace clears local shared state caches resolves new presence scope invalidates active shared-state subscriptions client re-subscribe Authorization.json namespace pattern```json
{
  "namespaces": [
    {
      "pattern": "workspace:*",
      "storeFilter":        true,
      "presenceRead":        { "$session.userId": { "ne": null } },
      "presenceWrite":       { "$session.userId": { "ne": null } },
      "presenceSharedWrite": { "$session.role":   { "in": ["host", "presenter"] } }
    }
  ],
  "store": []
}
```

| Rule | Controls |
|---|---|
| `presenceRead` | Who can subscribe to user presence and shared state in this namespace |
| `presenceWrite` | Who can call `presence.set()` and `presence.remove()` |
| `presenceSharedWrite` | Who can call `presence.setShared()` |
`$data` incoming field values exposed as$data in rules enabling content-gated authorization```json
"presenceSharedWrite": { "$data.slide": { "gte": 0 } }
```three rules accept `true presence `false RAM-only predicate over$session$namespace$data``presenceSharedWrite` omitted defaults same value as `presenceWrite` write user presence shared state unless restricted Use Cases Examples Collaborative Editor Cursors Status```typescript
// schema.json
{
  "presence": {
    "user": {
      "cursor": {
        "type": "object",
        "fields": {
          "x": { "type": "number" },
          "y": { "type": "number" }
        }
      },
      "name":   { "type": "string", "maxLength": 64 },
      "color":  { "type": "string", "pattern": "^#[0-9a-fA-F]{6}$" },
      "status": { "type": "string", "enum": ["active", "idle"] }
    }
  }
}

// Client
document.addEventListener('mousemove', (e) => {
  client.presence.set({ cursor: { x: e.clientX, y: e.clientY } })
})

client.presence.subscribe((users) => {
  users.forEach(u => renderCursor(u.userId, u.data.cursor, u.data.color))
})
```Typing```typescript
let typingTimer: ReturnType<typeof setTimeout>

input.addEventListener('input', () => {
  client.presence.set({ typing: true })
  clearTimeout(typingTimer)
  typingTimer = setTimeout(() => client.presence.set({ typing: false }), 3000)
})
```Presentation Shared Slide```typescript
// schema.json
{
  "presence": {
    "user": {
      "name": { "type": "string", "maxLength": 64 }
    },
    "shared": {
      "slide": { "type": "integer", "minimum": 0 }
    }
  }
}

// authorization.json — only hosts can advance the slide
{
  "namespaces": [
    {
      "pattern": "presentation:*",
      "storeFilter":        true,
      "presenceRead":        true,
      "presenceWrite":       { "$session.userId": { "ne": null } },
      "presenceSharedWrite": { "$session.role": { "eq": "host" } }
    }
  ],
  "store": []
}

// Host client
nextBtn.addEventListener('click', () => {
  const current = client.presence.getShared()?.slide ?? 0
  client.presence.setShared({ slide: current + 1 })
})

// All clients
client.presence.subscribeShared((shared) => {
  goToSlide(shared.slide)
})
```Co-Watch```typescript
// schema.json
{
  "presence": {
    "user": {
      "name": { "type": "string", "maxLength": 64 }
    },
    "shared": {
      "playing":   { "type": "boolean" },
      "timestamp": { "type": "number" }
    }
  }
}

// Any authorized user can control playback
video.addEventListener('play', () => {
  client.presence.setShared({ playing: true, timestamp: video.currentTime })
})
video.addEventListener('pause', () => {
  client.presence.setShared({ playing: false, timestamp: video.currentTime })
})

client.presence.subscribeShared(({ playing, timestamp }) => {
  video.currentTime = timestamp
  playing ? video.play() : video.pause()
})
```

---

## Throttling

| Operation | Layer | Mechanism |
|---|---|---|
| `presence.set()` | Client-side | SDK throttles to ~60fps (16ms intervals). |
| `presence.setShared()` | Client-side | Not throttled — shared state changes are infrequent by design. |
| User presence broadcasts | Server-side | Batched every 2ms. Multiple updates within the window are merged into one `PresenceBroadcast` per subscriber. |
| Shared state broadcasts | Server-side | Batched every 2ms. Multiple `setShared` calls within the window are merged into one `SharedStateBroadcast` per subscriber. |
Best Practices user fields include transient state Move stable data like `name to `users collection store persist queried **Send only what Merge semantics pay for what send Cursor-only updates include status name granularity** Use specific`doc:123 reduce fan-out avoid leaking presence across contexts `subscribeShared for room-level use store for ephemeral room state shared faster cleaned on disconnect shared state for LWW** two users write shared state last-writer-wins server Shared state fields self-describing values remain meaningful under concurrent writes Related Specifications API Reference persistent state management [Configuration presence store schemas [Authorization auth rules [Error Handling Error types handling strategies