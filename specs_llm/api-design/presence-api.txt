Presence API tracks user state real-time cursor positions selections typing indicators data stored in-memory schemaless validated against `schema.json cleaned on disconnect Table of Contents [Methods-methods [Namespaces-namespaces [Use Cases Examples [Best Practices Presence Methods `presence.set(data) presence data broadcast users same```typescript
client.presence.set({ cursor: { x: 100, y: 200 }, status: 'active' })
````presence.subscribe Subscribe real-time changes leaves updates users namespace```typescript
const unsubscribe = client.presence.subscribe((users) => {
  renderCursors(users)
})
```<PresenceEntry> each entry contains```typescript
{
  userId: string,        // Authenticated user ID
  data: object,          // User's presence data (cursor, status, etc.)
  joinedAt: number       // Unix timestamp of when the user joined
}
```**Returns** unsubscribe function `presence.get(userId) specific user's current presence data **synchronous local lookup** SDK's internal cache returns values active [Subscription] namespace```typescript
const alice = client.presence.get('user-123')
````presence.getAll Returns users presence data namespace Excludes self Like `get() **synchronous local returns empty array no active subscription```typescript
const everyone = client.presence.getAll({ includeSelf: true })
````presence.remove() presence data Called automatically disconnect manually```typescript
client.presence.remove()
```Presence Namespaces scoped [Client Initialization/configuration users same namespace see each other Document-Scoped Presence**```typescript
const client = createClient({
  presenceNamespace: 'workspace:acme:doc:123'
})
// Users in doc:123 only see each other's cursors.
```Use Cases Examples Collaborative Editor```typescript
document.addEventListener('mousemove', (e) => {
  client.presence.set({ cursor: { x: e.clientX, y: e.clientY }, name: 'Alice' })
})

client.presence.subscribe((users) => {
  users.forEach(u => renderCursor(u.userId, u.data.cursor, u.data.name))
})
```Typing```typescript
input.addEventListener('input', () => {
  client.presence.set({ typing: true })
  setTimeout(() => client.presence.set({ typing: false }), 3000)
})
```Throttling **Client SDK throttles high-frequency `presence.set() calls to ~60fps server batches presence broadcasts every 50ms efficiency Best Practices **Keep Data Presence updates frequent include essential transient state **Namespace Granularity** Use specific:123 reduce unneeded broadcasts Late Joiners** joining receive last 5s updates API Reference persistent state management [Configuration Defining presence fields schema [Connection Management Client lifecycle namespace switching [Error Handling Error types handling