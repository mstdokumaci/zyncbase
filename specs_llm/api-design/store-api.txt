Store API Reference handles synchronized data validated against [Schema.md persisted to SQLite synchronized across clients Table of Contents [Direct Path Access [Query API (Path Filtering [Batch Operations [Path Syntax-strings-vs-arrays Direct Path Access Target items properties using dot-notation arrays strict rules path Path Target Depth Allowed Operations Behavior **Collection**'users `1` `create` `query `listen `subscribe` **Append-only `set `remove entire collection **Document**'users.u1 `2` `get` `set` `remove `listen` CRUD** `set` upserts `remove` deletes **Field**.u1.name `3+` `get` `set` `listen **Updates only** clear field `set to `null` `remove` forbidden `store.create(collection value)` Creates new document SDK generates lowercase UUIDv7 string calls `set````typescript
// ID is generated locally for immediate optimistic UI rendering
const id = client.store.create('elements', { type: 'rect', x: 10 })
````string generated ID Custom document IDs supported path match short-ID grammar `[a-z0-9_-]{1,24}` `store.get(path) value state tree```typescript
const element = client.store.get('elements.rect-1')
```**Returns** by path depth): **Array of Objects** points to collection.store.get'users `Array<User **Single specific document **Scalar specific field `undefined path not found `store.set(path, value) Write value or upsert document **Optimistic applied locally synced Reverted if server rejects **Path Constraints** target (depth 2) or **Field** (depth 3+) Targeting collection (depth 1) throws error **Clearing Fields** remove field `set to `null triggers schema validation required **Typed Arrays** fields "array values normalized to canonical form before persistence returned canonical form on reads```typescript
// Upsert a full document. ID is extracted from path if needed.
client.store.set('users.u1', { name: 'Alice', status: 'active' })

// Update a specific field
client.store.set('users.u1.status', 'offline')

// Clear an optional field (instead of remove)
client.store.set('users.u1.address', null)

// Typed array field is canonicalized as sorted set
client.store.set('tasks.t1.tags', ['backend', 'urgent', 'backend'])
// Stored/read as: ['backend', 'urgent']
```Server-Time Last-Write-Wins (LWW) Path level `set() fire-and-forget errors reported via `client.on('error) [Error Handling `void `store.remove(path) Deletes document target 2) Targeting collection 1) field 3+) throws error remove field use `store.set(path, null)```typescript
client.store.remove('elements.rect-1')
````remove() fire-and-forget errors reported via `client.on('error [Error Handling-propagation). `void` `store.listen(path callback)` updates specific path```typescript
const unlisten = client.store.listen('elements.rect-1', (element) => {
  render(element)
})
```**Callback `store.get() return values): **Array** collection **Object** document **Scalar** field **Returns** unlisten function Query API (Path Filtering filtering sorting searching data collections Form validation Server-side rendering Filtered real-time subscriptions `store.query(collection options) one-off query-real-time).```typescript
const users = await client.store.query('users', {
  where: { age: { gte: 18 }, status: { eq: 'active' } },
  orderBy: { created_at: 'desc' },
  limit: 50
})

// Load next page using the attached cursor
if (users.nextCursor) {
  const nextPage = await client.store.query('users', {
    after: users.nextCursor,
    limit: 50
  })
}
```**Parameters `collection Name query 'users 'events `options` Query options `where` Filter conditions `orderBy Sort order `limit` Max results return > 0 `after` Opaque token next page `Promise<Array nextCursor string null standard JavaScript Array additional `nextCursor` property `store.subscribe(collection options callback) Subscribe filtered query results-time).```typescript
const { unsubscribe, loadMore, hasMore } = client.store.subscribe('tasks', {
  where: { status: { eq: 'active' } },
  limit: 50
}, (tasks) => {
  renderTaskList(tasks)
})

// Load older history into the live view
if (hasMore) await loadMore()
```**Parameters `collection (string Name query `options` (object `query() `callback` (function Called when results change unsubscribe void loadMore Promise<void> hasMore boolean See [Query Language Reference]-language.md operators`eq `gte `contains `in Batch Operations Perform multiple write operations`set `remove atomically `store.batch(operations)` If operation fails batch reverted).```typescript
await client.store.batch([
  { op: 'set', path: 'tasks.123', value: { status: 'assigned' } },
  { op: 'remove', path: 'temporary_locks.123' }
])
```Max 500 operations per batch `set `remove allowed Utilities `client.utils.id() Generates lowercase UUIDv7 string client generating IDs batch operations `store.create` relational keys set```typescript
const userId = client.utils.id();
const profileId = client.utils.id();

client.store.batch([
  { op: 'set', path: `users.${userId}`, value: { name: 'Alice' } },
  { op: 'set', path: `profiles.${profileId}`, value: { user_id: userId } }
])
```Path Syntax (Strings vs Arrays methods accept dot-notation strings Arrays Array canonical format Logical vs Wire-Format Paths follow logical progression collections documents fields SDK supports segments **wire compact structure schema index dictionaries SDK resolves nested strings translates_index_index integersTarget Logical Path (SDK Flattened (SDK Internal Wire-Format Path (Socket Pattern :--- **Collection** `['users']`[0`[table_index]` **Document** `['users', 'u1']` 'u1'][0 <bin16('u1')>[table_index id_bin16]` **Field**['users', 'u1' 'name']` `[0, <bin16('u1 2]`[table_index id_bin16 field_index]` **Nested Field**['users', 'u1' 'address' 'city'] 'address__city'[0, <bin16('u1>, 5]`[table_index id_bin16 field_index]` use Arrays preferred variables IDs dots```typescript
const userId = 'alice.smith' // Dot in ID!

// ❌ Dot-notation breaks (ambiguous)
client.store.get(`users.${userId}.name`) 

// ✅ Array handles it safely
client.store.get(['users', userId, 'name'])
```[Presence API.md user awareness features [Query Language Reference Detailed query syntax [Configuration Schema schema definitions Management Client lifecycle events [Error Handling Error types optimistic revert behavior