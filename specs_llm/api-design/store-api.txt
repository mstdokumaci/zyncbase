Store API handles synchronized data validated against [Schema persisted to SQLite synchronized across clients Table Contents Path Access [Query API (Path Filtering [Batch Operations [Path Syntax-strings-vs-arrays Direct Path Access Target items properties dot-notation arrays `store.get(path) value state tree value `undefined if `store.set(path value) Write value applied locally synced Reverted if server rejects Server-Time Last-Write-Wins) Path level client.store.set.remove(path) Removes value path.subscribe(path Subscribe real-time updates path unsubscribe.subscribe( unsubscribe function Query API (Path Filtering) filtering sorting searching data collections Form validation Server-side rendering Filtered real-time subscriptions.query(path options Execute one-off query-real-time). users await client.store.query( age 18 status 'active orderBy created limit 50 Load next page cursor (users.nextCursor await.store.query limit 50 **Parameters `path query `options Filter conditions `orderBy Sort order `limit Max results return `after` Opaque token next page<Array nextCursor null standard JavaScript Array additional `nextCursor` property `store.subscribe(path options callback Subscribe filtered query results (real-time). unsubscribe loadMore client.store.subscribe('tasks status limit 50 renderTaskList Load older history live view await loadMore( **Parameters `path query `options `callback (function Called results change unsubscribe void loadMore Promise<void hasMore boolean See [Query Language Reference-language.md operators`eq Batch Operations Perform multiple write operations`set `remove `store.batch(operations operation fails batch await client.store.batch 'set 'tasks.123 status 'assigned 'remove 'temporary_locks.123 Max 500 operations per batch `set` `remove` allowed Path Syntax (Strings Arrays methods accept dot-notation strings Arrays Array canonical formatAddressing Data Paths variadic follow progression collections documents fields Target Pattern Array Dot Example **Collection**'users' **Document**[collection id'users' 'u1'.u1 **Field**[collection id field'users 'name.u1.name' **Nested Field**[collection, id ...fields'users 'address 'city'.u1.address.city' use Arrays preferred variables IDs dots userId = 'alice.smith' Dot ID Dot-notation breaks client.store.get(`users.name Array handles safely See [Presence API user awareness features [Query Language Reference Detailed query syntax [Configuration & Schema] schema definitions