Store API Reference handles synchronized data validated against [Schema persisted to SQLite synchronized across clients Table of Contents Path Access [Query API (Path Filtering [Batch Operations [Path Syntax-strings-vs-arrays Direct Path Access Target items properties dot-notation arrays `store.get(path) Read value state tree```typescript
const element = client.store.get('elements.rect-1')
```value path `undefined not found `store.set(path value) Write value applied locally synced Reverted if server rejects Server-Time Last-Write-Wins) Path level```typescript
client.store.set('user.name', 'Alice')
````store.remove(path) Removes value path```typescript
client.store.remove('elements.rect-1')
````store.subscribe(path callback real-time updates path```typescript
const unsubscribe = client.store.subscribe('elements', (elements) => {
  render(elements)
})
```unsubscribe function Query API (Path Filtering filtering sorting searching data collections Form validation Server-side rendering Filtered real-time subscriptions `store.query(path options) one-off query-real```typescript
const users = await client.store.query('users', {
  where: { age: { gte: 18 }, status: { eq: 'active' } },
  orderBy: { created_at: 'desc' },
  limit: 50
})

// Load next page using the attached cursor
if (users.nextCursor) {
  const nextPage = await client.store.query('users', {
    after: users.nextCursor,
    limit: 50
  })
}
```**Parameters `path query `options` Query options `where Filter conditions `orderBy Sort order `limit` Max results return `after` Opaque token next page **Returns** `Promise<Array nextCursor string null standard JavaScript Array additional `nextCursor` property `store.subscribe(path options callback) Subscribe filtered query results-time).```typescript
const { unsubscribe, loadMore, hasMore } = client.store.subscribe('tasks', {
  where: { status: { eq: 'active' } },
  limit: 50
}, (tasks) => {
  renderTaskList(tasks)
})

// Load older history into the live view
if (hasMore) await loadMore()
```**Parameters `path (string to query `options (object() `callback (function Called when results change unsubscribe void loadMore Promise<void> hasMore boolean See [Query Language Reference/query-language.md operators`eq `gte `contains `in Batch Operations Perform multiple write operations`set `remove atomically `store.batch(operations) If operation fails batch rejected reverted).```typescript
await client.store.batch([
  { op: 'set', path: 'tasks.123', value: { status: 'assigned' } },
  { op: 'remove', path: 'temporary_locks.123' }
])
```**Limits** Max 500 operations per batch Only `set `remove` allowed Path Syntax (Strings vs Arrays methods accept dot-notation strings Arrays Array canonical format Addressing Data Paths variadic follow logical progression from collections to documents specific fields Target Pattern Array Dot Example **Collection**[collection]'users' **Document**[collection, id'users' 'u1'.u1' **Field**[collection, id, field]'users 'name'users.u1.name' **Nested Field**[collection, id ...fields]'users' 'u1 'address 'city'users.u1.address.city' Why use Arrays? preferred dealing with variables IDs dots```typescript
const userId = 'alice.smith' // Dot in ID!

// ❌ Dot-notation breaks (ambiguous)
client.store.get(`users.${userId}.name`) 

// ✅ Array handles it safely
client.store.get(['users', userId, 'name'])
```See [Presence API-api.md user awareness features Language Reference-language.md Detailed query syntax [Configuration Schema.md schema definitions