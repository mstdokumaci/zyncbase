Error Handling SDK surfaces errors developers `ZyncBaseError interface propagation model write failure reporting auto-retry behavior document defines SDK consumer contract full internal error catalog server-side retry implementation [Error Taxonomy Table of Contents ZyncBaseError Object Propagation Error Codes [Write Failure Reporting-Retry Summary ZyncBaseError Object errors SDK use consistent typed object```typescript
interface ZyncBaseError extends Error {
  code: string;                          // Machine-readable code (e.g., 'RATE_LIMITED')
  retryable: boolean;                    // Whether the SDK can/will auto-retry
  retryAfter?: number;                   // ms to wait before retry (server-provided)
  requestId?: number;                    // ID of the failed immediate request
  writeId?: string;                      // ID of a tracked or confirmed write, if applicable
  path?: string[];                       // Affected data path, if applicable
  details?: {
    phase?: 'accept' | 'write';           // Request acceptance or writer-thread outcome
    batchIndex?: number;                  // Failed batch operation, when known
    [key: string]: unknown;
  };
}
```Error Propagation reach developer two channels `try/catch async methods `Promise throw `ZyncBaseError```typescript
try {
  await client.connect()
} catch (error: ZyncBaseError) {
  if (error.code === 'AUTH_FAILED') {
    // Handle authentication error
  }
}
```Mutation promises phases server rejected request before writer "write writer failure for confirmed Default mutations `confirm "accepted resolve after acceptance update local subscription receive guaranteed-operation async error delivery after acceptance```typescript
try {
  await client.store.set('tasks.t1.status', 'done', { confirm: 'committed' })
} catch (error: ZyncBaseError) {
  if (error.details?.phase === 'write') {
    // Writer-thread failure for this confirmed write
  }
}
```.on( connection systemic errors global error event surfaces connection-level systemic writer failures write errors not attached mutation```typescript
client.on('error', (error: ZyncBaseError) => {
  console.error(error.code, error.message)
})
```3. WriteError events Asynchronous failures are `WriteError`s not rollback NACKs `writeId tracked writes for user feedback diagnostics imply local rollback SDK apply subscription updates 4. Framework hooks populate `error` field```typescript
const { data, error } = useStore('tasks.123')
if (error) { /* render error state */ }
```Common Error Codes to SDK consumers grouped by category Authentication & Authorization Auto-retry `AUTH_FAILED` Invalid ticket expired JWT new token `TOKEN_EXPIRED` Session expired fires event Partial refresh token `SESSION_NOT_READY` operation sent before namespace user resolution finished wait for `connect() or namespace switch `NAMESPACE_UNAUTHORIZED` Not authorized access namespace `PERMISSION_DENIED` Rule blocked operation via `authorization.json same-row guard `COLLECTION_NOT_FOUND` Path collection not defined schema No — fix path Validation Auto-retry `SCHEMA_VALIDATION_FAILED` Data match schema definition No fix data `FIELD_NOT_FOUND` name not defined in No fix path data `IMMUTABLE_FIELD` modify protected system field `INVALID_FIELD_NAME` forbidden `__` sequence `INVALID_ARRAY_ELEMENT` Array non-literal valuenested object fix data `INVALID_MESSAGE Malformed missing `type field `INVALID_MESSAGE_FORMAT Missing fields type id `INVALID_MESSAGE_ID Correlation ID negative out of range Rate Limiting Auto-retry `RATE_LIMITED Message frequency exceeded exponential backoff `MESSAGE_TOO_LARGE Payload exceeds `maxMessageSize reduce payload Connection Server Auto-retry `CONNECTION_FAILED Transport failure (WebSocket closed reconnect_CONNECTIONS_REACHED Server capacity try later `TIMEOUT Operation timed retry backoff `INTERNAL_ERROR Unexpected server failure retry 3 times Write Failure Reporting separates state delivery write outcome reporting Subscription callbacks authoritative state Default accepted writes receive guaranteed per-operation async error delivery Confirmed writes reject promise reports failure Tracked write errors use `writeId include-effort `path metadata Batch write errors include `details.batchIndex` failing operation known Confirmed write timeouts confirmation not received imply aborted failed commitreconnect subscriptions remain source truth final state Auto-Retry Summary Error Category Max Attempts Strategy Connection  Yes Infinite Exponential backoff [Reconnection-strategy Rate-Limit Yes (opt-out Infinite Respect `retryAfter` backoff Server  Yes 3 Exponential backoff Authentication Partial Fire `tokenExpired` wait refresh Authorization  No  Surface immediately Validation No immediately See [Connection Management] Client lifecycle events [Store API Accepted/committed mutation methods [Error Taxonomy] Full internal error catalog retry implementations [Wire Protocol]-format Error envelope wire format