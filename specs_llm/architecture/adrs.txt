Architecture Decision Records decisions ZyncBase consolidated organized chronologically ADR-001 Choice Zig Primary Language 2026-03-08 maximum performance predictable latency native multi-threading real-time collaborative database Node.js introduce pauses unacceptable for 100,000+ concurrent connections Use Zig primary language core engine 3-4x faster Node.js competitive with Go No GC predictable latency-threading** support CPU cores system threads **Memory Efficiency** control zero-cost abstractions predictable memory usage Statically linked binary< 15MB no runtime dependencies simplifies deployment/C++ Zero-cost FFI integration uWebSockets C/C++ libraries P-RTF-time P-PPF (Predictable Performance Best-in-class performance predictable latency Single binary deployment Longer development time Manual memory management ADR-002 Choice uWebSockets Networking 2026-03-08 Standard networking libraries struggle microsecond-scale latency memory overhead WebSocket connections Use uWebSockets C++ networking foundation-in-Class Supports 200,000+ requests/second microsecond-scale latency Powers Bun runtime used Discord millions connections **Memory Minimal overhead< 1KB per vertical scaling millions Successful integration Bun C++ integration Zig FFI zero wrapper overhead P-RTF (Real-time First P-PPF (Predictable Performance low latency high throughput Efficient resource usage connection Requires C++ integration Zig ADR-003 SQLite Only 2026-03-08 embedded database zero-config deployment ACID guarantees high reliability vertical scaling SQLite storage no other database adapters Embedded database single storage no installation external server Full transactional support data integrity guarantees 20 years Easy backup simple deployment-Text Built-in FTS5 extension efficient text indexing P-SHF (Self-Hosting First P-PRC (Predictable Costs P-PPF Performance P-SOT (SQLite Simplest operational model deployment No "hidden database infrastructure costsNo horizontal scaling storage Requires single-writer constraint ADR-004 SQLite WAL Mode Concurrency 2026-03-08 SQLite single writer blocks readers limits throughput multi-core systems use SQLite Write-Ahead Logging (WAL) mode Multiple readers one critical vertical scaling I WAL optimizes I/O patterns performance P-PPF (Predictable Performance) P-SOT (SQLite parallel reads CPU cores Faster write performance Writes serialized batching throughput ADR-005 Multi-threaded Core Engine 2026-03-08 single core utilize SQLite parallel read capability multi-threaded event loop uWebSockets Implement multi-threaded core read/write separation Lock-free cache parallel reads Mutex-protected writes SQLite connection pool reader per CPU 17x performance improvement (10k → 170k req/sec multi-core machines Matches performance characteristics Bun uWebSockets P-PPF (Predictable Performance) P-VSF (Vertical Scaling Full utilization system resources Predictable vertical scalingarchitectural complexity single designs ADR-006 No Horizontal Scaling 2026-03-08 v1.0 clustering/horizontal scaling No ZyncBase for vertical scaling server CPU Distributed state complexity out of v1 collaborative apps don't reach 100k users per server Simpler architecture faster development better reliability Vertical scaling to 100k+ connections sufficient 99% use cases Avoids performance consistency overhead P2P/Raft/Paxos P-PPF-SHF (Self-Hosting First P-VSF (Vertical Scaling First) Best-in-class single-node performance Simplest operational model limit ~100k users per node Future horizontal scaling external tools ADR-007 Optimistic Writes Default [ADR-031] decision replaced original text preserved 2026-03-09 Client SDK handle real-time state updates fluid user experience optimistic default Zero-latency performance Matches real-time collaboration patterns Simplifies UI code waiting API P-RTF (Real-time First) Instant UI feedbackRequires local state revert on server rejection Errors handled via global event listeners ADR-008 Server-side Validation 2026-03-09 Client SDK replicate server validation logic errors? No Validation enforced on server Client SDK uses TypeScript runtime validation Prevents version-coupling Keeps SDK lightweight Server for security client validation redundant P-SBD (Secure Default) - P-TSF (TypeScript-First) Smaller SDK bundle size No "stale schema" bugs on clients Errors after server round-trip ADR-009: Configuration-First (Zero-Zig) 2026-03-09 barrier to entry Zig ZyncBase follows "Zero-Zig" philosophy functionality managed via JSON configuration files No Zig knowledge required Mimics Nginx PostgreSQL Lowers barrier to entry for JS/TS developers-Zig Philosophy Core Principles P-SHF (Self-Hosting First) - P-TSF (TypeScript-First) No compilation instant setupLess flexible than code-based config authorization inputs from trusted identity claims ADR-010 Prisma-Inspired Query Language 2026-03-09 query language natural web developers Prisma-inspired JSON syntax AND lowercase TypeScript-first Clean syntax no prefixes complex nesting simple queries P-TSF (TypeScript-First) Familiarity ease of use Custom format standard SQL GraphQL ADR-011: Lock-Free Cache Atomic Reference Counting 2026-03-09 In-memory cache high reads atomic reference counting high-performance parallel reads Simple implement RCU pointers proper atomic operations global mutex fallbacks unacceptable P-PPF (Predictable Performance ADR-012 Iterative MessagePack Parser 2026-03-09 Risk stack overflow malicious client payloads deep nesting iterative MessagePack parser limits on nesting depth payload size P-SBD P-PPF (Predictable Performance ADR-013 MessagePack for Production JSON for Debug 2026-03-09 Wire protocol efficient accessible for debuggingMessagePack production JSON debug P-PPF Performance ADR-014 No WebSocket Compression (v1.0 2026-03-09 enable per-message deflate compression No compression v1.0 MessagePack compact Compression adds CPU overhead latency-PPF ADR-015 Strict Client API Namespaces 2026-03-09 Developers confuse ephemeral awareness data durable state SDK separates methods.store.presence Forcing choice creates Prevents misuse presence data P-TSF (TypeScript-First) ADR-016 Bun Hook Server [ADR-032]-driven-authentication-external-permission decision replaced original text preserved historical context 2026-03-09 complex relational authorization without bloating Zig core out-of-box Bun-based Hook Server complex logic Stateless checks `authorization.json P-DES Security P-TSF (TypeScript-First) Unlimited flexibility TypeScript Protects Zig core business logic complexity Small latency penalty~1-2ms delegated checksADR-017 Conflict Resolution Strategy 2026-03-09 semantics.set() concurrent users Server-Time Last-Write-Wins) Path Level Matches REST/Firebase semantics Eliminates field-level merging Single vertically-scaled server guarantees global order P-RTF-time P-PPF (Predictable Performance ADR-018 Query API MVP Scope 2026-03-09 MVP supports operators excludes Regex FTS complex joins v1 Regex introduces ReDoS limits predictable performance P-PPF (Predictable Performance P-SBD (Secure Default Developers rely computed properties application-side search complex search ADR-019 SDK-Friendly Syntax Compact Wire Format 2026-03-09 2026-03-31 SDK targets TypeScript developers expressive nested syntax-style query Zig server benefits flat positional data parsed without recursive descent string-keyed map inspection SDK developer-friendly syntax wire protocol uses compact positional arrays SDK before transmissionprinciple applies SDK accepts.u1.name wire sends[0 2 ADR-025 dictionary SDK accepts age 18 wire sends[3, 4 18[field_index op_code value SDK accepts created 'desc wire sends[8, 1]_index desc_flag SDK accepts'users.u1.address.city flattens `address__city sends mapped integer `field_index Grammar full wire encoding specification operator codes Paths map SQLite lookups eliminates ambiguity IDs Flat condition tuples eliminate recursive JSON parsing reads fixed-position array Integer operator codes avoid string comparison map Zig `Operator enum Nested path flattening`address.city `address__city TypeScript P-TSF (TypeScript-First) P-PPF (Predictable Performance) ADR-020 Performance Targets 2026-03-13 benchmarks prevent performance regressions Establish targets ZyncBase v1.0.Metric Target Measurement Concurrent connections 100,000+ Sustained Requests/second 200,000+ Mixed workload Latency (p50 < 1ms In-memory ops Latency (p99) < 10ms Including disk Memory connection < 1KB Excluding buffers Binary size < 15MB Cold start time < 100ms ready P-PPF (Predictable Performance P-VSF Scaling ADR-021: Fine-Grained Subscription Invalidation 2026-03-09 fine-grained change detection in-memory AST evaluation Avoids O scaling-running SQL queries Sub-100ms sync disk loop P-RTF-time First-PPF (Predictable Performance ADR-022 Formal Error Taxonomy Handling Strategy 2026-03-13 6-category error taxonomy automatic SDK behavior P-TSF (TypeScript-First P-SBD (Secure Default ADR-023 Unified Subscription Engine Operations 2026-03-31 ZyncBase defines 4 SDK commands 4 wire message typesmessage handler splits reads between `StorageEngine `SubscriptionManager creating dual code paths for overlapping operations path-based commands subsets collection-based ADR-021 mandates in-memory AST evaluation for subscriptions extends unified read Introduce single Subscription Engine entry point for server-side read operations Protocol 4 2 Read Message Remove `StoreGet `StoreListen `StoreUnlisten reads go through `StoreQuery `StoreSubscribe Retained `StoreUnsubscribe` `StoreLoadMore `StoreDelta` Path translates `get into `StoreQuery`StoreSubscribe paths collection-level queries id filters `get becomes `StoreQuery( maintains local value cache change detection serves exact-match queries when active subscription covers data Replaces split between `StorageEngine `SubscriptionManager `StoreQuery if collection warm evaluate from memory cold query SQLite-shot queries `StoreSubscribe SQLite cold register subscriber push deltas edge-transition evaluation `RecordChange events`RecordChange update in-memory state evaluate subscriber groups push `StoreDelta Clients identical collection share evaluation per-subscriber metadata New subscribers receive snapshot-Level Deltas Server broadcasts `set `remove granularity No field-level patches SDK handles field projection-Free Cache Subscription engine replaces lock-free cache application data reads cache continues for permission snapshots schema metadata Collection state evicted last unsubscribe Disconnect triggers auto-unsubscription Configurable `subscriptionEngine.maxMemoryMB New subscriptions `RESOURCE_EXHAUSTED limit approached`loadMore hits SQLite disk v1 In-memory SQLite warm-start `loadMore SDK subset query matching specialized per-field index structures Alignment** P-RTF-time First-SAT-PPF Performance-SOT Narrows ADR-005 Lock-free cache role to auth/schema metadata Extends ADR-021: In-memory AST evaluation covers all reads Wire protocol reads halved (4 → 2) Single server-side code pathSubscriber grouping evaluation per subscription not client SDK local cache eliminates round-trips First cold collection incurs SQLite read `loadMore` hits disk Subscription engine significant implementation effort ADR-024 Canonical Sorted-Set Semantics for Typed Array Fields 2026-04-17 Typed array fields for tags labels roles ZyncBase introduced canonicalization (sort dedupe) developer experience determinism runtime performance formal architectural contract ZyncBase treats schema-defined typed arrays as canonical sorted sets Validate element against `items type Reject `null nested arrays objects Sort elements with type-aware comparator Remove duplicates canonicalized representation SQLite Return canonicalized arrays `contains` fields membership over canonical set content` use canonical equality semantics `in` operand arrays canonicalized during parsing-insensitive Arrays objects unsupported Unsupported operators array fields invalid Better DX for tag-like fields Deterministic storage comparisons Faster membership/equality behavior in SQL in-memory evaluationP-TSF (TypeScript-First P-PPF (Predictable Performance) ADR-025 Schema Dictionary Compression (Integer Routing 2026-04-18 ZyncBase handles high message throughput Server-side routing string-based field names requires allocating strings computing hashes hash map lookups read write repeated strings wire increases bandwidth usage Replace schema-defined string with dense integer mappings wire protocol for operations connection server pushes `SchemaSync message positional arrays tables fields Runtime client SDK builds in-memory string-to mapping runtime SDK resolves developer logical string paths into integer arrays before server Operation values use integer-keyed maps sparse updates without payload ambiguity`Presence APIs exempt from optimization use string-based properties schema definitions by ADR-033 Presence participates in integer-index wire encoding via schema.json SDK hashes `SchemaSync payload detect schema modifications restarts queues against catastrophic index shiftingTransforms O(N) string hashing map lookups Zig into O(1) array assignments Memory allocations for strings parsing removed Exchanging 1-byte integers via MessagePack reduces payload Sending dictionary arrays older mobile clients adapt to fields P-PPF (Predictable Performance P-RTF (Real-time First P-TSF (TypeScript-First) ADR-026 Internal Namespace Dictionary Routing for Namespaces) 2026-04-24 Namespaces dynamic strings Storing in SQLite wastes space 300MB per 10 million rows makes index lookups slower namespace mapping to `config.json impossible dynamic Implement hidden system table_zync_namespaces (id Reserve ID `0` for global namespace client-created namespaces use positive IDs wire protocol untouched client sends namespace Zig engine upserts namespace string table connection caches integer ID on WebSocket connection uses for SQLite reads writes reduces database size performance gain on SQLite index lookupsZero friction developer Client SDK (strings used Wire protocol stateful optimized (no strings per data P-PPF (Predictable Performance P-TSF (TypeScript-First P-SOT (SQLite Source Truth ADR-027 `owner_id` System Column Stateless Authorization Limits 2026-04-24 secure multi-tenancy object-level ownership authorization line complexity JSON-based authorization rules real performance Add `owner_id` system column storage tables alongside `id `namespace_id `created `updated_at` Store `owner_id same `doc_id `id``BLOB(16) UUIDv7) never external identity string Implement implicit `users` system table maps external string identity claims to ZyncBase `id` UUIDv7) Keep `owner_id` on `users` table-shape consistency equal to `id` session resolution Zig engine maps external identity string to internal UUIDv7 `$session.userId` `BLOB(16) populate `owner_id` with$session.userId` upon document creation Treat `id` as document identity collectionexpected unique across collection/table `namespace_id` not part primary key routing/filtering column limit `authorization.json evaluation Zig five variables$session$namespace$path table$doc$value (incoming `$doc` reference columns target selected updated deleted rule relational join relationship traversal lookup table forbidden JSON permissions represented trusted external identity claims encoded same row `owner_id` enables code-free object-level security `users` mapping table guarantees_id compact 16-byte binary format ZyncBase `doc_id representation saving ~20 bytes per row `owner_id` on `users` avoids table-shape exceptions DDL generation authorization defaults replication payloads query planning self semantics identity `PRIMARY KEY(id)` avoids composite key fan-out foreign keys tie-breakers SDK APIs Namespaces constrain visibility redefine document identity limits evaluation context guarantee predictable rule evaluation same-row SQL predicate injection "slow query" problem auth layerP-POM (Primitives over Magic exposing `users` standard collection hidden config P-PPF (Predictable Performance P-SBD (Secure by Default) ADR-028 Global Master Data (namespaced: false) 2026-04-25 ZyncBase collections partitioned by `namespace_id SaaS applications require master data tables transcend namespace boundaries visible globally Introduce"namespaced": boolean primitive in `schema.json definitions storage tables retain standard system columns `namespace_id `owner_id `namespaced` changes_id not column `namespaced omitted defaults to `true` Zig stores active namespace ID filters set `false` stores global namespace ID `0` routes queries `users` collection defaults to"namespaced": false `users` configured"namespaced": true for tenant-isolated identity realms external identity mapping scoped by(namespace_id external namespace switching requires re-resolving$session.userId` Provides flexibility define global data collections without server-side logicAvoids duplicating data across namespaces Maintains "Secure by Default namespacing unless opted-out Preserves uniform table shape null-heavy special cases indexes DDL generation migrations authorization predicates consistent Avoids_id predicates in hot paths Each table one namespace predicate active `0 for global tables P-POM (Primitives Magic P-SOT (SQLite Source Truth ADR-029 Scoped Session Readiness Gate 2026-05-05 ADR-026 established integer namespace routing ADR-027 `owner_id `users.id ADR-028 allowed `users.namespaced = true` invariant WebSocket transport before scoped context.namespaced = true external identity string different `users.id per namespace identity resolution hardcoded anonymous UUID transport-only state Distinguish transport connectivity from scoped session readiness Store external identity string on connection SDK-generated authenticated clients JWT subject Resolve operation-domain scope through SQLite before accepting scoped operations scope namespace string resolved to_namespaces.id external identity `users.id resolved `users.id` stored as$session.userId Store presence separate scopes namespaces differ Store operations require Presence If `users.namespaced false identity resolution uses namespace ID `0` store presence scopes share same `users.id If `users.namespaced true identity resolution uses namespace ID scope Store namespace switching re-resolves presence namespace switching Before scope ready server accepts lifecycle messages authentication refresh store presence ping/pong close Store query subscription presence data messages rejected `SESSION_NOT_READY` Namespace identity changes invalidate scoped state Store namespace changes clear subscriptions Presence namespace changes clear presence subscriptions Auth refresh updates session active scopes continue auth refresh fails connection terminated Preserves `users.namespaced = true identity Ensures `owner_id presence `userId` correspond `users.id rows Keeps SDK ergonomics simple after scopes ready Avoids anonymous-user shortcuts keys authorizationP-POM (Primitives Magic P-SOT (SQLite Source Truth P-SBD (Secure Default ADR-030 Async Session Resolution-Blocking Reactor Handoff 2026-05-06 ADR-029 scoped session readiness gate `StoreSetNamespace triggers namespace user identity resolution implementation uses `CompletionSignal.wait() Mutex+Condition blocking uWS result blocks reactor 500μs–2ms resolution stalling WebSocket connections violating p50 < 1ms latency target problem `StoreSetNamespace `PresenceSetNamespace `AuthRefresh other operations accepted writes (ADR-031) fast reader-pool reads Resolution code path blocking round-trip writer thread Replace `CompletionSignal.wait() resolution two-tier strategy in-memory identity cache async SPSC ring buffer event loop wakeup handoff cache misses-tier 1 Check lock-free namespace user identity caches set scope respond zero I/O blocking 2 non-blocking cache miss enqueue `resolve_session` WriteOp writer thread return response uWS reactor freeTwo `lockFreeCache instances `StorageEngine `namespace_cache` Maps(namespace_string →_id (i64) Populated thread after id Immutable once set IDs `identity_cache` Maps_namespace_id_user_id → `users.id Populated thread after id Immutable once set ID mappings caches reuse `lockFreeCache` generic epoch-based reclamation atomic ref-counting COW map swaps Values trivial heap `deinit` no-op `resolve_session` WriteOp** Replaces_namespace `resolve_user WriteOp Performs namespace user resolution single reducing to zero`SessionResolutionBuffer SPSC ring buffer (capacity 256 `ChangeBuffer writer thread pushes `SessionResolutionResult structs uWS post_handler drains`SessionResolver new component `NotificationDispatcher uWS post_handler drains `SessionResolutionBuffer acquires target connection ID applies resolved scope encodes success/error response calls `ws.send()`scope_seq` monotonic per-connection counter on scope resetCarried_session WriteOp checked at delivery time match result stale sent `StoreSetNamespace discarded capacity rationale (256 + 512 overflow `StoreSetNamespace lifecycle message ~1x per session not data thread processes ~5,000–10,000 resolutions/second post_handler drains uWS loop Worst-case (100k buffer accumulates ~10–50 results between drain cycles 256 provides 25x headroom over worst case buffer full results spill into overflow ArrayList ring buffer (256) overflow (512) exhausted 768 writer logs error drops result client retries via SDK timeout combined capacity provides >150x headroom over worst-case load 1 eliminates I/O common After first resolution connections resolve in ~1μs cache no reader mutex SQLite writer thread 2 eliminates reactor blocking for cold New pairs resolve without blocking reactor response delivered asynchronously via `ChangeBuffer +_wakeup_loop()_handler pattern WriteOp** namespace user resolution reduces WriteOp variants eliminates two writer thread round-trips on cache miss`store_ready `false until resolver result Messages before resolution receive `SESSION_NOT_READY Connections disconnect during async resolution `SessionResolver uses `ConnectionManager.acquireConnection() discard results disconnected `NotificationDispatcher Extends ADR-026 Namespace string integer resolution lock-free cache first thread second Extends ADR-029 Scoped session resolution non-blocking uWS reactor Extends ADR-011 Lock-free cache namespace user identity mappings auth/schema metadata Zero blocking uWS reactor session resolution Common case cache resolves ~1μs 500x faster blocking path Simpler WriteOp surface_namespace_user_session connections unaffected during cold-start resolution Async response adds event latency cache misses Two new lock-free cache instances add memory proportional to unique pairs ADR-031: Mutation Acknowledgement Realtime Consistency Semantics 2026-05-25 ADR-007 Optimistic Writes by Default ZyncBase realtime-first database Client-visible state through subscriptions changes visible through server-pushed subscription deltasMutation acknowledgement writes pass asynchronous server-side pipeline mutation accepted server before thread committed subscription update after mutation response distinct events TypeScript SDK update local subscription data for mutating operations.set.remove.batch subscription state changes server emits updates write failure reporting not needed for rollback needed for user feedback diagnostics workflows precise write outcome reporting ZyncBase uses subscription-first state propagation default consistency model client-visible data Default mutation methods accepted `store.set.remove.batch mutate local subscription state successful mutation response server accepted mutation request write committed Subscription callbacks authoritative source committed state infer write failure from subscription delta ZyncBase distinguishes two confirmation levels```ts
await store.set(path, value);
await store.set(path, value, { confirm: "accepted" });
// Resolves when the server accepts the mutation into the write pipeline.

await store.set(path, value, { confirm: "committed" });
// Resolves when the writer commits the mutation or an accepted no-op.
// Rejects when the writer reports failure.
```"accepted default represented SDK types examples prefer no-options form "committed applies mutating operations.set.remove.batch server parsed validated authorized before accepted mutation write pipeline **committed**-thread result succeeded accepted no-op outcomes **request/response correlation responses **writeId** correlation tracked writer-thread outcomes **WriteError** asynchronous writer failure message SDK event public protocol SDK documentation use "NACK Writer failures errors not rollback commands **Immediate Request reject mutation before owns operation Examples malformed messages invalid paths payload shapes unknown tables fields session not ready authorization failures queue admission failure request handling failures before writer ownership Confirmed write failures immediate request failures SDK error type `ZyncBaseError metadata phase```ts
{
  phase: "accept" | "write"
}
```Outcome Writer-thread failures as errors subscription state rollback instructions Tracked confirmed writes use `writeId correlate outcomes existing request `id scoped response confirmed writes SDK promise public success signal ZyncBase expose public `WriteCommitted event core API confirmed promise resolves no-op rejects failure confirmation received `WriteError public async failure name```ts
{
  type: "WriteError",
  writeId: "...",
  code: "PERMISSION_DENIED",
  message: "...",
  details: {
    phase: "write"
  }
}
```best-effort metadata single `set include batch failure operation include `path `batchIndex transaction-level systemic failure `path omitted Global write-error events tracked systemic failures untracked writes receive per-operation async error delivery acceptance Commit confirmation subscription delivery not ordered confirmed write resolves subscriptions receive state change subscription filtering connection state SDK guarantee subscription callback before write resolves Subscriptions authoritative committed-state channel Write confirmation outcome reporting not state delivery.batch atomic resolves full batch commits no-op outcomes rejects operation fails exposes no partial success commit partial writes failure Batch failures include `batchIndex failing operation identified```ts
{
  code: "PERMISSION_DENIED",
  message: "...",
  details: {
    batchIndex: 2,
    phase: "write"
  }
}
```transaction-level failures no operation `batchIndex` omitted Default `store.batch()` follows same contract writes **Authorization Write authorization rules preserve invariant client create document same session update same write rules `StoreSet` `$doc` write rules interpreted by write kind candidate document existing document existing document Create authorization evaluated RAM against candidate document candidate document includes document id injected `owner_id = $session.userId` incoming normalized fields server-managed fields defaults `owner_id` injected server ownership rules pass creates owning session```json
{ "$doc.owner_id": { "eq": "$session.userId" } }
```create rule references$doc field absent document not server-injected defaulted create denied Update delete authorization guarded against rows ZyncBase require post-image authorization for updates user update document state rule write rule "draft update from `draft to `published after transition allows updates-Op Deleting missing document success/no-op Setting field document same stored value success ZyncBase suppress deltas value Applications rely on delta for same-value writes committed write client not write error write no delta subscription targets data change subscription result Default accepted writes perform extra storage reads classify zero-row outcomes Confirmed writes classify guarded zero-row outcomes produce errors row fails write guard `PERMISSION_DENIED absent row delete success/no-op storage transaction failure error **Connection Failure Operation status retained in memory scoped originating connection for tracked confirmed writes ZyncBase retain write-status records across disconnects connection drops before confirmed write result SDK rejects promise with connection error write commit reconnect subscriptions remain source truth for final state Confirmed writes need timeouttimeout confirmation received imply write aborted failed commit ZyncBase guarantee abort after write accepted Automatic idempotency keys for retried writes outside decision Some operations idempotent retry requires separate idempotency design Systemic writer storage failures engine health events not If processing new writes fail during acceptance engine unhealthy Confirmed writes reject with `ZyncBaseError server status degraded state Write errors retryability transient operational failures retryable access denied invalid values schema errors constraint violations not retryable disk full encourage automatic client retry loops decision separates concepts request acceptance mutation state observation write outcome reporting optional operation status error feedback preserves ZyncBase realtime-first model precise feedback mutation wait for commit request/response database latency for subscription Restoring local writes rollback conflict handling complexity Write errors valuable user feedback diagnostics not local state repair Keeps default writes low latency Preserves subscriptions for state Avoids rollback machinery precise committed-write confirmation clear async failure reporting modelKeeps request ids writer outcome ids separate `await store.set() documentation explicit Applications confirmation request committed confirmation Confirmed writes add writer-result correlation timeout handling Confirmed authorization errors require classification work mutation wait for writer commit changes latency Restore optimistic local writes require rollback conflict handling complex semantics report writer-thread failures Missing subscription deltas ambiguous save failure feedback Use NACKs as rollback messages no speculative local subscription state Writer failures write-status errors not rollback commands Reuse request id as writer outcome id Request ids correlate responses Writer outcomes need separate `writeId for confirmed writesADR-032 Config-Driven Authentication External Permission Claims 2026-06-02 Supersedes-016 Bun Hook Server Modifies [ADR-027 `owner_id System Column Stateless Authorization Limits Modifies [ADR-029 Scoped Session Readiness Gate ZyncBase needs secure authentication anonymous access tenant/project authorization object ownership without identity provider arbitrary application logic previous Bun Hook Server design allowed TypeScript permission checks extra runtime internal protocol latency circuit-breaker behavior blurred product boundary ZyncBase resource server validates identity enforces declarative rules compute source truth user accounts memberships billing state permission grants Remove Bun Hook Server support Keep HTTP ticket exchange native configuration-driven ZyncBase validates external JWTs issuers audiences algorithms shared secrets JWKS sources ZyncBase JWT claims$session defines JWT claim names$session variables Permission claims trusted token signature registered constraints validated Anonymous access same ticket pipelineSDK generates anonymous subject persists presents anonymous external identity auth enabled `users` table internal identity mapping optional profile/display-data table maps external subject to `BLOB(16) UUIDv7 `owner_id$session.userId presence identity foreign keys User-row fields not loaded for authorization part of$session `authorization.json limited to RAM checks over$session$namespace$path$value same-row$doc` predicates store query predicate model permission requiring joins relationship traversal API calls billing lookups permission computation represented before ZyncBase request trusted token same-row data application code Token refresh revocation permission-update mechanism ZyncBase revalidates replacement JWT updates session active scopes continue JWT invalid connection terminated server-side revocation list Tokens short-lived ≤15 SDK refresh before expiry session token expires no refresh server terminates connection Tenant project switching new JWT compatible scoped grant arrays large changing permission sets application mint narrower active-context tokens-SHF-Hosting no required process extra runtimeP-TSF (TypeScript use JS/TS identity layer ZyncBase run P-DES (Declarative ZyncBase authorization JSON P-PPF (Predictable no foreign calls joins hidden database reads Positive Removes Hook Server runtime hook protocol circuit-breaker failure mode WebSocket authentication safe JWTs URLs authorization deterministic fast permission ownership explicit external identity provider owns permission ZyncBase enforces trusted claims anonymous users low-friction database tables Negative ZyncBase answer permission questions token namespace target row value revocation depends short-lived tokens refresh behavior Large permission compressed into-manageable claims groups roles active-context tokens before ZyncBase maximum limit claim array elements 1000 items JWTs Hook Server advanced authorization second runtime adds latency availability modes permission computation Load `users` row fields$session authorization introduces hidden database reads refresh semantics second permission source identity mapping ownership references optional profile data not authorization input Add richer JSON relationship queries Rejectedjson query engine undermine same-row predicate limit authorization Require new JWT namespace switch Rejected strict token contain scoped grants `read `write_projects `authorization.json` check namespace parts same-row fields against arrays Applications choose narrower active-context tokens when token size staleness matters ADR-033 Typed Two-Tier Presence System 2026-06-11 Removes presence exemption clause from [ADR-025: Schema Dictionary Compression ADR-025 established integer-index wire routing exempted presence"Presence APIs exempt optimization use string-based properties lack formal schema definitions." presence designed freeform schemaless JSON blob indexing machinery exist Three factors make exemption obsolete design presence **Presence unimplemented server zero handlers no variants SDK methods implemented presence migration cost zero-world presence data structurally predictable use cases data shallow (≤2 nesting small (5–8 fields), typed Cursor positions status enums typing booleans not arbitrary JSON blobs benefits from integer-index compression high frequency-first developers need types ZyncBase targets TypeScript frontends Developers define presence field types UI Requiring `presence` schema section aligns server enforcement with type definitions redundancy analysis presence use cases gap in single-tier design not all ephemeral state user-owned slide video playback position viewport represent-level state one value for entire room not per user Without second developers misuse store or build out-of-band coordination mechanisms presence schema server defines presence schema.json explicit `presence` top-level key authoritative key absent synthesizes implicit schema no schemaless freeform mode presence data typed against schema **Two-tier field model `presence.user` fields owned by each user One record per user namespace cleaned on disconnect `presence.shared` fields namespace-level ephemeral state One record for entire namespace Survives namespace session lifetime **Flat wire format with `__` separator Nested presence fields flattened using same convention store SDK flattens outbound unflattens inbound dataDevelopers interact with nested object form Arbitrary nesting depth supported to 500 flat fields per tier). **Integer-keyed wire encoding presence messages `PresenceSet` send integer-keyed maps `SharedStateBroadcast deliver integer-keyed data `SchemaDictionary machinery extended with presence-specific index maps `SchemaSync` arrays`SchemaSync` extension Two new arrays added `presenceUserFields `presenceSharedFields names No companion flags arrays needed fields no system columns doc_id semantics field indices map to plain typed values semantics tiers `PresenceSet perform field-level merges not replacements 101.0 updates `cursor__x` other fields preserved server maintains per-user applies incoming integer-keyed maps as patches push message types `PresenceBroadcast` carries user presence events `SharedStateBroadcast` carries namespace-level shared state changes different SDK handling paths local cache targets separate message types state lifetime grace period cleared when users leave namespaceShared state RAM-only survive server restart grace period 5-second grace period prevents disconnections wiping shared state grace period enforced 50ms batch flush loop `namespace_empty_at` map records user count drops to zero flush cycle evicts entries older than grace threshold No additional timer infrastructure user rejoins within grace period preserved shared state`presenceSharedWrite` authorization rule new rule key `authorization.json controls-level shared state RAM-only against$session$namespace$data$data` variable exposes incoming field values content-gated authorization hosts set `status **Presence writes fire-and-forget Neither `confirm` option accepted server validates types field indices rejects invalid payloads `SCHEMA_VALIDATION_FAILED` No write pipeline confirmation for ephemeral state **Wire Protocol Changes** Complete message specifications in ProtocolSummary Message Change `SchemaSync Gains `presenceUserFields string `presenceSharedFields string `PresenceSet changes string-keyed to integer-keyed map `PresenceSetShared Sets namespace-level shared fields integer-keyed merge delta `PresenceSubscribe Returns user snapshot shared state `PresenceSubscribeShared `PresenceBroadcast changes to integer-keyed map `userId `bin16 `subId integer `SharedStateBroadcast Carries integer-keyed shared state delta to subscribers **Authorization```json
{
  "namespaces": [
    {
      "pattern": "workspace:*",
      "storeFilter":        true,
      "presenceRead":        { "$session.userId": { "ne": null } },
      "presenceWrite":       { "$session.userId": { "ne": null } },
      "presenceSharedWrite": { "$session.role":   { "in": ["host", "presenter"] } }
    }
  ],
  "store": []
}
````presenceSharedWrite defaults same omitted consistent with safe defaults presence shared state unless restricted Presence fields simpler than store fields collections document IDs queries integer-index encoding easier than store not payload savings substantial at high frequency cursor update status shrinks from ~50 to ~22 bytes At 60fps difference compounds across broadcast path server benefits array index access replaces string hashing lookups `decodeIntMap pattern reused Shared state fills product gap Without developers pollute store with ephemeral state stale data or build custom signaling outside ZyncBase grace period 50ms flush loop costs zero infrastructure runs unconditionally `namespace_empty_at check namespaces zero normal operation$data in presence auth rules consistent with$value store write rules incoming integer-keyed map decoded rule evaluator before write accepted P-RTF-time) P-TSF-First-PPF Performance P-SBD (Secure Default Full integer wire compression for presence messages same performance as store operations Server-side field type validation at accept time presence operationsNamespace-level shared state room-wide coordination `$data auth rules enables content-gated presence writes Zero migration cost presence unimplemented TypeScript SDK generates types from presence schema Grace period shared state cleanup without cost Presence usable via implicit schema explicit section in.json recommended for custom fields type safety schema changes trigger `schemaChange connected clients reconnect requirement 500 flat fields per presence tier enforced server startup pathological schemas Shared state RAM-only survive server restart