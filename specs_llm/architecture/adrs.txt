Architecture Decision Records decisions ZyncBase consolidated organized ADR-001 Choice Zig Primary Language 2026-03-08 maximum performance predictable latency native multi-threading real-time collaborative database Node.js introduce pauses unacceptable 100,000+ concurrent connections Use Zig primary language core engine 3-4x faster Node.js competitive Go No GC predictable latency-threading** support CPU cores mapping system threads Efficiency** Manual control zero-cost abstractions predictable memory usage Statically linked binary< 15MB no runtime dependencies simplifies deployment/C++ Zero-cost FFI integration uWebSockets C/C++ libraries #1 Real-time First #8 Predictable Performance Best-in-class performance predictable latency Single binary deployment Longer development time Manual memory management ADR-002 Choice uWebSockets Networking 2026-03-08 Standard networking libraries struggle microsecond-scale latency memory overhead concurrent WebSocket connections Use uWebSockets C++ networking foundation-in-Class Supports 200,000+ requests/second microsecond-scale latency Powers Bun runtime used Discord millions connections **Memory Minimal overhead< 1KB per vertical scaling millions Successful integration Bun C++ integration Zig FFI zero wrapper overhead #1 Real-time First #8 Predictable Performance low latency high throughput Efficient resource usage connection Requires C++ integration Zig ADR-003 Choice SQLite Only 2026-03-08 embedded database zero-config deployment ACID guarantees high reliability vertical scaling SQLite storage no other database adapters Embedded database single-file storage no installation external server Full support data integrity guarantees 20 years Easy backup simple deployment-Text FTS5 extension efficient text indexing #3 Self-Hosting First #4 Predictable Costs #8 Predictable Performance Simplest operational model deployment No "hidden" database infrastructure costsNo horizontal scaling storage Requires single-writer constraint ADR-004). ADR-004 SQLite WAL Mode Concurrency 2026-03-08 SQLite single writer blocks readers limits throughput multi-core systems use SQLite Write-Ahead Logging (WAL) mode Multiple readers one critical for vertical scaling I WAL optimizes I/O patterns write performance Predictable Performance parallel reads CPU cores Faster write performance Writes serialized batching throughput ADR-005 Multi-threaded Core Engine 2026-03-08 single-threaded core utilize SQLite parallel read capability multi event loop uWebSockets Implement multi core with read/write separation Lock-free cache for parallel reads Mutex-protected writes SQLite connection pool (one reader per CPU 17x performance improvement (10k → 170k/sec on multi-core machines Matches performance characteristics Bun uWebSockets Full utilization system resources Predictable vertical scaling Significant architectural complexity single-threadedADR-006 No Horizontal Scaling 2026-03-08 v1.0 clustering/horizontal scaling No ZyncBase for vertical scaling server CPU Distributed state complexity out for v1 collaborative apps don't reach 100k users per server Simpler architecture faster development better reliability Vertical scaling to 100k+ connections sufficient 99% use cases Avoids performance overhead P2P/Raft/Paxos #8 Predictable Performance #3 Self-Hosting First Best-in single-node performance Simplest operational model limit ~100k users per node Future horizontal scaling external tools ADR-007 Optimistic Writes Default 2026-03-09 Client SDK handle real-time state updates optimistic default Zero-latency performance real-time collaboration patterns Simplifies UI code API Real-time First Instant UI feedback Requires automatic local state revert server rejection Errors via global event listeners ADR-008 Server-side Validation 2026-03-09 Client SDK replicate server validation logic catch errors No Validation enforced serverClient SDK uses TypeScript types run runtime validation Prevents version-coupling Keeps SDK lightweight Server validate for security client validation redundant #9 Secure by Default - #5 TypeScript-First Smaller SDK size No "stale schema" bugs Errors after server round-trip ADR-009: Configuration-First (Zero-Zig) 2026-03-09 barrier to entry Zig ZyncBase follows "Zero-Zig" philosophy functionality managed via JSON configuration files No Zig knowledge required Mimics Nginx PostgreSQL Lowers barrier to entry for JS/TS developers [Zero-Zig Philosophy Core Principles #3 Self-Hosting First - #5 TypeScript-First No compilation instant setup Less flexible than code-based config-specific authorization inputs from trusted identity claims ADR-010: Prisma-Inspired Query Language 2026-03-09 query language natural web developers Prisma-inspired JSON syntax lowercase operators). TypeScript-first Clean syntax no prefixes complex nesting for queries#5 TypeScript-First Familiarity ease use Custom format SQL GraphQL ADR-011 Lock-Free Cache Atomic Reference Counting 2026-03-09 In-memory cache high-frequency reads threads contention Utilize atomic reference counting high parallel reads Simple implement RCU hazard pointers proper atomic operations global mutex fallbacks unacceptable #8 Predictable Performance ADR-012 Iterative MessagePack Parser 2026-03-09 Risk stack overflow malicious client payloads deep nesting iterative MessagePack parser limits nesting payload size #9 Secure Default #8 Predictable Performance ADR-013 MessagePack Production JSON Debug 2026-03-09 Wire protocol efficient accessible debugging MessagePack production JSON mode debug #8 Predictable Performance ADR-014 No WebSocket Compression (v1.0) 2026-03-09 per-message compression No compression v1.0 MessagePack compact Compression adds CPU overhead latency#8 Predictable Performance ADR-015 Strict Client API Namespaces`store 2026-03-09 Developers confuse ephemeral data durable state SDK separates methods.store.presence choice boundary Prevents misuse presence data #5 TypeScript-First ADR-016 Bun Hook Server 2026-03-09 complex relational authorization bloating Zig core-box Bun-based Hook Server complex logic Stateless checks `authorization.json #7 Declarative Security #5 TypeScript-First Unlimited flexibility TypeScript Protects Zig core logic complexity Small latency penalty~1-2ms delegated checks ADR-017 Conflict Resolution Strategy 2026-03-09 Fundamental semantics `store.set() concurrent users Server-Time Last-Write-Wins) at Path Level Matches REST/Firebase semantics Eliminates field-level merging core engine-scaled server guarantees global order Real-time #8 Predictable Performance ADR-018 Query API MVP Scope 2026-03-09 MVP supports operators excludes Regex FTS complex joins v1 Regex introduces ReDoS limits Guarantees predictable performance loop #8 Predictable Performance #9 Secure Default Developers rely computed properties application-side search complex search ADR-019 SDK-Friendly Syntax Compact Wire Format 2026-03-09 2026-03-31 SDK targets TypeScript developers expressive nested syntax-notation Prisma-style query Zig server benefits flat positional data without recursive descent string-keyed map inspection SDK developer syntax wire protocol uses compact positional arrays SDK before transmission SDK accepts.u1name wire sends[0 2 ADR-025 dictionary SDK accepts age 18 wire sends[3, 4 18[field_index op_code value SDK accepts created 'desc wire sends[8 1[field_index desc_flag SDK accepts'users.u1.address.city flattens `address__city sends mapped integer `field_index [Query Grammar full wire encoding operator codes Paths map SQLite lookups eliminates ambiguity IDs Flat condition tuples eliminate recursive JSON parsing reads fixed-position array Integer operator codes avoid string comparison map Zig `Operator enum Nested path flattening`address.city `address__city TypeScript #5 TypeScript-First #8 Predictable Performance ADR-020 Performance Targets 2026-03-13 benchmarks prevent performance regressions Establish targets ZyncBase v1.0.Metric Target Measurement Concurrent connections 100,000+ Sustained Requests/second 200,000+ Mixed workload Latency (p50 < 1ms In-memory ops Latency (p99) < 10ms Including disk Memory per connection < 1KB Excluding buffers Binary size < 15MB Cold start time < 100ms ready #8 Predictable Performance ADR-021: Fine-Grained Subscription Invalidation 2026-03-09 fine-grained change detection in-memory AST evaluation Avoids O) scaling-running SQL queries Sub-100ms sync disk loop Real-time First #8 Predictable Performance ADR-022 Formal Error Taxonomy Handling Strategy 2026-03-13 6-category error taxonomy automatic SDK behavior #5 TypeScript-First #9 Secure Default ADR-023 Unified Subscription Engine Operations 2026-03-31 ZyncBase defines 4 SDK read commands 4 message types handler splits reads dual code paths overlapping operationspath-based commands collection-based ADR-021 mandates in-memory AST evaluation subscriptions extends unified read Introduce single Subscription Engine server-side read operations Protocol — 4 → 2 Read Message Remove `StoreGet `StoreListen `StoreUnlisten reads through `StoreQuery `StoreSubscribe` Retained `StoreUnsubscribe` `StoreLoadMore `StoreDelta` Path Decomposition** translates `get into `StoreQuery`StoreSubscribe paths collection-level queries id filters `get('users.u1.name becomes `StoreQuery( SDK extraction maintains local value cache serves exact-match queries active subscription covers data Replaces split between `StorageEngine `SubscriptionManager` `StoreQuery` if collection warm evaluate from memory cold query SQLite one-shot queries `StoreSubscribe` from SQLite cold register subscriber push deltas-transition evaluation `RecordChange` events update in-memory state evaluate against subscriber groups push `StoreDelta` to matchesClients identical share evaluation per-subscriber metadata New subscribers receive snapshot-Level Deltas Server broadcasts granularity No field-level patches SDK handles field projection-Free Cache Subscription engine replaces lock-free cache application data reads permission snapshots schema metadata Collection evicted last unsubscribe Disconnect triggers auto-unsubscription Configurable `subscriptionEngine.maxMemoryMB New subscriptions_EXHAUSTED limit approached`loadMore hits SQLite disk In-memory SQLite `loadMore SDK subset query matching specialized per-field index structures Real-time First TypeScript-First Predictable Performance Narrows ADR-005 Lock-free cache auth/schema metadata Extends ADR-021: In-memory evaluation covers all reads Wire protocol reads halved (4 Single server-side code path Subscriber grouping evaluation per subscription not client SDK local cache eliminates redundant round-trips First subscribe cold collection incurs SQLite read `loadMore hits disk Subscription engine significant implementation effortADR-024 Canonical Sorted-Set Semantics Typed Array Fields 2026-04-17 Typed array fields tags labels roles ZyncBase introduced canonicalization (sort dedupe) developer experience determinism runtime performance formal architectural contract ZyncBase treats schema-defined typed arrays as canonical sorted sets-path Validate element against `items type Reject `null nested arrays objects Sort type-aware comparator Remove duplicates Persist canonicalized representation SQLite Return canonicalized arrays reads `contains array fields over canonical set content `eq` canonical equality semantics `in `notIn` operand arrays canonicalized during parsing-insensitive Arrays objects unsupported Unsupported operators array fields invalid Better DX for tag-like fields Deterministic storage comparisons Faster membership/equality behavior SQL in-memory evaluation #5 TypeScript-First #8 Predictable Performance ADR-025 Schema Dictionary Compression Routing) 2026-04-18 ZyncBase handles high message throughputServer-side routing string-based collection field names requires allocating strings computing hashes map lookups for read write operation Sending repeated strings increases bandwidth usage Replace schema-defined string with dense integer mappings over wire for `Store operations connection server pushes message positional arrays tables fields Runtime client SDK builds-memory string-to mapping runtime SDK resolves developer string paths into integer arrays before sending server Operation values use integer-keyed maps updates without payload ambiguity APIs exempt from optimization use string-based properties lack schema definitions by ADR-033 participates in integer-index wire encoding via schema.json SDK hashes `SchemaSync payload schema modifications operation queues against index shifting Transforms O(N) string hashing map lookups into O(1) direct array assignments Memory allocations for strings parsing logic removed Exchanging 1-byte integers via MessagePack instead repetitive strings reduces payload footprintSending dictionary arrays server older mobile clients adapt to injected fields without #8 Predictable Performance #1 Real-time First #5 TypeScript-First ADR-026 Internal Namespace Dictionary Routing for Namespaces 2026-04-24 Namespaces dynamic strings-123 Storing in SQLite wastes space 300MB per 10 million makes index lookups slower namespace mapping to `config.json impossible dynamic Implement hidden internal system table_zync_namespaces (id KEY name Reserve ID `0` for global namespace client-created namespaces use positive IDs wire protocol untouched client sends namespace Zig engine upserts namespace string table connection caches integer ID WebSocket connection uses for SQLite_id reads writes reduces database size performance gain SQLite index lookups Zero friction developer Client SDK Wire protocol stateful optimized strings sent per data #8 Predictable Performance #5 TypeScript-First ADR-027 `owner_id` System Column Stateless Authorization Limits 2026-04-24 secure multi-tenancy object-level ownership authorizationdraw line complexity JSON authorization rules real-time performance Add `owner_id` system column storage tables alongside `id` `namespace_id `created `updated_at` Store `owner_id` same `doc_id` as `id`(16) UUIDv7) never external identity string Implement implicit `users` system table maps external string identity claims `external_id` column to internal ZyncBase `id`(16) UUIDv7) Keep `owner_id` on `users` for table-shape consistency equal to `id` session resolution Zig engine maps external identity string to internal UUIDv7 `$session.userId` `owner_id` `BLOB(16)` populate `owner_id` with internal$session.userId` upon document creation Treat `id` as document identity collection unique across collection/table `namespace_id` not part primary key routing/filtering column limit `authorization.json` evaluation Zig to five variables `$session`$namespace`$path` table$doc`$value` `$doc` reference columns on target row selected updated.rule requiring relational join traversal lookup table forbidden in JSON permissions identity claims same row `owner_id` enables code-free object-level security `users mapping table guarantees compact 16-byte binary format ZyncBase `doc_id representation saving ~20 bytes per external string IDs `owner_id on `users avoids table-shape exceptions DDL generation authorization defaults replication payloads query planning self semantics identity `PRIMARY KEY(id) avoids composite key fan-out cursor tie-breakers SDK APIs Namespaces constrain visibility redefine document identity Strict limits evaluation context guarantee rule evaluation same-row SQL predicate injection "slow query Primitives over Magic `users` standard collection Predictable Performance #9 Secure by Default ADR-028 Global Master Data false 2026-04-25 ZyncBase collections partitioned by `namespace_id SaaS applications require master data tables transcend namespace boundaries visible globallyIntroduce"namespaced boolean primitive in.json definitions storage tables retain standard columns `namespace_id `owner_id `namespaced` changes_id not column `namespaced` omitted defaults to `true Zig stores active namespace ID in filters reads/writes by `false` stores global namespace ID `0 in routes queries through `users` collection defaults to"namespaced false `users configured with"namespaced true for tenant-isolated identity realms external identity mapping scoped by(namespace_id namespace switching$session.userId Provides flexibility define global data collections without server-side logic Avoids duplicating data across Maintains "Secure by Default" semantics namespacing unless opted-out Preserves uniform table shape null-heavy special cases field indexes DDL generation migrations authorization predicates consistent Avoids `namespace_id = 0 predicates in hot paths Each table one namespace predicate active namespace for namespaced tables `0` for global tablesPrimitives Magic SQLite Source Truth ADR-029 Scoped Session Readiness Gate 2026-05-05 ADR-026 established integer namespace routing ADR-027 established `owner_id internal `users.id ADR-028 allowed `users.namespaced = true` invariant WebSocket transport before scoped context store presence operations `users.namespaced true external identity string different `users.id per namespace identity resolution hardcoded anonymous UUID transport-only state Distinguish transport connectivity scoped session readiness Store external identity string connection anonymous SDK-generated authenticated clients JWT subject Resolve operation-domain scope SQLite before accepting scoped operations scope namespace string_namespaces.id external identity `users.id resolved `users.id scope$session.userId Store presence maintain separate scopes namespaces differ Store operations require scope ready Presence scope ready `users.namespaced false identity resolution uses global namespace ID `0` same `users.id `users.namespaced = true uses namespace ID scope resolved Store namespace switching re-resolves user ID presence namespace user IDBefore scope ready server accepts lifecycle messages refresh authentication store presence ping/pong close Store query subscription presence rejected `SESSION_NOT_READY Namespace identity changes invalidate scoped state Store namespace clear subscriptions Presence namespace clear subscriptions Auth refresh updates session active scopes continue auth refresh fails connection terminated Preserves `users.namespaced = true identity Ensures `owner_id presence `userId.id rows Keeps SDK ergonomics simple( storeNamespace presenceNamespace after scopes ready Avoids anonymous-user shortcuts SQLite foreign keys authorization auditability #1 Primitives over Magic #3 SQLite Source Truth Secure by Default ADR-030 Async Session Resolution-Blocking Reactor Handoff 2026-05-06 ADR-029 scoped session readiness gate `StoreSetNamespace triggers namespace user identity resolution implementation uses `CompletionSignal.wait() Mutex+Condition blocking writer thread resultblocks reactor 500μs–2ms per resolution stalling WebSocket connections violating p50 < 1ms latency target (ADR problem specific `StoreSetNamespace `PresenceSetNamespace `AuthRefresh other operations accepted writes (ADR-031) fast reader-pool reads code path blocking round-trip writer thread uWS reactor Replace blocking `CompletionSignal.wait() resolution with two-tier strategy in-memory identity cache case async SPSC ring buffer event loop wakeup handoff cache misses-tier 1 (sync Check lock-free namespace user identity caches uWS thread hit set scope respond zero I/O blocking 2 (async non-blocking cache miss enqueue `resolve_session` WriteOp writer thread return without response uWS reactor free Two `lockFreeCache` instances `StorageEngine `namespace_cache`(namespace_string → `namespace_id Populated thread after Immutable `identity_cache`_id → `users.id` Populated writer thread afterImmutable set ID mappings caches reuse `lockFreeCache` epoch-based reclamation atomic ref-counting COW map swaps Values trivial heap `deinit` no-op `resolve_session` Replaces_namespace `resolve_user WriteOp Performs namespace user resolution single reducing zero`SessionResolutionBuffer SPSC ring buffer (capacity 256 `ChangeBuffer thread pushes structs uWS post_handler drains`SessionResolver new component `NotificationDispatcher uWS post_handler drains `SessionResolutionBuffer acquires target connection ID applies resolved scope encodes success/error response calls `ws.send()`scope_seq` monotonic per-connection counter incremented scope reset Carried `resolve_session WriteOp checked delivery time If_seq match result result stale `StoreSetNamespace discarded capacity rationale (256 + 512 overflow `StoreSetNamespace` lifecycle message ~1x per connection session not hot data pathswriter thread processes ~5,000–10,000 resolutions/second post_handler drains uWS loop Worst-case (100k buffer accumulates ~10–50 results between drain cycles 256 provides 25x headroom over worst case ring buffer full results spill into overflow ArrayList (capacity ring buffer (256) overflow (512) exhausted 768 writer logs error drops result client retries via SDK timeout combined capacity >150x headroom over worst-case load 1 eliminates I/O After first resolution connections resolve ~1μs lock-free cache no reader mutex SQLite writer thread 2 eliminates reactor blocking cold New pairs resolve without blocking reactor response delivered asynchronously `ChangeBuffer_wakeup_loop() `post_handler pattern resolution reduces WriteOp variants eliminates round-trips cache miss **ADR-029 correctness `store_ready` remains `false until resolver delivers result Messages before resolution receive `SESSION_NOT_READY` Connections disconnect during async resolution `SessionResolver uses `ConnectionManager.() discard results disconnected clients identical `NotificationDispatcher pattern Extends ADR-026 Namespace string integer resolution lock-free cache first writer thread second Extends ADR-029 Scoped session resolution non-blocking uWS reactor Extends ADR-011 Lock-free cache namespace user identity mappings auth/schema metadata Zero blocking uWS reactor session resolution Common case cache resolves ~1μs 500x faster blocking path Simpler WriteOp surface_namespace_user_session connections unaffected cold-start resolution Async response delivery adds ~1 event latency cache misses Two lock-free cache instances add memory proportional unique user pairs ADR-031: Mutation Acknowledgement Realtime Consistency Semantics 2026-05-25 ADR-007 Optimistic Writes Default ZyncBase realtime-first database Client-visible state subscriptions changes visible server-pushed subscription deltas Mutation acknowledgement semantics explicit writes asynchronous server-side write pipeline mutation accepted before thread committed subscription update after mutation response distinct TypeScript SDK update local subscription data mutating operations.set.remove.batchsubscription state changes server emits updates write failure reporting not needed for rollback needed for user feedback diagnostics workflows precise write outcome reporting ZyncBase uses subscription-first state propagation default consistency model for client data Default mutation methods.set.remove.batch mutate local subscription state successful mutation response means server accepted request write committed Subscription callbacks authoritative committed state infer write failure subscription delta ZyncBase distinguishes two confirmation levels```ts
await store.set(path, value);
await store.set(path, value, { confirm: "accepted" });
// Resolves when the server accepts the mutation into the write pipeline.

await store.set(path, value, { confirm: "committed" });
// Resolves when the writer commits the mutation or an accepted no-op.
// Rejects when the writer reports failure.
```"accepted default represented SDK types examples prefer no-options form "committed applies mutating operations.set.remove.batch server parsed validated authorized before accepted mutation write pipeline **committed**-thread result succeeded accepted no-op outcomes **request/response correlation responses **writeId** correlation tracked writer-thread outcomes **WriteError** asynchronous writer failure message SDK event public protocol SDK documentation use "NACK Writer failures errors not rollback commands **Immediate Request reject mutation before owns operation Examples malformed messages invalid paths payload shapes unknown tables fields session not ready authorization failures queue admission failure request handling failures before writer ownership Confirmed write failures immediate request failures SDK error type `ZyncBaseError metadata phase```ts
{
  phase: "accept" | "write"
}
```Outcome Writer-thread failures as errors subscription state rollback instructions Tracked confirmed writes use `writeId correlate outcomes existing request `id scoped response confirmed writes SDK promise public success signal ZyncBase expose public `WriteCommitted event core API confirmed promise resolves no-op rejects failure confirmation received `WriteError public async failure name```ts
{
  type: "WriteError",
  writeId: "...",
  code: "PERMISSION_DENIED",
  message: "...",
  details: {
    phase: "write"
  }
}
```best-effort metadata single `set include batch failure operation include `path `batchIndex transaction-level systemic failure `path omitted Global write-error events tracked systemic failures untracked writes receive per-operation async error delivery acceptance Commit confirmation subscription delivery not ordered confirmed write resolves subscriptions receive state change subscription filtering connection state SDK guarantee subscription callback before write resolves Subscriptions authoritative committed-state channel Write confirmation outcome reporting not state delivery.batch atomic resolves full batch commits no-op outcomes rejects operation fails exposes no partial success commit partial writes failure Batch failures include `batchIndex failing operation identified```ts
{
  code: "PERMISSION_DENIED",
  message: "...",
  details: {
    batchIndex: 2,
    phase: "write"
  }
}
```transaction-level failures no operation `batchIndex` omitted Default `store.batch()` follows same contract writes **Authorization Write authorization rules preserve invariant client create document same session update same write rules `StoreSet` `$doc` write rules interpreted by write kind candidate document existing document existing document Create authorization evaluated RAM against candidate document candidate document includes document id injected `owner_id = $session.userId` incoming normalized fields server-managed fields defaults `owner_id` injected server ownership rules pass creates owning session```json
{ "$doc.owner_id": { "eq": "$session.userId" } }
```create rule references$doc field absent document not server-injected defaulted create denied Update delete authorization guarded against rows ZyncBase require post-image authorization for updates user update document state rule write rule "draft update from `draft to `published after transition allows updates-Op Deleting missing document success/no-op Setting field document same stored value success ZyncBase suppress deltas value Applications rely on delta for same-value writes committed write client not write error write no delta subscription targets data change subscription result Default accepted writes perform extra storage reads classify zero-row outcomes Confirmed writes classify guarded zero-row outcomes produce errors row fails write guard `PERMISSION_DENIED absent row delete success/no-op storage transaction failure error **Connection Failure Operation status retained in memory scoped originating connection for tracked confirmed writes ZyncBase retain write-status records across disconnects connection drops before confirmed write result SDK rejects promise with connection error write commit reconnect subscriptions remain source truth for final state Confirmed writes need timeouttimeout confirmation received imply write aborted failed commit ZyncBase guarantee abort after write accepted Automatic idempotency keys for retried writes outside decision Some operations idempotent retry requires separate idempotency design Systemic writer storage failures engine health events not If processing new writes fail during acceptance engine unhealthy Confirmed writes reject with `ZyncBaseError server status degraded state Write errors retryability transient operational failures retryable access denied invalid values schema errors constraint violations not retryable disk full encourage automatic client retry loops decision separates concepts request acceptance mutation state observation write outcome reporting optional operation status error feedback preserves ZyncBase realtime-first model precise feedback mutation wait for commit request/response database latency for subscription Restoring local writes rollback conflict handling complexity Write errors valuable user feedback diagnostics not local state repair Keeps default writes low latency Preserves subscriptions for state Avoids rollback machinery precise committed-write confirmation clear async failure reporting modelKeeps request ids writer outcome ids separate `await store.set() documentation explicit Applications confirmation request committed confirmation Confirmed writes add writer-result correlation timeout handling Confirmed authorization errors require classification work mutation wait for writer commit changes latency Restore optimistic local writes require rollback conflict handling complex semantics report writer-thread failures Missing subscription deltas ambiguous save failure feedback Use NACKs as rollback messages no speculative local subscription state Writer failures write-status errors not rollback commands Reuse request id as writer outcome id Request ids correlate responses Writer outcomes need separate `writeId for confirmed writesADR-032 Config-Driven Authentication External Permission Claims 2026-06-02 Supersedes-016 Bun Hook Server Modifies [ADR-027 `owner_id System Column Stateless Authorization Limits Modifies [ADR-029 Scoped Session Readiness Gate ZyncBase needs secure authentication anonymous access tenant/project authorization object ownership without identity provider arbitrary application logic previous Bun Hook Server design allowed TypeScript permission checks extra runtime internal protocol latency circuit-breaker behavior blurred product boundary ZyncBase resource server validates identity enforces declarative rules compute source truth user accounts memberships billing state permission grants Remove Bun Hook Server support Keep HTTP ticket exchange native configuration-driven ZyncBase validates external JWTs issuers audiences algorithms shared secrets JWKS sources ZyncBase JWT claims$session defines JWT claim names$session variables Permission claims trusted token signature registered constraints validated Anonymous access same ticket pipelineSDK generates anonymous subject persists presents anonymous external identity auth enabled `users` table internal identity mapping optional profile/display-data table maps external subject to `BLOB(16) UUIDv7 `owner_id$session.userId presence identity foreign keys User-row fields not loaded for authorization part of$session `authorization.json limited to RAM checks over$session$namespace$path$value same-row$doc` predicates store query predicate model permission requiring joins relationship traversal API calls billing lookups permission computation represented before ZyncBase request trusted token same-row data application code Token refresh revocation permission-update mechanism ZyncBase revalidates replacement JWT updates session active scopes continue JWT invalid connection terminated server-side revocation list Tokens short-lived ≤15 SDK refresh before expiry token expires no refresh server terminates connection Tenant project switching new JWT compatible scoped grant arrays large changing permission sets mint narrower active-context tokens Self-Hosting First no required process extra runtime TypeScript-First applications use JS/TS identity layer ZyncBase run#7 Declarative Security ZyncBase authorization in JSON #8 Predictable Performance no foreign calls joins hidden database reads Positive Removes Hook Server runtime hook protocol circuit-breaker failure mode WebSocket authentication safe JWTs URLs authorization deterministic fast permission ownership explicit external identity provider owns permission ZyncBase enforces trusted claims anonymous users low-friction without database tables Negative ZyncBase answer permission questions token target row incoming value revocation depends short tokens refresh behavior Large permission graphs compressed into-manageable claims groups roles active-context tokens before ZyncBase parsing bloat maximum limit claim array elements 1000 items JWTs Bun Hook Server for advanced authorization second runtime adds latency availability permission computation Load `users` row fields$session authorization hidden database reads refresh semantics second permission source identity mapping ownership references optional profile data not authorization input richer JSON relationship queries same-row predicate limit Require new JWT for every namespace switchtoken scoped grants `read `write_projects `authorization.json` check namespace parts same-row fields against arrays Applications choose narrower active-context tokens when token size staleness matters ADR-033 Typed Two-Tier Presence System 2026-06-11 Removes presence exemption clause from [ADR-025: Schema Dictionary Compression ADR-025 established integer-index wire routing for Store operations exempted presence"Presence APIs exempt from optimization use string-based properties lack formal schema definitions." presence designed as freeform schemaless JSON blob indexing machinery exist Three factors make exemption obsolete design presence **Presence unimplemented server zero no variants SDK methods implemented migration cost zero-world presence data structurally predictable use cases data shallow (≤2 small (5–8 fields), typed not arbitrary JSON blobs benefits from integer-index compression at high frequency **TypeScript-first developers need types ZyncBase targets TypeScript frontends Developers must define presence field types for UI`presence` schema section aligns server enforcement with type definitions redundancy analysis presence revealed gap in single-tier design not all ephemeral state user-owned slide video playback viewport for represent namespace-level state one value for room not per user Without second tier developers misuse store or build out-of-band coordination mechanisms **Typed presence schema server defines.json explicit `presence` top-level key authoritative key absent synthesizes implicit schema no schemaless or freeform mode presence data typed against schema **Two-tier field model `presence.user` fields owned by each user One record per user per namespace cleaned on disconnect `presence.shared` fields namespace-level ephemeral state One record for entire Survives namespace session lifetime **Flat wire format with `__` separator Nested presence fields flattened using same convention as store SDK flattens outbound unflattens inbound data Developers interact with nested object formnesting depth supported to 500 flat fields per-keyed wire encoding presence messages `PresenceSet` send integer-keyed maps `PresenceBroadcast `SharedStateBroadcast deliver integer-keyed data `SchemaDictionary machinery extended with presence-specific index maps `SchemaSync` arrays`SchemaSync` extension Two new flat arrays added `presenceUserFields string `presenceSharedFields string flattened names No companion flags arrays needed fields no system columns doc_id semantics field indices map to plain typed values semantics tiers `PresenceSet perform field-level merges not replacements 0 101.0 updates `cursor__x` other fields preserved server maintains per-user-namespace applies incoming integer-keyed maps as patches push message types `PresenceBroadcast` carries user presence events `SharedStateBroadcast` carries namespace-level shared state changes different SDK handling paths local cache targets separate types state lifetime grace period cleared when users leave namespaceShared state RAM-only survive server restart grace period 5-second grace period prevents disconnections wiping shared state grace period enforced 50ms batch flush loop `namespace_empty_at` map records user count drops to zero flush cycle evicts entries older than grace threshold No additional timer infrastructure user rejoins within grace period preserved shared state`presenceSharedWrite` authorization rule new rule key `authorization.json controls-level shared state RAM-only against$session$namespace$data$data` variable exposes incoming field values content-gated authorization hosts set `status **Presence writes fire-and-forget Neither `confirm` option accepted server validates types field indices rejects invalid payloads `SCHEMA_VALIDATION_FAILED` No write pipeline confirmation for ephemeral state **Wire Protocol Changes** Complete message specifications in ProtocolSummary Message Change `SchemaSync Gains `presenceUserFields string `presenceSharedFields string `PresenceSet changes string-keyed to integer-keyed map `PresenceSetShared Sets namespace-level shared fields integer-keyed merge delta `PresenceSubscribe Returns user snapshot shared state `PresenceSubscribeShared `PresenceBroadcast changes to integer-keyed map `userId `bin16 `subId integer `SharedStateBroadcast Carries integer-keyed shared state delta to subscribers **Authorization```json
{
  "namespaces": [
    {
      "pattern": "workspace:*",
      "storeFilter":        true,
      "presenceRead":        { "$session.userId": { "ne": null } },
      "presenceWrite":       { "$session.userId": { "ne": null } },
      "presenceSharedWrite": { "$session.role":   { "in": ["host", "presenter"] } }
    }
  ],
  "store": []
}
````presenceSharedWrite defaults same omitted consistent with safe defaults presence shared state unless restricted Presence fields simpler than store collections document IDs queries integer-index encoding easier than store not payload savings substantial at high frequency cursor update status shrinks from ~50 to ~22 bytes At 60fps difference compounds across broadcast path server benefits array index access replaces string hashing lookups `decodeIntMap pattern reused Shared state fills product gap Without developers pollute store with ephemeral room state stale data or build custom signaling outside ZyncBase grace period 50ms flush loop costs zero infrastructure runs_empty_at check namespaces zero normal operation$data in presence auth rules consistent with$value store write rules incoming integer-keyed map decoded rule evaluator before write accepted Real-time First TypeScript-First Predictable Performance Secure by Default Full integer wire compression for presence messages same performance as store Server-side field type validation at accept time operations Namespace-level shared state for room-wide ephemeral coordination$data auth enables content-gated presence writes Zero migration cost presence unimplemented TypeScript SDK generates types from presence schema Grace period shared state cleanup without cost Presence usable via implicit schema explicit `presence section in.json recommended for custom fields type safety changes trigger `schemaChange connected clients reconnect requirement store schema 500 flat fields per presence tier enforced at server startup pathological schemas Shared state RAM-only survive restart