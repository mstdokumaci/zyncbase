Architecture Decision Records ADR-001 Zig Implementation Language ZyncBase requires maximum throughput predictable latency load native multi-threading garbage collectors Use Zig primary implementation language core engine native code no garbage collector Zero GC sub-millisecond latency multi-threading** OS thread model maps CPU cores No green threads runtime scheduler overhead memory Specialized allocators Pool predictable allocation patterns zero-cost abstractions hot paths Statically linked output under 15MB no runtime dependencies Deployment file copy/C++ Zero-cost FFI integration uWebSockets SQLite C/C++ libraries wrapper overhead Best-in-class throughput tail latency server hardware Single binary deployment no external dependency management Manual memory management address sanitizer thread sanitizer P-RTF P-PPF ADR-002 Vertical Scaling Strategy database grow storage threading consistency decision ZyncBase scales all CPU cores single node Horizontal scaling out of collaborative applications reach 100,000 concurrent users per server achievable single nodesingle server provides write ordering no need vector clocks consensus protocols merge strategies load-bearing for conflict resolution Eliminating distributed state avoids performance overhead Raft/Paxos operational complexity multi-node deployments vertically-scaled single-node makes SQLite viable storage engine impossible multi-writer distributed system Best-in single-node performance minimal operational surface area upper limit 100,000 concurrent users per node Applications single node require external routing ZyncBase P-PPF P-SHF P-VSF ADR-003 Configuration-First Design (Zero-Zig) real-time database programming expertise target audience TypeScript/JavaScript developers not Zig programmers ZyncBase behavior controlled through JSON configuration files No knowledge Zig required configuration files.json server settings collection schemas field types definitions access control rules Nginx PostgreSQL configuration-driven systems edit config not source code application logic out Zig core preserves boundary ZyncBase configurable engine not embeddable library JSON configuration version-controlled diffable auditable JSON ZyncBase support configuration layerAuthorization logic joins dynamic computation encoded trusted identity claims before server Zero compilation instant setup pre-built binary Applications JSON configuration handle excess layer not ZyncBase P-SHF P-TSF P-DES ADR-004 Performance Targets binding architectural constraints ZyncBase not benchmarks design decision redesigned deferred Enforce hard targets| Metric | Target | Measurement |
| :--- | :--- | :--- |
| Concurrent connections | 100,000+ | Sustained |
| Requests/second | 200,000+ | Mixed workload |
| Latency (p50) | < 1ms | In-memory ops |
| Latency (p99) | < 10ms | Including disk |
| Memory per connection | < 1KB | Excluding buffers |
| Binary size | < 15MB | Stripped |
| Cold start time | < 100ms | To ready state |
P-PPF P-VSF ADR-005 SQLite Storage Engine ZyncBase needs zero-infrastructure ACID persistence operationally simple deployable single file saturating CPU cores SQLite sole persistent store WAL (Write-Ahead Logging mode No other storage backends adapters supported SQLite Zero configuration embedded single-file no external server Full ACID guarantees 20+ year production track record Operational simplicity backup file copy disaster recovery immediate Built-in FTS5 extension full-text indexing search engines SQLite saturates disk I/O before CPU vertical scaling WAL Mode Mandatory not tuning option default write blocks readers multiple readers operate single writer multi core engine viable implications Multiple readers serve queries writer thread active write load Write throughput improved sequential I/O patterns Writes serialized single thread batching No other storage backends added Zero-Zig deployment model (ADR-003) requires zero infrastructure dependencies Postgres Redis support constraint Zero-infrastructure deployment database fileNo horizontal scaling storage vertical-scaling strategy (ADR-002). P-SHF P-PRC P-PPF P-SOT ADR-006 Multi-Threaded Core Engine single-threaded core SQLite parallel read capability-005 multi-core capacity modern server hardware engine runs three thread domains writer thread reader pool SQLite connection uWS event-loop threads/O Metadata lock-free cache atomic reference counting Threading Topology **Writer receives mutations commits SQLite pushes change events Subscription Engine Serialization design SQLite supports one concurrent writer total write ordering architecturally valuable one thread per CPU core own SQLite connection WAL read mode cold queries `loadMore operations event-loop handle WebSocket events own reactor block Operations dispatched to thread reader pool results delivered back reactor via_wakeup_loop() Lock-Free Cache Metadata epoch-based reclamation atomic reference counting Copy-on-write map swaps allow writer publish updated versions without reader lock Atomic reference counting over RCU hazard pointers simpler metadata access patternslock-free cache Authentication snapshots JWT claim sets Schema metadata definitions collection shapes Namespace ID mappings string integer User identity mappings (external Per-document records keyed by_id table_index doc_id for authorization guard lookups Collection query results subscription state managed by Subscription Engine lock-free cache in collection reads write propagation Full CPU core utilization ~Nx throughput improvement to multi uWS reactor threads non-blocking I/O-dependent work dispatched returned asynchronously P-PPF P-VSF ADR-007 uWebSockets support microsecond-scale latency hundreds thousands concurrent connections ADR-004 uWebSockets (C++ library provides WebSocket transport connection management event-loop reactor Handles 200,000+ requests/second with microsecond-scale latency ZyncBase's throughput targets Under 1KB overhead per connection 100,000+ concurrent connections on standard hardware Powers Bun runtime used by Discord for millions connections Zero-cost FFI integration proven Bun'suWS event-loop threads entry point for network events own reactor block operations/O cross-thread coordination dispatched asynchronously results delivered reactor via_wakeup_loop() constraint governs session resolution design future blocking WebSocket handling by library C++ integration required Zig FFI P-RTF, P-PPF ADR-008 Wire Encoding real database processing 200,000+ messages per second text serialization recursive parsers CPU-intensive compression production wire format MessagePack JSON debug mode available parser iterative recursive WebSocket per-message compression disabled MessagePack binary serialization format smaller faster parse than JSON no schema pre-compilation required 200,000+ messages/second difference text binary encoding material JSON debug mode available chosen over Protobuf FlatBuffers no pre-generated stubs integrates with dynamic schema dictionary schema not known at compile time Iterative Parser (Security Constraint MessagePack parser iterative not recursive security requirement malicious client send nesting recursive parser overflow call stack parser enforces limits nesting depth payload size before processingNo Compression WebSocket per-message compression disabled MessagePack compact payloads 30–60% smaller than JSON Adding compression CPU overhead latency p50 < 1ms target (ADR-004). Smaller wire payloads than JSON without pre-compiled stubs Stack-overflow attacks impossible No compression overhead critical path P-PPF P-SBD ADR-009 Integer Routing Architecture high throughput string-based dominate wire bandwidth server-side parsing cost waste ZyncBase replaces string identifiers with dense integers Two routing systems serve identifier types static dictionary schema names dynamic dictionary for runtime namespace strings SDK translates string APIs into integers before transmission Static Schema Dictionary server `SchemaSync message collection field names system columns SDK builds in-memory string-to mapping runtime operations use Collection names integer indices Field names integer indices Operation values integer-keyed maps SDK hashes `SchemaSync` payload schema changes catastrophic index shifting Dynamic Namespace Dictionary Namespace strings runtime values:acme:project-123 pre-compiled static dictionary engine maintains hidden system table_zync_namespaces (id name reserved ID `0` for global namespace Runtime-created namespaces use positive IDs first use namespace engine upserts caches integer ID lock-free cache (ADR SQLite reads writes use integer `namespace_id column SDK sends namespace strings once engine handles mapping reduces namespace lookup cost saves storage 8 bytes per row 300+ bytes long namespace strings SDK Translation Contract developer syntax into compact wire format Developers write wire format directly| Developer writes | Wire sends |
| :--- | :--- |
| `'users.u1.name'` (path) | `[table_index, 'u1', field_index]` |
| `{ age: { gte: 18 } }` (query condition) | `[field_index, op_code, 18]` (positional tuple) |
| `{ created_at: 'desc' }` (sort) | `[field_index, 1]` (positional tuple, desc flag) |
| `'address.city'` (nested field) | field index for `address__city` (flattened with `__`) |
field paths flattened SDK `__` before integer indices server sees flat field names Presence fields participate integer-routing field index arrays `SchemaSync message presence-specific field index arrays Typed Two-Tier Presence System O(1) server-side field resolution array index access string hash bandwidth reduction 1–2 byte integers per field reference string Forward compatibility older SDK clients adapt updated `SchemaSync payloads Namespace storage reduced integer per row full string record P-PPF-RTF-TSF P-SOT ADR-010 Conflict Resolution Last Write Wins Multi-user collaboration two clients location write target ZyncBase(collection document_id field triple fields flat flattened `__` no sub-field partial updates write sets entire field value Concurrent writes resolved server-assigned timestamp last write wins single-node architecture provides write ordering processes mutations serially zero coordination overhead Field-level LWW O(1) Deep field-level merge equality traversal conflicts p99 < 10ms high write throughputLWW matches semantics REST PUT Firebase familiar no learning curve Two clients editing field overwrite server timestamp determines winner Applications requiring merge semantics use separate documents application layer Writing same value field no-op server subscription deltas same-value writes P-RTF P-PPF ADR-011 Data Ownership Namespace Tenancy Multi-tenant applications need object-level ownership namespace partitioning data model document carries `owner_id `namespace_id` system columns Collections opt out namespace partitioning global master data Authorization rules limited same-row predicates System Columns storage tables carry system columns `id` `namespace_id` `owner_id` `created_at `updated_at` No exceptions uniformity keeps DDL generation migration logic authorization predicates consistent across collections `id` document identity unique_id not primary key_id` tenant routing column `owner_id` stored `BLOB(16)` UUIDv7 populated on document creation.userId users Collection Identity Mapping system collection maps external string identitiesAuth0 `sub claims SDK-generated anonymous subjects `external_id` column internal UUIDv7 identities `id` column standard collection columns not hidden auth table session resolution engine maps external identity string to `users.id` stores$session.userId` `owner_id` values reference real `users.id` row `owner_id` equals `id` (self-ownership uniform table shape Global Master Data`namespaced: false` collections namespace-partitioned true Setting false` store `namespace_id = 0` reserved global namespace queries active namespace pattern master data visible across tenant namespaces `users` defaults `namespaced: false` Setting true creates isolated identity realms per namespace same external identity string maps distinct `users.id` each namespace mode for applications users one namespace namespace switching not supported user's identity resolved home namespace different namespace new unrelated identity row Applications `users.namespaced = true` JWT access single namespace `namespace_id = 0` routing transparent developer invisible in API Authorization Scope Limits `authorization.json rules evaluated against five variables$session$doc$value Cross-table joins relationship traversal lookups of collections forbidden in authorization rules permission encoded In validated JWT same row limit architectural boundary authorization evaluation latency zero hidden database reads `$doc.owner_id == $session.userId authorization rules work for ownership-based access control `users` collection inspectable queryable subscribable like Complex relational authorization permission trees in identity provider not ZyncBase P-POM P-PPF P-SBD P-SOT ADR-012 Typed Array Fields as Canonical Sorted Sets Schema-defined array fields for tags roles labels element order irrelevant duplicates nonsensical fields behave as canonical sorted sets Insertion order discarded server normalizes array to sorted deduplicated form write Validate element against type reject null elements nested arrays sort using type-aware comparator remove duplicates canonicalized representation stored in SQLite Arrays returned in canonical form`contains array field membership over set `eq / `ne` use equality equal for valid arrays `in` / `notIn` operand arrays canonicalized duplicate Arrays objects unsupported Operators semantically undefined sets invalid on array fields Eliminates B equal ambiguity storage Enables faster membership equality checks SQL-memory AST evaluation serves dominant array use case without developer opt-in Applications-order semantics array model ordered lists differently P-TSF P-PPF ADR-013 Query Language SDK targets TypeScript developers expressive type-safe syntax query language familiar safe performant ZyncBase uses Prisma-inspired JSON query syntax implicit AND lowercase operators comparison membership string operators Regex full-text search cross-collection joins excluded Syntax Design Query conditions as JSON objects implicit AND semantics```ts
// Single condition
{ status: { eq: "active" } }

// Compound condition (implicit AND)
{ age: { gte: 18 }, status: { eq: "active" } }

// Array membership
{ tags: { contains: "typescript" } }
```Prisma-style syntax TypeScript developers know widely used query API Implicit covers dominant case explicit structures composable No prefix notation no custom string parsing Supported Operators `eq `ne `gt `gte `lt `in `notIn `contains `startsWith `endsWith` Excluded Introduces ReDoS risk matching untrusted input denial-of-service-text search Requires FTS5 query planning incompatible Subscription Engine in-memory AST evaluation model **Cross-collection break subscriber grouping model evaluation **Computed/derived Application-side concern Developers full-text search use computed fields client-side filtering P-TSF P-PPF P-SBD ADR-014 Unified Subscription Engine sole entry point server-side read operations maintains warm in-memory state subscriptions evaluates change propagation in-memory AST evaluation re-running SQL queries alternative SQL re-evaluation per high connection counts untenable rejectedalternative separate code paths one-shot reads `StorageEngine path separate `SubscriptionManager query logic inconsistencies unified engine single read contract eliminates bug Read Message Types Two`StoreQuery one-shot warm memory cold SQLite return results warm engine`StoreSubscribe ongoing warm SQLite return snapshot register subscriber push `StoreDelta events until Additional messages `StoreUnsubscribe` `StoreLoadMore `StoreDelta` SDK Server Read Model server reads collection-level optional filters no sub-document paths Path-based SDK methods decompose into collection queries id filters field extraction result SDK Engine Lifecycle SQLite Return results No state **Subscribe (cold collection Warm SQLite register subscriber return snapshot (warm collection Register subscriber return-memory snapshot **RecordChange Update collection state evaluate subscriber groups push `StoreDelta matching subscribers Subscriber Grouping Subscribers identical collection share evaluation group `RecordChange event evaluates group once subscriber `limit per-subscriber metadata after evaluation New subscribers joining warm group receive current snapshotIn-Memory AST Evaluation Change re-runs SQL `RecordChange event engine evaluates changed record against subscriber group AST subscription propagation not Record-Level Deltas server pushes `set `remove No field-level patches SDK handles field projection cache updates Eviction Resource Limits Collection evicted last subscriber unsubscribes Disconnect triggers unsubscription `subscriptionEngine.maxMemoryMB limit new subscriptions `RESOURCE_EXHAUSTED `loadMore hits SQLite Single code path reads subscription groups evaluation per write cost not subscriber count First subscribe cold collection incurs one SQLite read subsequent reads in-memory P-RTF P-SAT P-TSF-PPF P-SOT Scoped Session Management WebSocket transport open before server context data Identity resolution before store operation accepted identity namespace resolution before accepts data operations Resolution non-blocking uWS two-tier cache async handoff strategy Transport Open ≠ Session Ready connectivity scoped session readiness distinct states Store presence maintain independent scopes resolved namespaces `users.namespaced = falsescope ready server accepts lifecycle messages authentication namespace selection ping/pong close data messages subscriptions queries mutations operations rejected with `SESSION_NOT_READY Scope resolved pair **Namespace namespace string resolved_zync_namespaces.id$session.userId external identity string resolved `BLOB(16) UUIDv7 `users table Store presence resolve independently each maintains own resolved namespace ID user ID `users.namespaced false global table external identity maps to same `users.id` namespace switching carries identity `users.namespaced true identity resolution scoped to active namespace mode for single-namespace users server enforces connection locked to single namespace store presence scopes first `SetNamespace establishes connection namespace subsequent rejected with `NAMESPACE_SWITCH_REJECTED if namespace string differs Non-Blocking Resolution Blocking uWS reactor connections p50 < 1ms latency target Synchronous cache hit Check namespace identity cache lock-free cache If hit set scope respond Zero I/O zero blocking After first resolution external_id pair subsequent connections same resolve via path2 Async writer handoff miss Enqueue `resolve_session WriteOp writer thread return reactor free writer thread performs `INSERT OR IGNORE_namespaces users populates caches pushes `SessionResolutionResult to `SessionResolutionBuffer `SessionResolver uWS `post_handler drains buffer acquires target connection ID applies resolved scope sends response Combining namespace user resolution single_session WriteOp eliminates two round-trips one Scope Invalidation Clears subscriptions domain **Auth refresh (success Updates$session` active scopes continue refresh (failure Connection terminated Stale Result Guard`scope_seq` per-connection counter incremented each reset_seq match result result stale namespace change discarded uWS reactor blocked during identity namespace resolution Warm-cache resolution~1μs makes repeated connections namespace switches free full TCP handshake → data-ready path async no reactor blockingP-POM-SOT-SBD-PPF ADR-016 Authentication Authorization Trust Boundary ZyncBase needs secure authentication multi-tenant authorization object ownership without identity provider application logic ZyncBase resource server validates JWTs projects verified claims into$session enforces JSON authorization rules run application logic perform joins auth identity provider runtime validation server-side JWT Authentication validates tokens external issuers Configuration specifies issuer audience algorithms shared secret or JWKS endpoint JWT claim-to-$session mapping defined configuration permission claims_id trusted token signature registered constraints validated Anonymous access same pipeline SDK generates high-entropy anonymous subject persists presents anonymous identity auth enabled No special database tables code paths required Trust Boundary trusts verify Permission decisions joins relationship traversal billing lookups permission graph computation encoded before request server verified JWT same-row data ADR-011 authorization scope product boundary decision ZyncBase declines compute permission truth application identity provider `authorization.Declarative RAM Rules Authorization rules evaluate against$session$namespace$path$doc$value No database reads during Rules JSON version-controlled diffable auditable Token Lifecycle Revocation Tokens short-lived (≤15 minutes SDK refresh tokens before expiry successful refresh$session updated active scopes continue refresh failure connection terminated ZyncBase server-side revocation list Token expiry primary revocation mechanism token expires without valid replacement connection terminated enforces maximum limit JWT claim array element counts prevent payload parsing bloat Server-Side Validation Schema validation field type enforcement authorization rules enforced by server SDK uses TypeScript types.json for safety no runtime validation SDK lightweight eliminates bugs validation alternative delegating authorization to external ZyncBase correctness second runtime per-check latency availability failure modes application-layer permission computation database boundary ZyncBase resource server permission truth belongs identity provider Authorization no foreign calls joins hidden database reads nanosecond evaluation external identity provider owns permission truth ZyncBase enforces trusted claimsApplications large permission sets compress into manageable claim structures narrower active-context tokens P-SHF-TSF-DES-PPF-SBD ADR-017 SDK API Surface `store vs `presence Durable ephemeral different consistency guarantees lifetime semantics storage backends Conflating causes bugs SDK enforces namespace boundary between.store.presence not interchangeable structural boundary not convention developer use presence for durable data SDK no API path choice forced at call site store/presence distinction visible in code example type signature Presence state scoped to Typed Two-Tier Presence System builds on namespace boundary P-TSF P-CIB ADR-018 Mutation Acknowledgement Consistency Semantics Client-visible state observed through subscriptions not write responses related distinct events semantics explicit Default writes.set.remove.batch accepted Subscriptions authoritative source committed client-visible state Clients opt in confirmation no optimistic local state no local rollback Three-Concept Separation concerns distinct **Request server mutation write pipelinesubscriptions delivered state outcome optional operation-level status error feedback Default writes participate (1) produce (2) block (3) model default writes low-latency Confirmation Levels```ts
await store.set(path, value);
// await store.set(path, value, { confirm: 'accepted' });
// Resolves when the server accepts the mutation into the write pipeline.

await store.set(path, value, { confirm: 'committed' });
// Resolves when the writer commits the mutation (or produces an accepted no-op).
// Rejects when the writer reports failure.
```'accepted default applies.set.remove.batch Terminology **accepted** parsed validated authorized pre-enqueue admitted mutation **committed**-thread result succeeded no-ops **writeId** correlation ID writer-thread outcomes Distinct request `id correlates accept/reject response **WriteError** async writer failure messages Not no speculative local state roll back Error Phases phases **accept** rejection before writer operation (malformed message invalid path/field schema validation failure session not ready pre-enqueue authorization failure queue admission **write**-thread failure after acceptance error constraint violation post-read authorization failure use `ZyncBaseError.phase 'accept discriminant Batch Semantics.batch 'committed atomic resolves full batch commits rejects operation fails no partial success partial writes failure Failed batch operations include `batchIndex transaction-level failures no operation culpable `batchIndex omitted Authorization Semantics$doc interpreted operation kind$doc candidate document_id existing stored document$doc document Ownership rules$doc.owner_id$session.userId pass session `owner_id injected before rule No-Op Semantics Deleting missing document success Writing same value to field success server suppress subscription deltas for same-value writes Connection Failures Confirmed write promises scoped to connection disconnect SDK rejects promises connection error write commit After reconnect subscriptions source truth for final state Confirmed writes require timeout timeout confirmation received **Rejected alternatives** **Make write block until committed** rejected changes adds latency **Optimistic local writes** rejected requires rollback conflict handling speculative materialized-view logic avoids **NACK messages as rollback commands** rejected no speculative local state **No writer failure reporting** rejected ambiguous missing deltas save-failure feedback **Reuse request id for writer outcomes** rejected correlate responses need distinct `writeId` Default writes resolve fast subscriptions deliver committed state save confirmation use `confirm: 'committed' SDK mutates local state subscriptions source of truthP-RTF-SAT-TSF-SOT ADR-019 Error Taxonomy SDK Error Handling Errors real-time connection failures authorization denials schema violations storage errors behavior SDK ZyncBase errors 8 categories dictates SDK retry escalation| Category | Examples | SDK Auto-Behavior |
| :--- | :--- | :--- |
| **Connection** | Network drop, server restart | Auto-reconnect with exponential backoff |
| **Authentication** | Invalid JWT, expired token | Trigger token refresh; reconnect if refresh succeeds |
| **Authorization** | Permission denied | Surface to application; do not retry |
| **State** | Session not ready, namespace switch rejected | Surface to application; do not retry |
| **Validation** | Schema violation, invalid payload shape | Surface to application; do not retry |
| **Client** | Path format invalid, message too large | Surface to application; do not retry |
| **Rate-Limit** | Too many requests | Respect server `retry-after`; retry after indicated delay |
| **Server** | Storage failure, engine error | Surface to application; optional retry |
ZyncBase error typed with `category `details `batchIndex 8-category model server-side classification SDK maps to typed error objects via `deriveCategory consumer categories `authentication `authorization `validation_limit `network `unknown Application code receives classified actionable error at error boundary developers handle meaningful error categories not raw status codes opaque strings SDK implements automatic retry behavior token refresh reconnect `retry-after without application involvement P-TSF P-SBD ADR-020 Typed Two-Tier Presence System real-time awareness layer ZyncBase freeform JSON blob performance type safety typed schema-driven integer-encoded Two tiers serve collaboration patterns per-user-level shared state presence messages same integer-routing architecture as store messages Typed Presence validated against schema `schema.json `presence` top-level key authoritative key absent server synthesizes implicit minimal schema```json
{
  "user": {
    "status": { "type": "string", "enum": ["active", "idle", "away"] }
  },
  "shared": {}
}
```no schemaless freeform presence mode data typed schema Two-Tier Field Model`presence.user fields owned user One record per user namespace cleaned on disconnect.shared fields namespace-level ephemeral state One namespace Survives namespace session lifetime two tiers address product gap not all ephemeral state user-owned slide video playback position viewport namespace-level state one value for room not per user Without second tier developers misuse store stale data or build out-of-band coordination mechanisms Flat Wire Format Integer Encoding Nested presence fields flattened `__` separator SDK flattens outbound reconstructs inbound data Developers interact with nested object form `SchemaSync includes two arrays field index encoding `presenceUserFields string flattened string `PresenceSet send integer-keyed maps `SharedStateBroadcast deliver integer-keyed data 500 flat fields per presence tier at server startup Payload savings high frequency cursor update status shrinks to ~22 bytes60fps users subscribers difference compounds across broadcast path Merge Semantics `PresenceSet` `PresenceSetShared` perform field-level merges not full replacements ` 0: 101.0 updates `cursor__x` other fields preserved Separate Push Message Types `PresenceBroadcast` carries user presence events`join `leave `SharedStateBroadcast` carries namespace-level state changes different SDK handling paths local cache targets separate message types Shared State Lifetime Grace Period cleared users leave namespace 5-second grace period prevents disconnections wiping state 50ms batch flush loop `namespace_empty_at` map records user count drops to zero evicts entries older No additional timer infrastructure required state RAM-only survive server restart Authorization `presenceSharedWrite` rule key.json namespace-level shared state `presenceWrite RAM-only$session$namespace$data`$data` exposes field values content-gated authorization `presenceSharedWrite` defaults same value when omitted consistent safe defaultsPresence Writes Fire-Forget accepted/eventual Type field index validation accept time invalid payloads rejected_VALIDATION_FAILED Write pipeline confirmation not meaningful ephemeral state Wire Protocol Summary| Message | Description |
| :--- | :--- |
| `SchemaSync` | Includes `presenceUserFields: string[]` and `presenceSharedFields: string[]` |
| `PresenceSet` | C→S; `data` is integer-keyed merge delta for user fields |
| `PresenceSetShared` | C→S; integer-keyed merge delta for shared fields |
| `PresenceSubscribe` ok | Returns current user presence snapshot for the namespace |
| `PresenceBroadcast` | S→C; `data` is integer-keyed map; `userId` encoded as `bin16` |
| `SharedStateBroadcast` | S→C; integer-keyed shared state delta |
integer wire compression presence messages same performance store operations Server-side field type validation accept operations Namespace shared state room-wide coordination without store pollution$data auth enables content-gated presence writes Shared state RAM-only server restart Presence schema changes trigger `schemaChange clients same reconnect store schema 500 flat fields per presence startup pathological schemas P-RTF-TSF-PPF-SBD-CIB