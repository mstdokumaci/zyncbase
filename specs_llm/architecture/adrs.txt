Architecture Decision Records decisions ZyncBase consolidated organized chronologically ADR-001 Choice Zig Primary Language 2026-03-08 Accepted maximum performance predictable latency native multi-threading real-time collaborative database Node.js introduce pauses unacceptable 100,000+ concurrent connections Use Zig primary language core engine 3-4x faster Node.js competitive Go No GC predictable latency-threading** support CPU cores mapping system threads **Memory Efficiency** Manual control zero-cost abstractions predictable memory usage Statically linked binary< 15MB no runtime dependencies simplifies deployment/C++ Zero-cost FFI integration uWebSockets C/C++ libraries #1 Real-time First #8 Predictable Performance Best-in-class performance predictable latency Single binary deployment Longer development time Manual memory management ADR-002: Choice uWebSockets Networking 2026-03-08 Accepted Standard networking libraries struggle latency memory overhead WebSocket connections Use uWebSockets C++ networking foundation-in-Class Supports 200,000+ requests/second microsecond-scale latency Powers Bun runtime used Discord millions connections Minimal overhead< 1KB per vertical scaling millions Successful integration Bun C++ integration Zig FFI zero wrapper overhead #1 Real-time First #8 Predictable Performance low latency high throughput Efficient resource usage connection Requires C++ integration Zig ADR-003 SQLite Only 2026-03-08 Accepted embedded database zero-config deployment ACID guarantees high reliability vertical scaling SQLite storage layer no other database adapters Embedded database single storage no installation external server Full transactional support data integrity guarantees 20 years Easy backup simple deployment-Text Built-in FTS5 extension efficient text indexing #3 Self-Hosting First #4 Predictable Costs #8 Predictable Performance Simplest operational model deployment No "hidden database infrastructure costsNo horizontal scaling storage Requires single-writer constraint ADR-004). ADR-004 SQLite WAL Mode Concurrency 2026-03-08 Accepted SQLite single writer blocks readers limits throughput multi-core systems use SQLite Write-Ahead Logging (WAL) mode Multiple readers one critical for vertical scaling I WAL optimizes I/O patterns performance Predictable Performance parallel reads CPU cores Faster write performance Writes serialized batching throughput ADR-005 Multi-threaded Core Engine 2026-03-08 Accepted single-threaded core utilize SQLite parallel read capability or multi-threaded event loop uWebSockets Implement multi core read/write separation Lock-free cache for parallel reads Mutex-protected writes correctness SQLite connection pool (one reader per CPU 17x performance improvement (10k → 170k/sec on multi-core machines Matches performance characteristics Bun uWebSockets Full utilization system resources Predictable vertical scaling Significant architectural complexity singleADR-006 No Horizontal Scaling 2026-03-08 Accepted v1.0 clustering/horizontal scaling No ZyncBase for vertical scaling server CPU Distributed state complexity out v1 collaborative apps don't reach 100k users per server Simpler architecture faster development better reliability Vertical scaling to 100k+ connections sufficient 99% use cases Avoids performance overhead P2P/Raft/Paxos Predictable Performance #3 Self-Hosting First Best-in single-node performance Simplest operational model limit ~100k users per node Future horizontal scaling external tools ADR-007 Optimistic Writes Default 2026-03-09 Superseded by ADR-031 Client SDK real-time state updates fluid experience optimistic default Zero-latency performance real-time collaboration patterns Simplifies UI code API Real-time First Instant UI feedback Requires automatic local state revert on server rejection Errors global event listenersADR-008 Server-side Validation 2026-03-09 Accepted Client SDK replicate server validation logic errors? No Validation enforced on server Client SDK uses TypeScript types run runtime validation Prevents version-coupling Keeps SDK lightweight Server validate for security client validation redundant #9 Secure by Default #5 TypeScript-First Smaller SDK size No "stale schema" bugs clients Errors discovered after server round-trip ADR-009: Configuration-First (Zero-Zig) 2026-03-09 Accepted High barrier to entry Zig ZyncBase follows "Zero-Zig" philosophy core functionality managed via JSON configuration files No Zig knowledge required Mimics Nginx PostgreSQL Lowers barrier to entry for JS/TS developers-Zig Philosophy Core Principles #3 Self-Hosting First #5 TypeScript-First No compilation instant setup Less flexible than code-based config authorization inputs trusted identity claimsADR-010 Prisma-Inspired Query Language 2026-03-09 Accepted query language natural modern web developers Use Prisma-inspired JSON syntax AND lowercase TypeScript-first Clean syntax no prefixes complex nesting simple queries #5 TypeScript-First Familiarity ease use Custom format standard SQL GraphQL ADR-011 Lock-Free Cache Atomic Reference Counting 2026-03-09 Accepted In-memory cache high-frequency reads threads Utilize atomic reference counting high-performance parallel reads Simple implement RCU pointers proper atomic operations global mutex fallbacks unacceptable #8 Predictable Performance ADR-012 Iterative MessagePack Parser 2026-03-09 Accepted Risk stack overflow malicious client payloads deep nesting Use iterative MessagePack parser limits nesting payload size #9 Secure by Default #8 Predictable Performance ADR-013 MessagePack for Production JSON for Debug 2026-03-09 Accepted Wire protocol efficient accessible for debuggingMessagePack production JSON debug #8 Predictable Performance ADR-014 No WebSocket Compression (v1.0 2026-03-09 Accepted enable per-message compression No compression v1.0 MessagePack compact Compression adds CPU overhead latency Performance ADR-015 Strict Client API Namespaces 2026-03-09 Accepted Developers confuse ephemeral data durable state SDK separates methods.store.presence Forcing choice creates Prevents misuse presence data #5 TypeScript-First ADR-016 Bun Hook Server 2026-03-09 Superseded [ADR-032]-driven-authentication-external-permission-claims complex relational authorization without bloating Zig core out-box Bun-based Hook Server complex logic Stateless checks `authorization.json #7 Declarative Security #5 TypeScript-First Unlimited flexibility TypeScript Protects Zig core business logic complexity Small latency penalty~1-2ms delegated checksADR-017 Conflict Resolution Strategy 2026-03-09 Accepted semantics.set() concurrent users Server-Time Last-Write-Wins (LWW) Path Level Matches REST/Firebase semantics Eliminates field-level merging Single vertically-scaled server guarantees global order #1 Real-time First #8 Predictable Performance ADR-018 Query API MVP Scope 2026-03-09 Accepted MVP supports operators excludes Regex FTS complex joins v1 Regex introduces ReDoS limits predictable performance #8 Predictable Performance #9 Secure Default Developers rely computed properties application-side search complex search ADR-019 SDK-Friendly Syntax Compact Wire Format 2026-03-09 2026-03-31 Accepted SDK targets TypeScript developers expressive nested syntax-style query Zig server benefits flat positional data without recursive descent string-keyed map inspection SDK developer-friendly syntax wire protocol uses compact positional arrays SDK before transmissionprinciple applies SDK accepts.u1.name wire sends[0 2 ADR-025 dictionary SDK accepts age gte 18 wire sends[3, 4 18[field_index op_code value SDK accepts created 'desc wire sends[8, 1][field_index desc_flag SDK accepts'users.u1.address.city flattens `address__city sends mapped integer `field_index [Query Grammar full wire encoding specification codes Paths map SQLite lookups eliminates ambiguity IDs Flat condition tuples eliminate recursive JSON parsing reads fixed-position array Integer operator codes avoid string comparison map Zig `Operator enum Nested path flattening`address.city `address__city TypeScript #5 TypeScript-First #8 Predictable Performance ADR-020 Performance Targets 2026-03-13 Accepted benchmarks prevent performance regressions Establish targets ZyncBase v1.0.Metric Target Measurement Concurrent connections 100,000+ Sustained Requests/second 200,000+ Mixed workload Latency (p50) < 1ms In-memory ops Latency (p99) < 10ms Including disk Memory per connection < 1KB Excluding buffers Binary size < 15MB Cold start time < 100ms ready #8 Predictable Performance ADR-021: Fine-Grained Subscription Invalidation 2026-03-09 Accepted fine-grained change detection in-memory AST evaluation Avoids O) scaling re-running SQL queries Sub-100ms sync disk loop #1 Real-time First #8 Predictable Performance ADR-022 Formal Error Taxonomy Handling Strategy 2026-03-13 Accepted 6-category error taxonomy automatic SDK behavior #5 TypeScript-First #9 Secure Default ADR-023: Unified Subscription Engine Operations 2026-03-31 Accepted ZyncBase defines 4 SDK commands 4 wire message typesmessage handler splits reads between `StorageEngine `SubscriptionManager creating dual code paths for overlapping operations path-based commands subsets collection-based ADR-021 mandates in-memory AST evaluation for subscriptions extends unified read Introduce single Subscription Engine entry point for server-side read operations Protocol 4 2 Read Message Remove `StoreGet `StoreListen `StoreUnlisten reads go through `StoreQuery `StoreSubscribe Retained `StoreUnsubscribe` `StoreLoadMore `StoreDelta` Path translates `get into `StoreQuery`StoreSubscribe paths collection-level queries id filters `get becomes `StoreQuery( maintains local value cache change detection serves exact-match queries when active subscription covers data Replaces split between `StorageEngine `SubscriptionManager `StoreQuery if collection warm evaluate from memory cold query SQLite-shot queries `StoreSubscribe SQLite cold register subscriber push deltas edge-transition evaluation `RecordChange eventsupdate-memory state evaluate subscriber groups push Clients identical share evaluation per-subscriber metadata New subscribers receive snapshot-Level Deltas Server broadcasts No field-level patches SDK handles field projection-Free Cache Subscription engine replaces lock-free cache application data reads cache permission snapshots schema metadata Collection evicted last unsubscribe Disconnect triggers auto-unsubscription Configurable `subscriptionEngine.maxMemoryMB New subscriptions_EXHAUSTED limit approached`loadMore hits SQLite disk v1 In-memory SQLite-start `loadMore SDK subset query matching specialized per-field index structures Real-time First TypeScript-First #8 Predictable Performance Narrows ADR-005 Lock-free cache auth/schema metadata Extends ADR-021: In-memory evaluation covers all reads Wire protocol reads halved (4 2) Single server-side code path Subscriber grouping evaluation per subscription not client SDK cache eliminates redundant round-trips First subscribe cold collection incurs SQLite read`loadMore` hits disk Subscription engine implementation effort ADR-024 Canonical Sorted-Set Semantics Typed Array Fields 2026-04-17 Accepted Typed array fields tags labels roles ZyncBase introduced canonicalization (sort dedupe) developer experience determinism runtime performance formal architectural contract ZyncBase treats schema-defined typed arrays as canonical sorted sets-path Validate element against `items type Reject `null nested arrays objects Sort elements type-aware comparator Remove duplicates Persist canonicalized representation SQLite Return canonicalized arrays `contains` array fields membership over canonical set content `eq` `ne` canonical equality semantics-equivalent `in` / `notIn` operand arrays canonicalized during parsing-insensitive Arrays objects unsupported Unsupported operators array fields invalid Better DX tag-like fields Deterministic storage comparisons Faster membership/equality behavior SQL in-memory evaluation#5 TypeScript-First #8 Predictable Performance ADR-025 Schema Dictionary Compression (Integer Routing) 2026-04-18 Accepted ZyncBase handles high message throughput Server-side routing string-based collection field names requires allocating strings computing hashes hash map lookups read write repeated strings wire increases bandwidth usage Replace schema-defined string with dense integer mappings wire for `Store operations server pushes message positional arrays tables fields system Runtime client SDK builds in-memory string-to mapping runtime SDK resolves developer logical string paths into integer arrays server Operation values use integer-keyed maps sparse updates without payload ambiguity APIs exempt from optimization use string-based properties schema definitions SDK hashes `SchemaSync payload detect schema modifications queues against catastrophic index shifting Transforms O(N) string hashing map lookups Zig into O(1) array assignments Memory allocations for strings parsing logic removedExchanging 1-byte integers via MessagePack reduces payload Sending dictionary arrays older mobile clients adapt to fields #8 Predictable Performance #1 Real-time First #5 TypeScript-First ADR-026 Internal Namespace Dictionary Routing for Namespaces 2026-04-24 Accepted Namespaces dynamic strings-123 Storing in SQLite wastes space 300MB per 10 million rows makes index lookups slower than namespace mapping to `config.json impossible dynamic Implement hidden system table_zync_namespaces (id INTEGER KEY name UNIQUE Reserve ID `0` for global namespace client-created namespaces use positive IDs wire protocol untouched client sends namespace via Zig engine upserts namespace string into table caches integer ID on WebSocket uses for SQLite reads writes reduces database size performance gain on SQLite index lookupsZero friction developer Client SDK (strings used Wire protocol stateful optimized strings per data #8 Predictable Performance #5 TypeScript-First ADR-027 `owner_id` System Column Stateless Authorization Limits 2026-04-24 Accepted secure multi-tenancy object-level ownership authorization line complexity JSON-based authorization rules real-time performance Add `owner_id` system column storage tables alongside `id` `namespace_id `created_at `updated_at` Store `owner_id same `doc_id `id`(16) UUIDv7) never external identity string Implement implicit `users` system table maps external string identity claims_id internal ZyncBase `id` UUIDv7) Keep `owner_id` on `users` table-shape consistency equal to `id` session resolution Zig engine maps external identity string JWT `sub to internal UUIDv7 `$session.userId`_id `BLOB(16)` populate `owner_id` with$session.userId` upon document creation Treat `id` as document identity collectionexpected unique across collection/table `namespace_id` not part primary key routing/filtering column limit `authorization.json evaluation Zig five variables$session$namespace$path table$doc$value (incoming `$doc` reference columns target selected updated deleted rule relational join relationship traversal lookup table forbidden JSON permissions represented trusted external identity claims encoded same row `owner_id` enables code-free object-level security `users` mapping table guarantees_id compact 16-byte binary format ZyncBase `doc_id representation saving ~20 bytes per row `owner_id` on `users` avoids table-shape exceptions DDL generation authorization defaults replication payloads query planning self semantics identity `PRIMARY KEY(id)` avoids composite key fan-out foreign keys tie-breakers SDK APIs Namespaces constrain visibility redefine document identity limits evaluation context guarantee predictable rule evaluation same-row SQL predicate injection "slow query" problem auth layer#1 Primitives over Magic `users` standard collection hidden config #8 Predictable Performance #9 Secure by Default ADR-028 Global Master Data (namespaced: false) 2026-04-25 Accepted ZyncBase collections partitioned by `namespace_id SaaS applications require master data tables transcend namespace boundaries visible globally Introduce"namespaced": boolean primitive in `schema.json definitions storage tables retain standard system columns `namespace_id `owner_id` `namespaced` changes_id not `namespaced omitted defaults to `true` Zig stores active namespace ID filters set `false` Zig stores global namespace ID `0` routes queries `users` collection defaults to"namespaced: false `users` configured"namespaced": true for tenant-isolated identity realms external identity mapping scoped by(namespace_id external_id namespace switching requires re-resolving$session.userId` Provides flexibility define global data collections without server-side logic Avoids duplicating static master data across tenant namespacesMaintains "Secure Default semantics namespacing unless opted-out Preserves uniform table shape null-heavy special cases field indexes DDL generation migrations authorization predicates consistent Avoids_id predicates hot paths Each table one namespace predicate active `0 global #1 Primitives over Magic #3 SQLite Source Truth ADR-029 Scoped Session Readiness Gate 2026-05-05 Accepted ADR-026 established integer namespace routing ADR-027 established `owner_id `users.id ADR-028 allowed `users.namespaced = true` invariant WebSocket transport open before scoped context store operations `users.namespaced = true external identity string different `users.id per namespace identity resolution hardcoded anonymous UUID transport-only state Distinguish transport connectivity from scoped session readiness Store external identity string on connection anonymous SDK-generated authenticated clients JWT Resolve operation-domain scope through SQLite before accepting scoped operations scope namespace string_namespaces.id external identity `users.id$session.userId Store presence maintain separate scopes namespaces differ Store operations require scope readyPresence operations require scope If `users.namespaced false identity resolution uses namespace ID `0` store presence scopes share same `users.id `users.namespaced true resolution uses namespace ID scope Store namespace switching re-resolves ID presence namespace switching ID Before scope server accepts lifecycle messages authentication store ping close Store query data messages with `SESSION_NOT_READY Namespace identity changes invalidate scoped state Store namespace changes clear subscriptions Presence namespace changes clear subscriptions Auth refresh updates session active scopes continue auth refresh fails connection terminated Preserves `users.namespaced = true identity Ensures `owner_id presence `userId correspond `users.id rows Keeps SDK ergonomics simple after scopes ready Avoids anonymous-user shortcuts keys #1 Primitives over Magic #3 SQLite Source Truth #7 Secure by Default ADR-030 Async Session Resolution-Blocking 2026-05-06 Accepted ADR-029 scoped session readiness gate `StoreSetNamespace triggers namespace user identity resolution initial implementation uses `CompletionSignal.wait() Mutex+Condition blocking call uWS event loop writer thread result blocks reactor 500μs–2ms per resolution stalling WebSocket connections violating p50 < 1ms latency target (ADR problem specific `StoreSetNamespace `PresenceSetNamespace `AuthRefresh other operations accepted/eventual writes (ADR-031) or fast reader-pool reads Resolution code path blocking round-trip thread Replace `CompletionSignal.wait() resolution two-tier strategy in-memory identity cache async SPSC ring buffer event loop wakeup handoff cache misses-tier 1 Check lock-free namespace user identity caches hit set scope respond zero I/O blocking 2 (async non-blocking cache miss enqueue `resolve_session` WriteOp writer thread return without response uWS reactor free Two `lockFreeCache` instances `StorageEngine `namespace_cache`(namespace_string) → `namespace_id Populated writer thread after Immutable once set IDs `identity_cache` `users.Populated writer thread after `INSERT OR IGNORE RETURNING id Immutable once set ID mappings caches reuse `lockFreeCache` generic epoch-based reclamation atomic ref-counting COW map swaps Values trivial heap `deinit no-op `resolve_session Replaces_namespace_user Performs namespace user resolution single to zero`SessionResolutionBuffer SPSC ring buffer (capacity 256 `ChangeBuffer writer thread pushes `SessionResolutionResult structs uWS post_handler drains`SessionResolver new component `NotificationDispatcher uWS post_handler drains `SessionResolutionBuffer acquires target connection ID applies resolved scope encodes success/error response calls `ws.send()`scope_seq` monotonic per-connection counter incremented scope reset Carried in `resolve_session WriteOp checked at delivery time If match result stale discarded capacity rationale (256 + 512 overflow `StoreSetNamespace lifecycle message ~1x per connection session not hot data pathswriter thread processes ~5,000–10,000 resolutions/second post_handler drains uWS loop Worst-case (100k buffer accumulates ~10–50 results between drain cycles 256 provides 25x headroom over worst case ring buffer full results spill into overflow ArrayList (capacity ring buffer (256) overflow (512) exhausted 768 writer logs error drops result client retries via SDK timeout combined capacity >150x headroom over worst-case load 1 eliminates I/O After first resolution connections resolve ~1μs lock-free cache no reader mutex SQLite writer thread 2 eliminates reactor blocking cold New pairs resolve without blocking reactor response delivered asynchronously `ChangeBuffer_wakeup_loop() `post_handler pattern resolution reduces WriteOp variants eliminates round-trips cache miss **ADR-029 correctness `store_ready` remains `false until resolver delivers result Messages before resolution receive `SESSION_NOT_READY` Connections disconnect during async resolution `SessionResolver uses `ConnectionManager.() discard results disconnected clients identical `NotificationDispatcher pattern Extends ADR-026 Namespace string integer resolution lock-free cache first writer thread second Extends ADR-029 Scoped session resolution non-blocking uWS reactor Extends ADR-011 Lock-free cache role namespace user identity mappings auth/schema metadata Zero blocking uWS reactor session resolution Common case cache resolves ~1μs 500x faster blocking path Simpler WriteOp surface_namespace_user_session connections unaffected cold-start resolution Async response delivery adds ~1 event latency cache misses Two lock-free cache instances add memory proportional unique user pairs ADR-031: Mutation Acknowledgement Realtime Consistency Semantics 2026-05-25 Accepted ADR-007 Optimistic Writes Default ZyncBase realtime-first database Client-visible state observed subscriptions changes visible server-pushed subscription deltas Mutation acknowledgement semantics explicit writes asynchronous server-side write pipeline mutation accepted before thread committed subscription update after mutation distinct TypeScript SDK update subscription data mutating operations.batch Local subscription state changes server emits updates write failure reporting not needed for rollback needed for user feedback diagnostics workflows precise write outcome reporting ZyncBase uses subscription-first state propagation default consistency model for client-visible data Default mutation methods.set.remove.batch mutate local subscription state successful mutation response means server accepted mutation request write committed Subscription callbacks committed state infer write failure from subscription delta ZyncBase distinguishes two confirmation levels```ts
await store.set(path, value);
await store.set(path, value, { confirm: "accepted" });
// Resolves when the server accepts the mutation into the write pipeline.

await store.set(path, value, { confirm: "committed" });
// Resolves when the writer commits the mutation or an accepted no-op.
// Rejects when the writer reports failure.
```"accepted default represented SDK types examples prefer no-options form "committed applies mutating operations.set.remove.batch server parsed validated authorized before accepted mutation write pipeline **committed**-thread result succeeded accepted no-op outcomes **request/response correlation responses **writeId** correlation tracked writer-thread outcomes **WriteError** asynchronous writer failure message SDK event public protocol SDK documentation use "NACK Writer failures errors not rollback commands **Immediate Request reject mutation before owns operation Examples malformed messages invalid paths payload shapes unknown tables fields session not ready authorization failures queue admission failure request handling failures before writer ownership Confirmed write failures immediate request failures SDK error type `ZyncBaseError metadata phase```ts
{
  phase: "accept" | "write"
}
```Outcome Writer-thread failures as errors subscription state rollback instructions Tracked confirmed writes use `writeId correlate outcomes existing request `id scoped response confirmed writes SDK promise public success signal ZyncBase expose public `WriteCommitted event core API confirmed promise resolves no-op rejects failure confirmation received `WriteError public async failure name```ts
{
  type: "WriteError",
  writeId: "...",
  code: "PERMISSION_DENIED",
  message: "...",
  details: {
    phase: "write"
  }
}
```best-effort metadata single `set include batch failure operation include `path `batchIndex transaction-level systemic failure `path omitted Global write-error events tracked systemic failures untracked writes receive per-operation async error delivery acceptance Commit confirmation subscription delivery not ordered confirmed write resolves subscriptions receive state change subscription filtering connection state SDK guarantee subscription callback before write resolves Subscriptions authoritative committed-state channel Write confirmation outcome reporting not state delivery.batch atomic resolves full batch commits no-op outcomes rejects operation fails exposes no partial success commit partial writes failure Batch failures include `batchIndex failing operation identified```ts
{
  code: "PERMISSION_DENIED",
  message: "...",
  details: {
    batchIndex: 2,
    phase: "write"
  }
}
```transaction-level failures no operation `batchIndex` omitted Default `store.batch()` follows same contract writes **Authorization Write authorization rules preserve invariant client create document same session update same write rules `StoreSet` `$doc` write rules interpreted by write kind candidate document existing document existing document Create authorization evaluated RAM against candidate document candidate document includes document id injected `owner_id = $session.userId` incoming normalized fields server-managed fields defaults `owner_id` injected server ownership rules pass creates owning session```json
{ "$doc.owner_id": { "eq": "$session.userId" } }
```create rule references$doc field absent document not server-injected defaulted create denied Update delete authorization guarded against rows ZyncBase require post-image authorization for updates user update document state rule write rule "draft update from `draft to `published after transition allows updates-Op Deleting missing document success/no-op Setting field document same stored value success ZyncBase suppress deltas value Applications rely on delta for same-value writes committed write client not write error write no delta subscription targets data change subscription result Default accepted writes perform extra storage reads classify zero-row outcomes Confirmed writes classify guarded zero-row outcomes produce errors row fails write guard `PERMISSION_DENIED absent row delete success/no-op storage transaction failure error **Connection Failure Operation status retained in memory scoped originating connection for tracked confirmed writes ZyncBase retain write-status records across disconnects connection drops before confirmed write result SDK rejects promise with connection error write commit reconnect subscriptions remain source truth for final state Confirmed writes need timeouttimeout confirmation received imply write aborted failed commit ZyncBase guarantee abort after write accepted Automatic idempotency keys for retried writes outside decision Some operations idempotent retry requires separate idempotency design Systemic writer storage failures engine health events not If processing new writes fail during acceptance engine unhealthy Confirmed writes reject with `ZyncBaseError server status degraded state Write errors retryability transient operational failures retryable access denied invalid values schema errors constraint violations not retryable disk full encourage automatic client retry loops decision separates concepts request acceptance mutation state observation write outcome reporting optional operation status error feedback preserves ZyncBase realtime-first model precise feedback mutation wait for commit request/response database latency for subscription Restoring local writes rollback conflict handling complexity Write errors valuable user feedback diagnostics not local state repair Keeps default writes low latency Preserves subscriptions for state Avoids rollback machinery precise committed-write confirmation clear async failure reporting modelKeeps request ids writer outcome ids separate `await store.set() documentation explicit Applications confirmation request committed confirmation Confirmed writes add writer-result correlation timeout handling Confirmed authorization errors require classification work mutation wait for writer commit changes latency Restore optimistic local writes require rollback conflict handling complex semantics report writer-thread failures Missing subscription deltas ambiguous save failure feedback Use NACKs as rollback messages no speculative local subscription state Writer failures write-status errors not rollback commands Reuse request id as writer outcome id Request ids correlate responses Writer outcomes need separate `writeId for confirmed writesADR-032 Config-Driven Authentication External Permission Claims 2026-06-02 Accepted Supersedes-016 Bun Hook Server Modifies [ADR-027 `owner_id System Column Stateless Authorization Limits Modifies [ADR-029 Scoped Session Readiness Gate ZyncBase needs secure authentication anonymous access tenant/project authorization object ownership without identity arbitrary application logic database previous Bun Hook Server design allowed TypeScript permission checks added extra runtime internal protocol latency circuit-breaker behavior blurred product ZyncBase resource server validates identity enforces declarative rules compute source truth user accounts memberships billing state permission grants Remove Bun Hook Server support Keep HTTP ticket exchange native configuration-driven ZyncBase validates external JWTs issuers audiences algorithms shared secrets JWKS sources ZyncBase JWT claims$session` defines JWT claim names$session variables Permission claims_id trusted token signature registered constraints validatedAnonymous access uses ticket pipeline SDK generates anonymous subject persists presents anonymous external identity auth enabled `users table internal identity mapping optional profile/display-data table maps external subject to internal `BLOB(16) UUIDv7 `owner_id$session.userId presence identity foreign keys User-row fields not loaded for authorization part of$session `authorization.json limited to RAM checks over$session$namespace$path$value same-row$doc predicates store query predicate model permission requiring joins relationship traversal external API calls billing lookups permission computation represented before ZyncBase request trusted token same-row data application code Token refresh revocation permission-update mechanism ZyncBase revalidates replacement JWT updates session active scopes continue JWT invalid connection terminated server-side revocation list Tokens short-lived SDK refresh before expiry session token expires no refresh server terminates connection Tenant project switching new JWT compatible scoped grant arrays large changing permission sets mint narrower active-context tokens Self-Hosting no required process extra runtime#5 TypeScript-First use JS/TS identity layer ZyncBase run #7 Declarative Security ZyncBase authorization JSON #8 Predictable Performance no foreign calls joins hidden database reads Positive Removes Hook Server runtime hook protocol circuit-breaker failure mode WebSocket authentication safe JWTs URLs authorization deterministic fast permission ownership explicit external identity provider owns permission ZyncBase enforces trusted claims anonymous users low-friction database tables Negative ZyncBase answer permission questions token namespace target row incoming value revocation depends short-lived tokens refresh behavior Large permission graphs compressed into human-manageable claims groups roles active-context tokens before ZyncBase maximum limit claim array elements 1000 items Hook Server advanced authorization second runtime adds latency availability permission computation Load `users` row fields$session authorization hidden database reads refresh semantics second permission source identity mapping ownership optional profile data not authorization input Add richer JSON relationship queries Rejectedquery engine undermine same-row predicate limit authorization Require new JWT namespace switch Rejected strict token scoped grants `authorization.json check namespace parts same-row fields Applications choose narrower active-context tokens size staleness matters