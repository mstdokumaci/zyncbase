Threading Model 2026-03-09 Overview ZyncBase uses-threaded architecture read/write vertical scaling design CPU cores read operations correctness serialized writes Lock-free cache reads mutex writes 17x performance improvement Architecture Diagram```
┌─────────────────────────────────────────────────────────┐
│          uWebSockets Event Loop (Multi-threaded)        │
│       │                    │                    │       │
│    Thread 1             Thread 2             Thread N   │
│   WebSocket            WebSocket            WebSocket   │
│  Connections          Connections          Connections  │
│       │                    │                    │       │
│       └────────────────────┼────────────────────┘       │
│                            │                            │
│                     Callbacks (Zig)                     │
│                            │                            │
│              ┌─────────────▼─────────────┐              │
│              │       Message Router      │              │
│              └─────────────┬─────────────┘              │
│                            │                            │
│              ┌─────────────┴─────────────┐              │
│              │                           │              │
│          READ PATH                   WRITE PATH         │
│    (Parallel, Lock-Free)        (Serialized, Mutex)     │
│              │                           │              │
│    ┌─────────▼─────────┐       ┌─────────▼─────────┐    │
│    │  Lock-Free Cache  │       │    Write Mutex    │    │
│    │  ┌──┐  ┌──┐  ┌──┐ │       │  ┌──────────────┐ │    │
│    │  │T1│  │T2│  │TN│ │       │  │ Single Writer│ │    │
│    │  └──┘  └──┘  └──┘ │       │  └──────────────┘ │    │
│    │  Atomic Ref Count │       │    State Updates  │    │
│    └─────────┬─────────┘       └─────────┬─────────┘    │
│              │                           │              │
│    ┌─────────▼─────────┐       ┌─────────▼─────────┐    │
│    │ SQLite Read Pool  │       │   SQLite Writer   │    │
│    │  ┌──┐  ┌──┐  ┌──┐ │       │  ┌──────────────┐ │    │
│    │  │R1│  │R2│  │RN│ │       │  │ WAL Batching │ │    │
│    │  └──┘  └──┘  └──┘ │       │  └──────────────┘ │    │
│    │    (WAL Mode)     │       │    (WAL Mode)     │    │
│    └───────────────────┘       └───────────────────┘    │
│                                                         │
│  Performance (16-core, 90% reads):                      │
│  - Reads:  16 × 11k = 176k req/sec (parallel)           │
│  - Writes:  1 × 10k =  10k req/sec (serialized)         │
│  - Total: ~170k req/sec average                         │
│  - CPU usage: ~95% (all cores utilized)                 │
└─────────────────────────────────────────────────────────┘
```uWebSockets Handles Multi-threading Event loop threads distributes connections Handles network I/O No blocking Zig Callbacks Execute Parallel threads process messages simultaneously router directs read write path No blocking Writes queued serialized Core Engine Uses Read/Write Separation Path Lock-free cache access Multiple threads execute simultaneously SQLite connection pool parallel database reads Scales CPU cores Path Mutex-protected Single thread Batched writes SQLite Notifies subscribers after write Safety Strategy core engine orchestrator messages parallel read serialized write path ensures lock-free cache sync storage synchronization logic Zig implementation Implementation Performance Characteristics Workload reads 10% writes-core machine```
Reads:  16 cores × ~11k each = 176k req/sec (parallel)
Writes:  1 core  × ~10k      =  10k req/sec (serialized)
Combined: ~170k req/sec average
CPU usage: ~95% (all cores utilized)
```Single-threaded```
Reads:  1 core × 10k = 10k req/sec
Writes: 1 core × 10k = 10k req/sec
Combined: 10k req/sec
CPU usage: 6% (1/16 cores)
```17x performance improvement Reads lock Multiple threads read simultaneously scales CPU cores SQLite parallel reads utilized Writes Necessary correctness SQLite single-writer limitation fast (10k+ writes/sec Read-heavy workloads scale real-time apps 80-95% reads use all CPU cores bottleneck reads Memory Management Strategy ZyncBase specialized allocation patterns minimize overhead high Management Implementation specifics request-scoped data common structures**Allocator Strategies** (Arena Pool GPA). Pros Cons Pros all CPU vertical scaling scale More cores more throughput **SQLite parallel reads** **17x better single-threaded No complex locking patterns Cons **Writes serialized** SQLite single-writer limitation atomic lock-free cache single-threaded Mitigation serialized writes workloads read-heavy (90%+) 10k writes/sec batch writes higher throughput atomic operations Zig safe atomic primitives Compile-time checks prevent race conditions testing validates correctness Comparison Alternatives Single-threaded Core (Rejected Simpler implementation No race conditions Easier reason utilize multiple CPU cores Limited to ~10k req/sec total Wastes SQLite parallel read capability Not competitive with systems Thread-per-Namespace (Future) Natural isolation tenant scale beyond single-writer limit complex Harder load balance sophisticated scheduling Defer to v2.5+ [ADRs [Storage Layer parallel disk access Principles Performance validation benchmarks