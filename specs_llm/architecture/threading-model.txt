Threading Model ZyncBase uses-threaded architecture read/write vertical scaling CPU cores read operations correctness serialized writes Subscription Engine parallel application data reads lock-free cache metadata mutex writes 17x performance improvement Architecture Diagram```
┌─────────────────────────────────────────────────────────┐
│          uWebSockets Event Loop (Multi-threaded)        │
│       │                    │                    │       │
│    Thread 1             Thread 2             Thread N   │
│   WebSocket            WebSocket            WebSocket   │
│  Connections          Connections          Connections  │
│       │                    │                    │       │
│       └────────────────────┼────────────────────┘       │
│                            │                            │
│                     Callbacks (Zig)                     │
│                            │                            │
│              ┌─────────────▼─────────────┐              │
│              │       Message Router      │              │
│              └─────────────┬─────────────┘              │
│                            │                            │
│              ┌─────────────┴─────────────┐              │
│              │                           │              │
│          READ PATH                   WRITE PATH         │
│    (Parallel, Lock-Free)        (Serialized, Mutex)     │
│              │                           │              │
│    ┌─────────▼─────────┐       ┌─────────▼─────────┐    │
│    │ Subscription      │       │    Write Mutex    │    │
│    │ Engine (warm)     │       │  ┌──────────────┐ │    │
│    │ ┌──┐  ┌──┐  ┌──┐  │       │  │ Single Writer│ │    │
│    │ │T1│  │T2│  │TN│  │       │  └──────────────┘ │    │
│    │ └──┘  └──┘  └──┘  │       │    State Updates  │    │
│    │ Lock-Free Cache   │       └─────────┬─────────┘    │
│    │ (auth/schema/ns)  │                 │              │
│    └─────────┬─────────┘       ┌─────────▼─────────┐    │
│         (cold)                 │   SQLite Writer   │    │
│    ┌─────────▼─────────┐       │  ┌──────────────┐ │    │
│    │ SQLite Read Pool  │       │  │ WAL Batching │ │    │
│    │  ┌──┐  ┌──┐  ┌──┐ │       │  └──────────────┘ │    │
│    │  │R1│  │R2│  │RN│ │       │    (WAL Mode)     │    │
│    │  └──┘  └──┘  └──┘ │       └───────────────────┘    │
│    │    (WAL Mode)     │                                 │
│    └───────────────────┘                                 │
│                                                         │
│  Performance (16-core, 90% reads):                      │
│  - Reads:  16 × 11k = 176k req/sec (parallel)           │
│  - Writes:  1 × 10k =  10k req/sec (serialized)         │
│  - Total: ~170k req/sec average                         │
│  - CPU usage: ~95% (all cores utilized)                 │
└─────────────────────────────────────────────────────────┘
```uWebSockets Handles Multi-threading Event loop threads distributes connections Handles network I/O No blocking Zig Callbacks Execute Parallel threads process messages simultaneously router directs read write path No blocking Writes queued serialized Core Engine Uses Read/Write Separation data reads Warm collections evaluated memory no SQLite involvement Cold queries hit SQLite read pool-free serves auth/schema metadata namespace-to-integer mappings user identity mappings immutable wait-free reads Multiple threads execute simultaneously no cross-thread contention Scales linearly with CPU cores Path Mutex-protected Single thread processes batched operations Publishes events Subscription Engine after commit Notifies subscribers `NotificationDispatcher Safety Strategy core engine routes messages parallel read serialized write path Subscription Engine maintains in-memory collection state updated commit lock-free cache handles auth/schema/identity metadata atomic reference counting COW map swaps Detailed synchronization logic Zig implementation Implementation Performance Characteristics Typical Workload reads 10% writes-core machine```
Reads:  16 cores × ~11k each = 176k req/sec (parallel)
Writes:  1 core  × ~10k      =  10k req/sec (serialized)
Combined: ~170k req/sec average
CPU usage: ~95% (all cores utilized)
```Single-threaded```
Reads:  1 core × 10k = 10k req/sec
Writes: 1 core × 10k = 10k req/sec
Combined: 10k req/sec
CPU usage: 6% (1/16 cores)
```17x performance Warm reads lock-free in Subscription Engine evaluates in-memory No SQLite round-trip collections scales CPU cores Cold reads parallel SQLite read One reader connection per CPU core WAL mode enables parallel reads Writes correctness SQLite single-writer limitation Batching throughput high (10k+ writes/sec Read-heavy workloads scale real-time apps 80-95% reads use CPU cores bottleneck Memory Management Strategy ZyncBase specialized allocation patterns minimize overhead high Management Implementation specifics request-scoped data common structures**Allocator Strategies** (Arena Pool GPA). Pros Cons all CPU vertical scaling **Warm reads in-memory** Subscription Engine eliminates SQLite round-trips active data parallel reads** for cold queries **17x better single-threaded-free metadata** Auth/schema/namespace lookups wait-free Cons **Writes SQLite single-writer limitation atomic lock-free cache queries hit First subscribe incurs read single-threaded Mitigation serialized writes workloads read-heavy (90%+) 10k writes/sec Batching increases throughput cold queries After first subscribe subsequent reads in-memory `loadMore` hits SQLite Comparison Alternatives Single-threaded Core (Rejected Simpler implementation No race conditions Easier reason multiple CPU cores Limited to ~10k req/sec total Wastes SQLite parallel read capability Not competitive with systems Thread-per-Namespace (Future Natural isolation per tenant scale beyond single-writer limit complex Harder load balance sophisticated scheduling [ADRs [Storage Layer parallel disk access [Core Principlesmd Design philosophy.md Performance validation benchmarks