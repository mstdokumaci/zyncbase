ZyncBase Performance Deep Dive Making Writes Faster analysis based on current hot paths across `write_coordinator `pipeline `storage_engine_engine `message_handler Current Write Hot Path `StoreSet` single `StoreSet` request arrives```mermaid
sequenceDiagram
    participant Client
    participant EventLoop as Event Loop Thread
    participant WC as WriteCoordinator
    participant SE as StorageEngine
    participant SubE as SubscriptionEngine
    participant WriteTh as Write Thread
    participant SQLite

    Client->>EventLoop: StoreSet (msgpack)
    EventLoop->>EventLoop: decode + validate schema
    EventLoop->>WC: coordinateSet()
    
    rect rgb(255, 100, 100)
        Note over WC,SQLite: BLOCKING on Event Loop Thread!
        WC->>SE: selectDocument() [read old row]
        SE->>SQLite: SELECT (or cache hit)
        SQLite-->>SE: old_row
        SE-->>WC: ManagedPayload
    end
    
    rect rgb(255, 150, 100)
        Note over WC: 4× deep clones!
        WC->>WC: mergeRow() [clone old + new fields]
        WC->>WC: deepClone(old_row) for notification
        WC->>WC: deepClone(new_row) for notification
    end
    
    rect rgb(255, 200, 100)
        Note over WC,Client: BLOCKING on Event Loop Thread!
        WC->>SubE: handleRowChange()
        SubE->>SubE: lock + iterate ALL groups
        SubE->>SubE: evaluateFilter(old_row)
        SubE->>SubE: evaluateFilter(new_row)
        SubE-->>WC: []Match
        WC->>WC: encode msgpack per subscriber
        WC->>Client: ws.send() per subscriber
    end
    
    WC->>SE: insertOrReplace() [fire & forget]
    SE->>WriteTh: pushWrite(op)
    Note over WriteTh: Might FAIL later!
    EventLoop-->>Client: "ok"
```event loop thread blocked entire reading old cloning filter evaluation WebSocket sends before other connection single biggest performance bottleneck 6 Bottlenecks 1. Synchronous Read-Before-Write (CRITICAL) [coordinateSet]/write_coordinator.zig#L62-L66)```zig
// This blocks the event loop thread on every single write!
var managed_old = self.storage_engine.selectDocument(arena, table, doc_id, namespace)
```write triggers `selectDocument() zero subscribers cache miss hits SQLite reader pool mutex preparing SQL executing decoding rows). cache hit faster involves lock-free cache traversal atomic operations Time blocked event loop Cache hit ~5μs Cache miss (SQLite read ~200-500μs No subscribers watching 100% wasted work Excessive Deep Cloning [coordinateSet]#L72-L82) [mergeRow]#L231-L256 `StoreSet triggers deep clone operations** `mergeRow clones field_row new field value `RowChange `new_row deep-cloned `RowChange walks MessagePack tree allocating new nodes maps arrays strings document 20 fields 80+ allocations per write Notification Event Loop Thread [broadcastChange]/write_coordinator#L159-L229 notification pipeline event loop Acquires lock `SubscriptionEngine Iterates subscription groups Evaluates filter conditions old new rows Encodes MessagePack messages Sends WebSocket frames 100 groups 10 subscribers per group **2000 filter evaluations 1000 WebSocket event loop SQL Generation Per Write [buildInsertOrReplaceOp]/storage_engine/writer generates SQL text strings REPLACE SQLite parses bytecode SQL identical values change Phantom Notifications Failed Writes ordering [coordinateSet]/write_coordinator.zig#L88-L91)```zig
try self.broadcastChange(arena, change);           // Notify first
try self.storage_engine.insertOrReplace(...);       // Write second (might fail!)
```Notifications write confirmed write fails constraint violation subscribers see phantom change correctness bug wasted work No Write Coalescing `user:123 receives 3 updates pipeline executes 3 `INSERT OR REPLACE statements last matters Proposed Architecture Deferred Notification Pipeline```mermaid
sequenceDiagram
    participant Client
    participant EventLoop as Event Loop Thread
    participant SE as StorageEngine
    participant WriteTh as Write Thread
    participant SQLite
    participant NotifQ as Notification Queue
    participant SubE as SubscriptionEngine

    Client->>EventLoop: StoreSet (msgpack)
    EventLoop->>EventLoop: decode + validate (~10μs)
    EventLoop->>SE: insertOrReplace() (~15μs)
    SE->>WriteTh: pushWrite(op)
    EventLoop-->>Client: "ok" (ack)
    
    Note over EventLoop: FREE to serve other connections!
    
    rect rgb(100, 200, 100)
        Note over WriteTh,SQLite: On Write Thread
        WriteTh->>SQLite: BEGIN
        WriteTh->>SQLite: SELECT old_row (same transaction!)
        WriteTh->>SQLite: INSERT OR REPLACE
        WriteTh->>SQLite: COMMIT
        WriteTh->>NotifQ: push(RowChange)
    end
    
    rect rgb(100, 150, 255)
        Note over NotifQ,Client: On Event Loop (non-blocking callback)
        NotifQ->>SubE: handleRowChange()
        SubE-->>NotifQ: matches
        NotifQ->>Client: ws.send() per subscriber
    end
```5 Optimizations 1: Deferred Notification Pipeline 15-30x event loop throughput Metric Current Proposed Event loop time per write 320-770μs ~25μs serve during write blocked free Phantom notifications Yes Old row read correctness Optimistic Transactional validate build `WriteOp push queue return "ok`flushBatch read old row execute write capture `RowChange change buffer loop Drain change buffer evaluate subscriptions send WebSocket notifications old-row read moves write old row read atomically with write eliminating TOCTOU races Notifications for writes No phantoms Optimization 2: Prepared Statement Cache 2-3x SQL execution```zig
// Current: regenerated per-write
const sql = try writer.buildInsertOrReplaceSql(allocator, table, columns);
// sqlite parses → optimizes → compiles → executes → frees

// Proposed: prepare once, rebind many
const stmt = self.prepared_stmts.get(table) orelse blk: {
    const s = try conn.prepareDynamic(sql);
    try self.prepared_stmts.put(table, s);
    break :blk s;
};
stmt.reset();
stmt.bind(values...);
stmt.step();
```SQLite_prepare_v2_reset faster re-parsing SQL text batch 200 writes × 2-3x faster batch throughput gain Optimization 3: Zero-Copy Notification Encoding 50-70% fewer allocations notification `broadcastChange` deferred notifications Pre-encode `RowChange payload Store encoded bytes notification queue event loop append `subscription_id` send No per-subscriber MessagePack encoding Optimization 4: Subscription Index O(1) filter equality```zig
// Current: iterate all groups, evaluate all conditions
for (group_ids.items) |gid| {
    const group = self.groups.get(gid) orelse continue;
    const matched = try evaluateFilter(group.filter, row);
    // O(groups × conditions)
}

// Proposed: hash-index for equality conditions
// Key: "status=active" → [group_ids...]
const exact_groups = self.equality_index.get(field_value_key);
// O(1) lookup + O(remaining non-equality conditions)
```-world subscriptions use equality filters pre-built index makes case instant Optimization 5: Write Coalescing Linear reduction hot```
Batch window:  [set user:123 name=A] [set user:123 name=B] [set user:123 name=C]
Current:       3× INSERT OR REPLACE
Coalesced:     1× INSERT OR REPLACE (name=C)
```per-batch deduplication pass merges writes same id namespace Expected Impact Workload Current Proposed Improvement Write-heavy subscribers ~2k writes/sec ~20k/sec Mixed moderate subs ~170k req/sec ~500k req/sec **3x** Write burst same documents ~5k writes/sec ~25k writes/sec Event loop saturation point ~1.5k concurrent writes ~30k win **Optimization 1** (Deferred changes event loop thread write clone match encode send to "validate additive Implementation Phases Deferred Notifications Add `ChangeBuffer buffer `StorageEngine Move old-row read `flushBatch Capture `RowChange buffer after commit Add `drainChanges() method Remove synchronous read/notify from `WriteCoordinator.coordinateSet Phase 2: Prepared Statement Cache Add `HashMap( sqlitePrepare statements first use table Reset rebind Invalidate schema migration Phase 3: Write Coalescing deduplicate ops id namespace Merge arrays duplicate keys Keep final state Phase 4: Subscription Index Build equality index() Lookup scan Maintain index `unsubscribe() Trade-offs Risks Mitigation Notification latency increases sub-millisecond configurable flush interval Write thread old row Single connection no contention SQLite reads fast WAL mode Prepared statement cache uses memory One statement table coalescing changes semantics coalesce batch subscribers see final state real-time Change buffer overflow Ring buffer backpressure configurable size