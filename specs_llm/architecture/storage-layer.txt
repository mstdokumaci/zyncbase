Storage Layer 2026-03-09 Overview ZyncBase uses SQLite Write-Ahead Logging (WAL) mode storage layer zero-config deployment ACID transactions parallel critical for vertical scaling SQLite WAL mode connection pool parallel reads across CPU cores architectural decision [ADR-004]-sqlite-wal-mode SQLite Advantages **Zero Embedded database no separate server **ACID Data integrity guarantees-text FTS5 extension 20+ years billions of devices Parallel reads scaling Easy backup deployment Performance+ reads WAL mode,600+ writes batching-millisecond-memory cache with CPU (parallel reads WAL Mode transforms SQLite concurrency model single-user to high-concurrency engine Without WAL```
┌───────────────────────────────────────┐
│           Single Writer               │
│  ┌─────────────────────────────────┐  │
│  │      Blocks ALL Readers         │  │
│  └─────────────────────────────────┘  │
│                                       │
│          Random I/O patterns          │
│       Slower write performance        │
│          Limited concurrency          │
└───────────────────────────────────────┘
```

### With WAL

```
┌───────────────────────────────────────┐
│     Multiple Readers (Parallel)       │
│  ┌──────┐    ┌──────┐    ┌──────┐     │
│  │  R1  │    │  R2  │    │  RN  │     │
│  └──────┘    └──────┘    └──────┘     │
│                                       │
│     Single Writer (No blocking)       │
│  ┌─────────────────────────────────┐  │
│  │      Writes to WAL file         │  │
│  └─────────────────────────────────┘  │
│                                       │
│        Sequential I/O patterns        │
│       Faster write performance        │
│          True parallel reads          │
└───────────────────────────────────────┘
```WAL Works to WAL Not main database avoiding contention access Main DB + WAL concurrent read/write WAL entries merged to main DB periodically Readers operate independently Performance Impact```
16-core machine with WAL mode:
- Reads: 16 threads × 10k = 160k reads/sec
- Writes: 1 thread × 10k = 10k writes/sec
- Total: 170k ops/sec (90% read workload)
```Connection Pool Strategy multiple CPU cores ZyncBase maintains connection pool one reader per core writes queued executed single connection SQLite serialization requirements [ADR-005-threaded-core reader per Maximizes parallel read throughput connection contention Batched efficiency `fsync() overhead-local Minimizes internal synchronization overhead Schema Design Relational-Document Hybrid ZyncBase generates SQLite tables from `schema.json frontend developers work paths core maintains relational integrity [ADR-019]-hybrid-path Path-to-Table Mapping first segment path maps to database table simplifies indexing query optimization Schema ZyncBase strict-schema architecture user database tables columns derived from `schema.json ad-hoc table creation prohibited `schema.json omitted missing server boots with synthesized users-only schema fails if schema invalid Dynamic/schemaless storage fallbacks removed typed relational integrity Relational Patterns nested objects flattened into relational columns `address.city →Document enforced via SQLite foreign keys configurable actions `cascade `restrict_null normalized canonical sorted sets on write Canonical arrays persisted returned sorted unique form forbidden separate store paths references Schema Tasks Projects declarative schema ZyncBase generates optimized DDL```sql
-- Projects table
CREATE TABLE projects (
    id BLOB NOT NULL CHECK(length(id) = 16),
    namespace_id TEXT NOT NULL,
    name TEXT,
    created_at INTEGER NOT NULL,
    updated_at INTEGER NOT NULL,
    PRIMARY KEY (id)
);

-- Tasks table with foreign key
CREATE TABLE tasks (
    id BLOB NOT NULL CHECK(length(id) = 16),
    namespace_id TEXT NOT NULL,
    title TEXT,
    status TEXT,
    priority INTEGER,
    projectId BLOB,
    created_at INTEGER NOT NULL,
    updated_at INTEGER NOT NULL,
    PRIMARY KEY (id),
    FOREIGN KEY (projectId) REFERENCES projects(id) ON DELETE CASCADE
);

CREATE INDEX idx_tasks_namespace ON tasks(namespace_id);
CREATE INDEX idx_tasks_status ON tasks(status);
CREATE INDEX idx_tasks_projectId ON tasks(projectId);
```SDKs expose IDs references as strings core system stores packed 16-byte values Write Strategy Batching for Throughput SQLite single-writer limitation mitigated by async ring-buffer queue transaction batching Batching batching Each write separate transaction `fsync() call limits throughput to ~100 writes/sec batching 100+ operations one transaction one `fsync( call enables 10,000+ writes/sec 100x improvement Batching Strategy Triggered when queue reaches threshold 100 Triggered if operations queued long Trade-off Asynchronous Error Reporting server returns `ok` operation accepted write fails asynchronous error message SDK update Auto-Migration System ZyncBase structural detection system manages schema evolution Additions** Creating tables adding columns automatically **Destructive Type changes column removals gated behind environment configuration Uses temporary table backups SQLite `ALTER TABLE` limitations Checkpoint Management WAL files require management prevent disk bloat read degradation Non-blocking merges during idle periodsTriggered by WAL file page threshold Forced merges during maintenance windows Comparison Alternatives Database Type Concurrency Setup Performance Use Case **SQLite (WAL Embedded Parallel reads Zero-config 70k reads/s Vertical scaling Server Full parallel Complex 100k+ ops/s Horizontal scaling In-memory Single-threaded Simple 100k+ ops/s Caching **MongoDB** Server Full parallel Medium 50k+ ops/s Document store Limitations Trade-offs Single Writer SQLite allows one writer batching scale massive write-heavy workloads Checkpoint Starvation Heavy read load WAL entries main DB WAL file growth read degradation ZyncBase manages proactive checkpointing No Horizontal Scaling storage layer for vertical scaling single node multi clusters suggests LiteFS or Marmot future [Storage Implementation Zig code PRAGMAs DDL generator Model parallel reads Architectural Decision Records SQLite performance benchmarks