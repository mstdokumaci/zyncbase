Storage Layer 2026-03-09 ZyncBase uses SQLite Write-Ahead Logging mode storage zero-config deployment ACID transactions parallel critical vertical scaling SQLite WAL mode connection pool parallel reads CPU cores architectural decision [ADR-004-wal-mode SQLite Advantages Embedded database no separate server Data integrity-text FTS5 extension 20+ years billions devices Parallel reads scaling Easy backup deployment Performance+ reads WAL,600+ writes batching-millisecond-memory cache CPU reads WAL Mode transforms SQLite concurrency model single-user high engine```
┌───────────────────────────────────────┐
│           Single Writer               │
│  ┌─────────────────────────────────┐  │
│  │      Blocks ALL Readers         │  │
│  └─────────────────────────────────┘  │
│                                       │
│          Random I/O patterns          │
│       Slower write performance        │
│          Limited concurrency          │
└───────────────────────────────────────┘
```

### With WAL

```
┌───────────────────────────────────────┐
│     Multiple Readers (Parallel)       │
│  ┌──────┐    ┌──────┐    ┌──────┐     │
│  │  R1  │    │  R2  │    │  RN  │     │
│  └──────┘    └──────┘    └──────┘     │
│                                       │
│     Single Writer (No blocking)       │
│  ┌─────────────────────────────────┐  │
│  │      Writes to WAL file         │  │
│  └─────────────────────────────────┘  │
│                                       │
│        Sequential I/O patterns        │
│       Faster write performance        │
│          True parallel reads          │
└───────────────────────────────────────┘
```WAL Works WAL Not main database contention access Main DB WAL concurrent read/write WAL entries main DB Readers operate independently Performance Impact```
16-core machine with WAL mode:
- Reads: 16 threads × 10k = 160k reads/sec
- Writes: 1 thread × 10k = 10k writes/sec
- Total: 170k ops/sec (90% read workload)
```Connection Pool Strategy CPU cores ZyncBase connection pool one reader per core writes queued executed single connection SQLite serialization requirements-005-threaded-core reader per Maximizes read throughput contention Batched efficiency `fsync overhead-local Minimizes synchronization Schema Design Relational-Document Hybrid generates SQLite tables from.json frontend developers paths core relational integrity [ADR-019 Path-to-Table Mapping path maps to database table simplifies indexing query optimization strict-schema architecture valid JSON schema server startup no schema invalid database tables columns derived from schema ad-hoc table creation prohibited Dynamic/schemaless storage fallbacks removed typed relational integrity Relational Patterns nested objects flattened into relational columns `address.cityDocument enforced SQLite keys configurable actions `cascade_null stored JSON blobs forbidden separate store paths references Schema Tasks Projects declarative schema ZyncBase generates DDL```sql
-- Projects table
CREATE TABLE projects (
    id TEXT PRIMARY KEY,
    namespace_id TEXT NOT NULL,
    name TEXT,
    created_at INTEGER NOT NULL,
    updated_at INTEGER NOT NULL
);

-- Tasks table with foreign key
CREATE TABLE tasks (
    id TEXT PRIMARY KEY,
    namespace_id TEXT NOT NULL,
    title TEXT,
    status TEXT,
    priority INTEGER,
    projectId TEXT,
    created_at INTEGER NOT NULL,
    updated_at INTEGER NOT NULL,
    FOREIGN KEY (projectId) REFERENCES projects(id) ON DELETE CASCADE
);

CREATE INDEX idx_tasks_namespace ON tasks(namespace_id);
CREATE INDEX idx_tasks_status ON tasks(status);
CREATE INDEX idx_tasks_projectId ON tasks(projectId);
```Strategy Batching for Throughput SQLite single-writer limitation mitigated async ring-buffer queue transaction batching batching Each separate `fsync( call limits throughput writes/sec batching 100+ operations one transaction one `fsync call enables 10,000+ writes/sec 100x improvement Batching Strategy Triggered queue threshold Triggered if queued long Trade-off Asynchronous Error Reporting server returns `ok operation accepted write fails asynchronous error message Auto-Migration System ZyncBase structural detection schema evolution tables columns Type column removals gated behind environment configuration Uses temporary table backups TABLE limitations Checkpoint Management WAL files management prevent disk bloat read degradation Non-blocking merges idle periods Triggered by file page threshold Forced merges during maintenance windowsComparison Alternatives Database Type Concurrency Setup Performance Use Case **SQLite Embedded Parallel reads Zero-config 70k reads/s Vertical scaling Server Full parallel Complex 100k+ ops/s Horizontal scaling In-memory Single-threaded Simple 100k+ ops/s Caching Server Full parallel Medium 50k+ ops/s Document store Limitations Trade-offs Single Writer SQLite allows one writer batching write workloads Checkpoint Starvation read load entries file growth degradation ZyncBase manages checkpointing No Horizontal Scaling storage vertical scaling single node multi clusters suggests LiteFS Marmot Implementation Zig code PRAGMAs DDL generator Model parallel reads SQLite performance benchmarks