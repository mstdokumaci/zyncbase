{
	"namespaces": [
		{
			"pattern": "world-1",
			"storeFilter": true,
			"presenceRead": true,
			"presenceWrite": { "$session.role": { "eq": "player" } },
			"presenceSharedWrite": false
		}
	],
	"store": [
		{
			"collection": "meta",
			"read": { "$session.role": { "eq": "simulation" } },
			"write": { "$session.role": { "eq": "simulation" } }
		},
		{
			"collection": "chunks",
			"read": true,
			"write": { "$session.role": { "eq": "simulation" } }
		},
		{
			"collection": "users",
			"read": true,
			"write": { "$session.role": { "eq": "simulation" } }
		},
		{
			"collection": "countries",
			"read": true,
			"write": { "$session.role": { "eq": "simulation" } }
		}
	]
}
