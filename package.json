{
  "workspaces": [
    "sdk/*"
  ],
  "scripts": {
    "test:e2e": "bun test tests/e2e/src/e2e.test.ts",
    "test:load": "bun run tests/load/run.ts",
    "test:load:smoke": "bun run tests/load/run.ts --smoke",
    "specs:compress": "uv run python scripts/compress_specs.py",
    "test:tsan": "bash scripts/tsan-prep.sh && zig build test -Dsanitize=thread --summary all --error-style minimal; tsan_exit=$?; bash scripts/tsan-prep.sh --restore; exit $tsan_exit",
    "test:safe": "zig build test -Doptimize=ReleaseSafe --summary all --error-style minimal",
    "test:fast": "zig build test -Doptimize=ReleaseFast --summary all --error-style minimal",
    "test:slowest": "zig build test-slowest -Doptimize=ReleaseSafe --summary all --error-style minimal",
    "lint": "zsort check . --ban-prefix ./ --ban-prefix src/ && zig fmt --check src && zlint --deny-warnings && zwanzig --format text src && zig build check --summary all",
    "lint:fix": "zsort fix . --ban-prefix ./ --ban-prefix src/ && zig fmt src"
  },
  "dependencies": {
    "@msgpack/msgpack": "^3.1.3",
    "@zyncbase/client": "workspace:*"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.10",
    "@types/bun": "^1.3.11"
  }
}
